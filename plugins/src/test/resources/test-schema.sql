CREATE TABLE ENTIDADCONFIG  (
   ID                   INTEGER                      IDENTITY NOT NULL,
   ENTIDAD_ID           INTEGER,
   DATAKEY              VARCHAR,
   DATAVALUE            VARCHAR,
   CONSTRAINT PK_ENTIDADCONFIG PRIMARY KEY (ID)
);

CREATE TABLE ENTIDAD  (
   ID                   INTEGER                     IDENTITY NOT NULL,
   DESCRIPCION          VARCHAR,
   USUARIOCREAR         VARCHAR                    NOT NULL,
   FECHACREAR           DATETIME                       NOT NULL,
   USUARIOMODIFICAR     VARCHAR,
   FECHAMODIFICAR       DATETIME,
   USUARIOBORRAR        VARCHAR,
   FECHABORRAR          DATETIME,
   BORRADO              INTEGER                    DEFAULT 0 NOT NULL,
   CONSTRAINT PK_ENTIDAD PRIMARY KEY (ID)
);

CREATE TABLE EPL_ENTIDAD_PLUGIN
(
  EPL_ID                   NUMBER(16)           IDENTITY NOT NULL,
  ENTIDAD_ID                NUMBER(16),
  EPL_PLUGIN_CODE             VARCHAR2(50 CHAR)    NOT NULL,
  VERSION                  INTEGER              DEFAULT 0                     NOT NULL,
  USUARIOCREAR             VARCHAR2(10 CHAR)    NOT NULL,
  FECHACREAR               TIMESTAMP(6)         NOT NULL,
  USUARIOMODIFICAR         VARCHAR2(10 CHAR),
  FECHAMODIFICAR           TIMESTAMP(6),
  USUARIOBORRAR            VARCHAR2(10 CHAR),
  FECHABORRAR              TIMESTAMP(6),
  BORRADO              NUMBER(1,0)                    DEFAULT 0 NOT NULL,
  CONSTRAINT PK_EPL_ENTIDAD_PLUGIN PRIMARY KEY (EPL_ID)
);

CREATE TABLE PLC_PLUGIN_CONFIG
(
  PLC_ID                   NUMBER(16)           IDENTITY NOT NULL,
  PLC_PLUGIN_CODE             VARCHAR2(50 CHAR)    NOT NULL,
  PLC_KEY             VARCHAR2(50 CHAR)    NOT NULL,
  PLC_VALUE             VARCHAR2(50 CHAR)    NOT NULL,
  VERSION                  INTEGER              DEFAULT 0                     NOT NULL,
  USUARIOCREAR             VARCHAR2(10 CHAR)    NOT NULL,
  FECHACREAR               TIMESTAMP(6)         NOT NULL,
  USUARIOMODIFICAR         VARCHAR2(10 CHAR),
  FECHAMODIFICAR           TIMESTAMP(6),
  USUARIOBORRAR            VARCHAR2(10 CHAR),
  FECHABORRAR              TIMESTAMP(6),
  BORRADO              NUMBER(1,0)                    DEFAULT 0 NOT NULL,
  CONSTRAINT PK_PLC_PLUGIN_CONFIG PRIMARY KEY (PLC_ID)
);

