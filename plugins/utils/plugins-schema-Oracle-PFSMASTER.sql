CREATE TABLE EPL_ENTIDAD_PLUGIN
(
  EPL_ID                   NUMBER(16)           NOT NULL,
  ENTIDAD_ID               NUMBER(16),
  EPL_PLUGIN_CODE             VARCHAR2(50 CHAR)    NOT NULL,
  VERSION                  INTEGER              DEFAULT 0                     NOT NULL,
  USUARIOCREAR             VARCHAR2(10 CHAR)    NOT NULL,
  FECHACREAR               TIMESTAMP(6)         NOT NULL,
  USUARIOMODIFICAR         VARCHAR2(10 CHAR),
  FECHAMODIFICAR           TIMESTAMP(6),
  USUARIOBORRAR            VARCHAR2(10 CHAR),
  FECHABORRAR              TIMESTAMP(6),
  BORRADO              NUMBER(1,0)                    DEFAULT 0 NOT NULL,
  CONSTRAINT PK_EPL_ENTIDAD_PLUGIN PRIMARY KEY (EPL_ID),
  CONSTRAINT FK_EPL_ENTIDAD_ID_ENTIDAD_ID FOREIGN KEY (ENTIDAD_ID) REFERENCES ENTIDAD (ID)
);

CREATE SEQUENCE S_EPL_ENTIDAD_PLUGIN START WITH 1 NOCYCLE CACHE 100;

GRANT SELECT ON EPL_ENTIDAD_PLUGIN TO PFS01;
GRANT SELECT ON EPL_ENTIDAD_PLUGIN TO PFS02;