/**
 * This view is used to present the details of a single AgendaItem.
 */
Ext.define('HreRem.model.ImagenActivo', {
    extend: 'HreRem.model.Base',

    fields: [
        'id','idActivo','src', 'title', 'alt', 'imgPrincipal'
    ]

});