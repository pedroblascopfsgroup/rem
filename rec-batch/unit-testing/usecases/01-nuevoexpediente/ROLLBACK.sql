SELECT * FROM BATCH_DATOS_SALIDA;

SELECT * FROM BATCH_DATOS_EXP;

SELECT * FROM EXP_EXPEDIENTES WHERE EXP_ID IN (SELECT EXP_ID FROM EXP_EXPEDIENTES WHERE USUARIOCREAR='REC-BATCH');
SELECT * FROM EXP_EXPEDIENTES_REC;
SELECT * FROM CRE_CICLO_RECOBRO_EXP ORDER BY EXP_ID;
SELECT * FROM CEX_CONTRATOS_EXPEDIENTE WHERE EXP_ID IN (SELECT EXP_ID FROM EXP_EXPEDIENTES WHERE USUARIOCREAR='REC-BATCH');
SELECT * FROM CRC_CICLO_RECOBRO_CNT ORDER BY CRE_ID, CNT_ID;
SELECT * FROM PEX_PERSONAS_EXPEDIENTE WHERE EXP_ID IN (SELECT EXP_ID FROM EXP_EXPEDIENTES WHERE USUARIOCREAR='REC-BATCH');
SELECT * FROM CRP_CICLO_RECOBRO_PER ORDER BY CRE_ID , PER_ID;

SELECT * FROM BATCH_DATOS_EXP;

SELECT * FROM REC_FICHERO_CONTRATOS;

SELECT * FROM REC_FICHERO_PERSONAS;

SELECT * FROM REC_FICHERO_CONTRATOS_PERSONAS;

SELECT * FROM REC_FICHERO_DIRECCIONES;

SELECT * FROM REC_FICHERO_TELEFONOS;

DELETE FROM CRP_CICLO_RECOBRO_PER;
DELETE FROM PEX_PERSONAS_EXPEDIENTE WHERE EXP_ID IN (SELECT EXP_ID FROM EXP_EXPEDIENTES WHERE USUARIOCREAR='REC-BATCH');
DELETE FROM CRC_CICLO_RECOBRO_CNT;
DELETE FROM CEX_CONTRATOS_EXPEDIENTE WHERE EXP_ID IN (SELECT EXP_ID FROM EXP_EXPEDIENTES WHERE USUARIOCREAR='REC-BATCH');
DELETE FROM CRE_CICLO_RECOBRO_EXP;
DELETE FROM EXP_EXPEDIENTES_REC;
DELETE FROM EXP_EXPEDIENTES WHERE EXP_ID IN (SELECT EXP_ID FROM EXP_EXPEDIENTES WHERE USUARIOCREAR='REC-BATCH');

COMMIT;

DELETE FROM REC_FICHERO_CONTRATOS;
DELETE FROM REC_FICHERO_PERSONAS;
DELETE FROM REC_FICHERO_CONTRATOS_PERSONAS;

DELETE FROM REC_FICHERO_DIRECCIONES;
DELETE FROM REC_FICHERO_TELEFONOS;

COMMIT;