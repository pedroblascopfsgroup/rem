-- Verificar como cargar datos con el cliente
UPDATE DD_TPR_TIPO_PROD SET DD_TPR_CODIGO = CONCAT('DEF_', DD_TPR_ID) WHERE DD_TPR_CODIGO IS NULL;

-- Agregar constraint
ALTER TABLE DD_TPR_TIPO_PROD MODIFY (DD_TPR_CODIGO VARCHAR2(25 BYTE) NOT NULL);
