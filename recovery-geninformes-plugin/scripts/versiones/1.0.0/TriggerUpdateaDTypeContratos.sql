CREATE OR REPLACE TRIGGER LIN001."UPDATEA_DTYPE_CONTRATOS" 
AFTER INSERT ON LIN001.CNT_CONTRATOS 
BEGIN
        UPDATE CNT_CONTRATOS SET DTYPE='EXTContrato' WHERE DTYPE != 'EXTContrato';
END;
/
