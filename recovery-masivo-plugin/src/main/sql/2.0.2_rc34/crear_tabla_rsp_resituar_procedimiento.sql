DROP SEQUENCE S_RSP_RESITUAR_PROCEDIMIENTO;

CREATE SEQUENCE S_RSP_RESITUAR_PROCEDIMIENTO
  START WITH 1
  MAXVALUE 999999999999999999999999999
  MINVALUE 0
  NOCYCLE
  CACHE 100
  NOORDER;

ALTER TABLE RSP_RESITUAR_PROCEDIMIENTO
 DROP PRIMARY KEY CASCADE;
 
DROP TABLE RSP_RESITUAR_PROCEDIMIENTO CASCADE CONSTRAINTS;

CREATE TABLE RSP_RESITUAR_PROCEDIMIENTO
(
  RSP_ID              NUMBER(16)                NOT NULL,
  ASU_ID              NUMBER(16)                NOT NULL,
  DD_TAC_ID           NUMBER(16)                NOT NULL,
  DD_TPO_ID           NUMBER(16)                NOT NULL,
  TAR_ID              NUMBER(16)                NOT NULL,
  RSP_INSTRUCCIONES   VARCHAR2(2500 BYTE),
  VERSION             INTEGER                   DEFAULT 0                     NOT NULL,
  USUARIOCREAR        VARCHAR2(20 CHAR)         NOT NULL,
  FECHACREAR          TIMESTAMP(6)              NOT NULL,
  USUARIOMODIFICAR    VARCHAR2(10 CHAR),
  FECHAMODIFICAR      TIMESTAMP(6),
  USUARIOBORRAR       VARCHAR2(10 CHAR),
  FECHABORRAR         TIMESTAMP(6),
  BORRADO             NUMBER(1)                 DEFAULT 0                     NOT NULL,
  RSP_FECHA_REVISADO  DATE,
  PRC_ID              NUMBER(16),
  T_REFERENCIA        NUMBER(16),
  RSP_REFERENCIA      NUMBER(16),
  VOLVER_REORGANIZAR  NUMBER(1),
  PRM_ID              NUMBER(16)
);

CREATE UNIQUE INDEX RSP_RESITUAR_PROCEDIMIENTO_PK ON RSP_RESITUAR_PROCEDIMIENTO
(RSP_ID);

ALTER TABLE RSP_RESITUAR_PROCEDIMIENTO ADD (
  CONSTRAINT RSP_RESITUAR_PROCEDIMIENTO_PK
 PRIMARY KEY
 (RSP_ID));