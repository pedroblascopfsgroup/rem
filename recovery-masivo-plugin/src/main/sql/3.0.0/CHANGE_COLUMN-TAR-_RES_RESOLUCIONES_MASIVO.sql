ALTER TABLE RES_RESOLUCIONES_MASIVO DROP COLUMN RES_TAR_ID;

ALTER TABLE RES_RESOLUCIONES_MASIVO ADD RES_TEX_ID NUMBER(16);

ALTER TABLE LIN001.RES_RESOLUCIONES_MASIVO ADD (
  CONSTRAINT FK_RES_TEX_ID 
 FOREIGN KEY (RES_TEX_ID) 
 REFERENCES LIN001.TEX_TAREA_EXTERNA (TEX_ID));