CREATE TABLE CAT_CATEGORIAS
(
  CAT_ID            NUMBER                  	NOT NULL,
  CTG_ID			NUMBER						NOT NULL,
  CAT_NOMBRE        VARCHAR2(200 BYTE)          NOT NULL,
  CAT_DESCRIPCION   VARCHAR2(500 BYTE),
  CAT_ORDEN			NUMBER(3),
  VERSION           INTEGER                     DEFAULT 0                     NOT NULL,
  USUARIOCREAR      VARCHAR2(10 CHAR)           NOT NULL,
  FECHACREAR        TIMESTAMP(6)                NOT NULL,
  USUARIOMODIFICAR  VARCHAR2(10 CHAR),
  FECHAMODIFICAR    TIMESTAMP(6),
  USUARIOBORRAR     VARCHAR2(10 CHAR),
  FECHABORRAR       TIMESTAMP(6),
  BORRADO           NUMBER(1)                   DEFAULT 0                     NOT NULL
);

CREATE UNIQUE INDEX CAT_CATEGORIAS_PK ON CAT_CATEGORIAS (CAT_ID);

ALTER TABLE CAT_CATEGORIAS ADD (CONSTRAINT CAT_CATEGORIAS_PK PRIMARY KEY (CAT_ID) USING INDEX);

 create sequence S_CAT_CATEGORIAS;
