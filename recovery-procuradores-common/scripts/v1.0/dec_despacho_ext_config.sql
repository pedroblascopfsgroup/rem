CREATE TABLE DEC_DESPACHO_EXT_CONFIG
(
  DEC_ID                 NUMBER,
  DES_ID                 NUMBER,
  DEC_CTG_TAREAS         NUMBER,
  DEC_CTG_RESOLUCIONES   NUMBER,
  DEC_DESPACHO_INTEGRAL  NUMBER(1)              DEFAULT 0,
  VERSION                NUMBER                 DEFAULT 0                     NOT NULL,
  USUARIOCREAR           VARCHAR2(10 CHAR)      NOT NULL,
  FECHACREAR             TIMESTAMP(6)           NOT NULL,
  USUARIOMODIFICAR       VARCHAR2(10 CHAR),
  FECHAMODIFICAR         TIMESTAMP(6),
  USUARIOBORRAR          VARCHAR2(10 CHAR),
  FECHABORRAR            TIMESTAMP(6),
  BORRADO                NUMBER(1)              DEFAULT 0                     NOT NULL
)
LOGGING 
NOCOMPRESS 
NOCACHE
NOPARALLEL
MONITORING;


CREATE UNIQUE INDEX DEC_DESPACHO_EXT_CONFIG_PK ON DEC_DESPACHO_EXT_CONFIG (DEC_ID);


CREATE UNIQUE INDEX UK_DEC_DESPACHO_EXT_CONFIG ON DEC_DESPACHO_EXT_CONFIG (DES_ID);


ALTER TABLE DEC_DESPACHO_EXT_CONFIG ADD (CONSTRAINT DEC_DESPACHO_EXT_CONFIG_PK PRIMARY KEY (DEC_ID) USING INDEX);
    

CREATE SEQUENCE S_DEC_DESPACHO_EXT_CONFIG;