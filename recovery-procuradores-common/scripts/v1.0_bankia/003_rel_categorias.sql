CREATE TABLE REL_CATEGORIAS
(
  REL_ID  	    	NUMBER	                    NOT NULL,
  CAT_ID  	    	NUMBER	                    NOT NULL,
  VERSION           INTEGER                     DEFAULT 0                     NOT NULL,
  USUARIOCREAR      VARCHAR2(10 CHAR)           NOT NULL,
  FECHACREAR        TIMESTAMP(6)                NOT NULL,
  USUARIOMODIFICAR  VARCHAR2(10 CHAR),
  FECHAMODIFICAR    TIMESTAMP(6),
  USUARIOBORRAR     VARCHAR2(10 CHAR),
  FECHABORRAR       TIMESTAMP(6),
  BORRADO           NUMBER(1)                   DEFAULT 0                     NOT NULL
)
LOGGING 
NOCOMPRESS 
NOCACHE
NOPARALLEL
NOMONITORING;

CREATE UNIQUE INDEX REL_CATEGORIAS_PK ON REL_CATEGORIAS(REL_ID);

ALTER TABLE REL_CATEGORIAS ADD (CONSTRAINT REL_CATEGORIAS_PK PRIMARY KEY (REL_ID) USING INDEX);

CREATE SEQUENCE S_REL_CATEGORIAS;