ALTER TABLE RES_RESOLUCIONES_MASIVO
ADD RES_ADA_ID NUMBER(16);

ALTER TABLE RES_RESOLUCIONES_MASIVO ADD (
  CONSTRAINT FK_RES_ADA_ID
 FOREIGN KEY (RES_ADA_ID) 
 REFERENCES ADA_ADJUNTOS_ASUNTOS (ADA_ID));

