<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context" 
    xmlns:p="http://www.springframework.org/schema/p"
    xmlns:tx="http://www.springframework.org/schema/tx"
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
               http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-2.5.xsd
               http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-2.5.xsd"
    default-autowire="byName">

    <bean id="activoGenericEnterActionHandler" class="es.pfsgroup.plugin.rem.jbpm.handler.ActivoGenericEnterActionHandler"/>
    <bean id="activoGenericLeaveActionHandler" class="es.pfsgroup.plugin.rem.jbpm.handler.ActivoGenericLeaveActionHandler"/>
    <bean id="activoAdmisionEnterActionHandler" class="es.pfsgroup.plugin.rem.jbpm.handler.ActivoAdmisionEnterActionHandler"/>
	<bean id="activoComprobarGestorActionHandler" class="es.pfsgroup.plugin.rem.jbpm.handler.ActivoComprobarGestorActionHandler"/>
    <bean id="ActivoGenericSaveActionHandler" class="es.pfsgroup.plugin.rem.jbpm.handler.ActivoGenericSaveActionHandler"/>
    <bean id="activoGenericEnterEmailActionHandler" class="es.pfsgroup.plugin.rem.jbpm.handler.ActivoGenericEnterEmailActionHandler"/>
	<bean id="activoComprobarPropuestaPreciosActionHandler" class="es.pfsgroup.plugin.rem.jbpm.handler.ActivoComprobarPropuestaPreciosActionHandler"/>
	<bean id="activoComprobarDatosActionHandler" class="es.pfsgroup.plugin.rem.jbpm.handler.ActivoComprobarDatosActionHandler"/>
	<bean id="resultadoPBCEnterActionHandler" class="es.pfsgroup.plugin.rem.jbpm.handler.ResultadoPBCEnterActionHandler"/>
	<bean id="resolucionExpedienteEnterActionHandler" class="es.pfsgroup.plugin.rem.jbpm.handler.ResolucionExpedienteEnterActionHandler"/>
	<bean id="posicionamientoYFirmaLeaveActionHandler" class="es.pfsgroup.plugin.rem.jbpm.handler.PosicionamientoYFirmaLeaveActionHandler"/>

	<bean id="avanzaBPM" class="es.pfsgroup.plugin.rem.jbpm.handler.ActivoAvanzaBPMActionHandler"/>

	<bean id="activoEndProcessActionHandler" class="es.pfsgroup.plugin.rem.jbpm.handler.ActivoEndProcessActionHandler"/>

	<bean id="activarProrroga" class="es.pfsgroup.plugin.rem.jbpm.handler.ActivoProrrogarTareasActionHandler"/>
	<bean id="tramiteActuacionTecnicaNodoOcultoHandler" class="es.pfsgroup.plugin.rem.jbpm.handler.TramiteActuacionTecnicaNodoOcultoHandler"/>
	<bean id="tramiteSancionOfertaNodoOcultoHandler" class="es.pfsgroup.plugin.rem.jbpm.handler.TramiteSancionOfertaNodoOcultoHandler"/>
	<bean id="routerBPMSancionOfertaHandler" class="es.pfsgroup.plugin.rem.jbpm.handler.routerBPMSancionOfertaHandler"/>

	<bean parent="bpm.processFactory" p:definitionLocation="classpath:process/rem/activo_tramiteAdmision.xml" p:version="2033"/>
	<bean parent="bpm.processFactory" p:definitionLocation="classpath:process/rem/activo_tramiteDocumental.xml" p:version="-1"/>
	<bean parent="bpm.processFactory" p:definitionLocation="classpath:process/rem/activo_tramiteDocumentalCEE.xml" p:version="2042"/>
	<bean parent="bpm.processFactory" p:definitionLocation="classpath:process/rem/activo_tramiteActuacionTecnica.xml" p:version="-1"/>
	<bean parent="bpm.processFactory" p:definitionLocation="classpath:process/rem/activo_tramiteTasacion.xml" p:version="2062"/>
	<bean parent="bpm.processFactory" p:definitionLocation="classpath:process/rem/activo_tramiteInforme.xml" p:version="2066"/>
	<bean parent="bpm.processFactory" p:definitionLocation="classpath:process/rem/activo_tramiteFacturacion.xml" p:version="2072"/>
	<bean parent="bpm.processFactory" p:definitionLocation="classpath:process/rem/activo_tramiteDocumentalCedula.xml" p:version="2081"/>
	<bean parent="bpm.processFactory" p:definitionLocation="classpath:process/rem/activo_tramitePropuestaPrecios.xml" p:version="2028"/>
	<bean parent="bpm.processFactory" p:definitionLocation="classpath:process/rem/activo_tramiteActualizacionPrecios.xml" p:version="2035"/>
	<bean parent="bpm.processFactory" p:definitionLocation="classpath:process/rem/activo_tramiteActualizacionEstados.xml" p:version="2037"/>
	<bean parent="bpm.processFactory" p:definitionLocation="classpath:process/rem/activo_tramiteAprobacionInformeComercial.xml" p:version="255"/>
	<bean parent="bpm.processFactory" p:definitionLocation="classpath:process/rem/activo_tramiteSancionOfertaAlquiler.xml" p:version="2042"/>
	<bean parent="bpm.processFactory" p:definitionLocation="classpath:process/rem/activo_tramiteSancionOferta.xml" p:version="-1"/>
	<bean parent="bpm.processFactory" p:definitionLocation="classpath:process/rem/activo_tramiteComercialAlquiler.xml" p:version="-1"/>
    <bean parent="bpm.processFactory" p:definitionLocation="classpath:process/rem/activo_tramiteComunicacionGENCAT.xml" p:version="-1"/>
    <bean parent="bpm.processFactory" p:definitionLocation="classpath:process/rem/activo_tramiteSancionOfertaApple.xml" p:version="-1"/>
    <bean parent="bpm.processFactory" p:definitionLocation="classpath:process/rem/activo_tramiteAlquilerNoComercial.xml" p:version="200"/>

	<bean id="jbpmActivoScriptExecutor" class="es.pfsgroup.plugin.rem.jbpm.activo.JBPMActivoScriptExecutor" >
  	<property name="contextScripts">
  		<list>
  			<value><![CDATA[
            def damePlazo= {
  				new java.text.SimpleDateFormat('yyyy-MM-dd').parse(it).getTime()-System.currentTimeMillis()
  			}
            def hoyDate = {
            	def trabajoManager = ctx.getBean("trabajoManager");
                return trabajoManager.hoyDateServer(); 
  			}
            def diffDate(String date1, String date2) {
            	def trabajoManager = ctx.getBean("trabajoManager");
                return trabajoManager.diffDate(date1, date2);
  			}
  			def esFechaIgual(String date1, String date2) {
  				if(date2 != null){
	  				if ("IGUAL".equals(diffDate(date1, date2))){
	  					return true;
	  				}else{
	  					return false;
	  				}
	  			}else{
	  				return false;
	  			}
  			}
            def esFechaMayor(String date1, String date2) {
            	if(date2 != null){
	            	if ("MAYOR".equals(diffDate(date1, date2))){
	            		return true;
	            	}else{
	            		return false;
	            	}
	            }else{
	            	return false;
	            }
  			}
            def esFechaMenor(String date1, String date2) {
            	if(date2 != null && date1 != null){
	            	if ("MENOR".equals(diffDate(date1, date2))){
	            		return true;
	            	}else{
	            		return false;
	            	}
	            }else{
	            	return false;
	            }
  			}
            def esFechaMayorIgual(String date1, String date2) {
            	if(date2 != null){
	            	if ("MAYOR".equals(diffDate(date1, date2)) || "IGUAL".equals(diffDate(date1, date2))){
	            		return true;
	            	}else{
	            		return false;
	            	}
	            }else{
	            	return false;
	            }
  			}
            def esFechaMenorIgual(String date1, String date2) {
            	if(date2 != null){
	            	if ("MENOR".equals(diffDate(date1, date2)) || "IGUAL".equals(diffDate(date1, date2))){
	            		return true;
	            	}else{
	            		return false;
	            	}
	            }else{
	            	return false;
	            }
  			}
            def esFechaEntre(String date1, String date2, String date3) {
            	if(date1 != null && date3 != null){
	            	if (esFechaMayor(date1, date2)){
	            		return false;
	            	}else{
						if (esFechaMayor(date2, date3)){
							return false;
						}else{
	            			return true;
	            		}
	            	}
	            }else{
	            	return false;
	            }
  			}  			
            def fechaSolicitudTrabajo = {
            	def trabajoManager = ctx.getBean("trabajoManager");
                return trabajoManager.getFechaSolicitudTrabajo(tareaExterna);
            }
            def fechaAprobacionTrabajo = {
            	def trabajoManager = ctx.getBean("trabajoManager");
                return trabajoManager.getFechaAprobacionTrabajo(tareaExterna);
            }
            def fechaRechazoTrabajo = {
            	def trabajoManager = ctx.getBean("trabajoManager");
                return trabajoManager.getFechaRechazoTrabajo(tareaExterna);
            }
            def fechaEleccionProveedorTrabajo = {
            	def trabajoManager = ctx.getBean("trabajoManager");
                return trabajoManager.getFechaEleccionProveedorTrabajo(tareaExterna);
            }
            def fechaEjecutadoTrabajo = {
            	def trabajoManager = ctx.getBean("trabajoManager");
                return trabajoManager.getFechaEjecutadoTrabajo(tareaExterna);
            }
            def fechaValidacionTrabajo = {
            	def trabajoManager = ctx.getBean("trabajoManager");
                return trabajoManager.getFechaValidacionTrabajo(tareaExterna);
            }
            def fechaAnulacionTrabajo = {
            	def trabajoManager = ctx.getBean("trabajoManager");
                return trabajoManager.getFechaAnulacionTrabajo(tareaExterna);
            }
            def fechaCierreEcoTrabajo = {
            	def trabajoManager = ctx.getBean("trabajoManager");
                return trabajoManager.getFechaCierreEcoTrabajo(tareaExterna);
            }
            def fechaPagoTrabajo = {
            	def trabajoManager = ctx.getBean("trabajoManager");
                return trabajoManager.getFechaPagoTrabajo(tareaExterna);
            }
            def fechaPosicionamiento = {
            	def ofertaManager = ctx.getBean("ofertaManager");
            	return ofertaManager.getFechaPosicionamiento(tareaExterna);
            }
			def comprobacionGestorActivo = {
            	def gestorActivo = ctx.getBean("gestorActivoManager");
                return gestorActivo.isGestorActivo(tareaExterna);
            }
            def comprobarPestanaCheckingInformacion = {
            	def activoManager = ctx.getBean("activoManager");
				return activoManager.comprobarPestanaCheckingInformacion(idActivo);
            }
            def comprobarExisteAdjuntoActivo(String codigoDocumento) {
            	def activoManager = ctx.getBean("activoManager");
				return activoManager.comprobarExisteAdjuntoActivo(idActivo, codigoDocumento);
            }
            def comprobarExisteAdjuntoActivoTarea = {
            	def activoManager = ctx.getBean("activoManager");
            	return activoManager.comprobarExisteAdjuntoActivoTarea(tareaExterna);
            }
			def comprobarExisteFechaPosesionActivo = {
            	def gestorActivo = ctx.getBean("gestorActivoManager");
                return activoManager.comprobarExisteFechaPosesionActivo(idActivo);
            }
            def comprobarExistePresupuestoTrabajo = {
            	def trabajoManager = ctx.getBean("trabajoManager");
                return trabajoManager.existePresupuestoTrabajo(tareaExterna);
            }
            def comprobarExisteProveedorTrabajo = {
            	def trabajoManager = ctx.getBean("trabajoManager");
                return trabajoManager.existeProveedorTrabajo(tareaExterna);
            }
            def comprobarExisteTarifaTrabajo = {
            	def trabajoManager = ctx.getBean("trabajoManager");
                return trabajoManager.existeTarifaTrabajo(tareaExterna);
            }
            def comprobarExisteTarifaSuperiorCeroTrabajo = {
            	def trabajoManager = ctx.getBean("trabajoManager");
                return trabajoManager.existeTarifaSuperiorCeroTrabajo(tareaExterna);
            }
			def comprobarObligatoriosCheckingInfoActivo = {
            	def activoManager = ctx.getBean("activoManager");
            	return activoManager.comprobarObligatoriosCheckingInfoActivo(idActivo);
			}
			
            def existeAdjuntoUG(String codigo, String unidadGestion) {
            	def activoTramiteManager = ctx.getBean("activoTramiteManager");
				return activoTramiteManager.existeAdjuntoUG(tareaExterna, codigo, unidadGestion);
            }
            def existeAdjuntoUGCartera(String codigo, String unidadGestion, String cartera) {
                def activoTramiteManager = ctx.getBean("activoTramiteManager");
                return activoTramiteManager.existeAdjuntoUGCartera(tareaExterna, codigo, unidadGestion, cartera);
            }
            def existeAdjuntoUG(String cadenaDAUG) {
            	def activoTramiteManager = ctx.getBean("activoTramiteManager");
				return activoTramiteManager.existeAdjuntoUGCadena(tareaExterna, cadenaDAUG);
            }
            def existeAdjuntoUGValidacion(String codigo, String unidadGestion) {
				def activoTramiteManager = ctx.getBean("activoTramiteManager");
            	return activoTramiteManager.existeAdjuntoUGValidacion(tareaExterna, codigo, unidadGestion);
            }
            def existeAdjuntoUGCarteraValidacion(String codigo, String unidadGestion, String cartera) {
				def activoTramiteManager = ctx.getBean("activoTramiteManager");
				return activoTramiteManager.existeAdjuntoUGCarteraValidacion(tareaExterna, codigo, unidadGestion, cartera);
            }
            def existeAdjuntoUGValidacion(String cadenaDAUG) {
				def activoTramiteManager = ctx.getBean("activoTramiteManager");
            	return activoTramiteManager.existeAdjuntoUGValidacionCadena(tareaExterna, cadenaDAUG);
            }
            def mismoNumeroAdjuntosComoActivosExpedienteUGValidacion(String codigo, String unidadGestion) {
				def activoTramiteManager = ctx.getBean("activoTramiteManager");
            	return activoTramiteManager.mismoNumeroAdjuntosComoActivosExpedienteUGValidacion(tareaExterna, codigo, unidadGestion);
            }
            
            def mismoNumeroAdjuntosComoCompradoresExpedienteUGValidacion(String codigo, String unidadGestion, String cartera){
            	def activoTramiteManager = ctx.getBean("activoTramiteManager");
            	return activoTramiteManager.mismoNumeroAdjuntosComoCompradoresExpedienteUGValidacion(tareaExterna, codigo, unidadGestion, cartera);
            }
            
            def mismoNumeroAdjuntosComoTareasTipoUGValidacion(String codigoDocAdjunto, String uGestion, Long idTareaProcedimiento){
            	def activoTramiteManager = ctx.getBean("activoTramiteManager");
            	return activoTramiteManager.mismoNumeroAdjuntosComoTareasTipoUGValidacion(tareaExterna, codigoDocAdjunto, uGestion, idTareaProcedimiento);
            }
            
            def checkFechaEmisionInformeJuridico() {
				def activoTramiteManager = ctx.getBean("activoTramiteManager");
            	return activoTramiteManager.checkFechaEmisionInformeJuridico(tareaExterna);
            }
            def checkFormalizacion = {
            	def trabajoManager = ctx.getBean("trabajoManager");
            	return trabajoManager.checkFormalizacion(tareaExterna);
            }
            def checkReservaNecesariaNotNull = {
            	def trabajoManager = ctx.getBean("trabajoManager");
            	return trabajoManager.checkReservaNecesariaNotNull(tareaExterna);
            }
            def definicionOfertaT013(String codigo) {
            	def validateJbpmManager = ctx.getBean("validateJbpmManager");
            	return validateJbpmManager.definicionOfertaT013(tareaExterna, codigo, valores);
            }
            def tieneTramiteGENCATVigenteByIdActivo = {
            	def activoTramiteManager = ctx.getBean("activoTramiteManager");
            	return activoTramiteManager.tieneTramiteGENCATVigenteByIdActivo(tareaExterna);
            }
            def respuestaOfertanteT013(String importeOfertante) {
            	def validateJbpmManager = ctx.getBean("validateJbpmManager");
            	return validateJbpmManager.respuestaOfertanteT013(tareaExterna, importeOfertante);
            }  
            def resolucionComiteT013 = {
            	def validateJbpmManager = ctx.getBean("validateJbpmManager");
            	return validateJbpmManager.resolucionComiteT013(tareaExterna, valores);
            }            
            def ratificacionComiteT013 = {
            	def validateJbpmManager = ctx.getBean("validateJbpmManager");
            	return validateJbpmManager.ratificacionComiteT013(tareaExterna);
            }
            def checkDeDerechoTanteo = {
            	def ofertaManager = ctx.getBean("ofertaManager");
            	return ofertaManager.checkDeDerechoTanteo(tareaExterna);
            }
     		def checkDerechoTanteo = {
     			def ofertaManager = ctx.getBean("ofertaManager");
     			return ofertaManager.checkDerechoTanteo(tareaExterna);
     		}
     		def checkEjercidoTanteo= {
     			def ofertaManager = ctx.getBean("ofertaManager");
     			return ofertaManager.checkEjercidoTanteo(tareaExterna);
     		}    		
     		def checkRenunciaTanteo= {
     			def ofertaManager = ctx.getBean("ofertaManager");
     			return ofertaManager.checkRenunciaTanteo(tareaExterna);
     		}
            def checkComiteSancionador = {
            	def ofertaManager = ctx.getBean("ofertaManager");
            	return ofertaManager.checkComiteSancionador(tareaExterna);
            }
            def checkComiteSancionadorAlquilerHaya = {
            	def ofertaManager = ctx.getBean("ofertaManager");
            	return ofertaManager.checkComiteSancionadorAlquilerHaya(tareaExterna);
            }
            def checkAtribuciones = {
            	def ofertaManager = ctx.getBean("ofertaManager");
            	return ofertaManager.checkAtribuciones(tareaExterna);
            }
            def esMayorista = {
            	def ofertaManager = ctx.getBean("ofertaManager");
            	return ofertaManager.esMayorista(tareaExterna);
            }
            def checkReserva = {
            	def ofertaManager = ctx.getBean("ofertaManager");
            	return ofertaManager.checkReserva(tareaExterna);
            }
            def checkTango = {
            	def trabajoManager = ctx.getBean("trabajoManager");
            	return trabajoManager.checkTango(tareaExterna);
            }
            def checkSareb = {
            	def trabajoManager = ctx.getBean("trabajoManager");
            	return trabajoManager.checkSareb(tareaExterna);
            }
            def checkBankia = {
            	def trabajoManager = ctx.getBean("trabajoManager");
            	return trabajoManager.checkBankia(tareaExterna);
            }
            def checkCajamar = {
            	def trabajoManager = ctx.getBean("trabajoManager");
            	return trabajoManager.checkCajamar(tareaExterna);
            }
            def checkLiberbank = {
            	def trabajoManager = ctx.getBean("trabajoManager");
            	return trabajoManager.checkLiberbank(tareaExterna);
            }
            def checkGiants = {
            	def trabajoManager = ctx.getBean("trabajoManager");
            	return trabajoManager.checkGiants(tareaExterna);
            }
            
            def checkExpedienteFechaChequeLiberbank = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkExpedienteFechaChequeLiberbank(idTramite);
            }
            def checkExpedienteBloqueado = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkExpedienteBloqueado(idTramite);
            }
            def reservaFirmada = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.reservaFirmada(idTramite);
            }
            def comprobarActivoComercializable = {
            	def activoManager = ctx.getBean("activoManager");
            	return activoManager.comprobarActivoComercializable(idActivo);
            }
            def comprobarComiteLiberbankPlantillaPropuesta = {
            	def ofertaManager = ctx.getBean("ofertaManager");
            	return ofertaManager.comprobarComiteLiberbankPlantillaPropuesta(tareaExterna);
            }
            def comprobarObligatoriosDesignarMediador = {
            	def activoManager = ctx.getBean("activoManager");
                return activoManager.comprobarObligatoriosDesignarMediador(idActivo);
            }
            def checkSinRiesgoReputacional = {
            	def ofertaManager = ctx.getBean("ofertaManager");
            	return ofertaManager.checkSinRiesgoReputacional(tareaExterna);
            }
			def checkRiesgoReputacional = {
				def ofertaManager = ctx.getBean("ofertaManager");
				return ofertaManager.checkRiesgoReputacional(tareaExterna);
			}
			def checkImporte = {
				def ofertaManager = ctx.getBean("ofertaManager");
				return ofertaManager.checkImporte(tareaExterna);
			}
			def checkCompradores = {
				def ofertaManager = ctx.getBean("ofertaManager");
				return ofertaManager.checkCompradores(tareaExterna);
			}
			def checkConflictoIntereses = {
				def ofertaManager = ctx.getBean("ofertaManager");
				return ofertaManager.checkConflictoIntereses(tareaExterna);
			}
            def comprobarPropuestaPrecios = {
            	def trabajoManager = ctx.getBean("trabajoManager");
            	return trabajoManager.comprobarPropuestaPrecios(tareaExterna);
            }
			def checkAdmisionAndGestion = {
				def activoManager = ctx.getBean("activoManager");
				return activoManager.checkAdmisionAndGestion(tareaExterna);
			}
			def checkTiposDistintos = {
				def activoManager = ctx.getBean("activoManager");
				return activoManager.checkTiposDistintos(tareaExterna);
			}
			def checkVPO = {
				def activoManager = ctx.getBean("activoManager");
				return activoManager.checkVPO(tareaExterna);
			}
			def altaComite = {
				def ofertaManager = ctx.getBean("ofertaManager");
				return ofertaManager.altaComite(tareaExterna);
			}
			def ratificacionComite = {
				def ofertaManager = ctx.getBean("ofertaManager");
				return ofertaManager.ratificacionComite(tareaExterna);
			}
			def altaComiteProcess = {
				def ofertaManager = ctx.getBean("ofertaManager");
				return ofertaManager.altaComiteProcess(tareaExterna);
			}
			def ratificacionComiteProcess = {
				def ofertaManager = ctx.getBean("ofertaManager");
				return ofertaManager.ratificacionComiteProcess(tareaExterna);
			}
			def checkPoliticaCorporativa = {
				def ofertaManager = ctx.getBean("ofertaManager");
				return ofertaManager.checkPoliticaCorporativa(tareaExterna);
			}
			def checkPosicionamiento = {
				def ofertaManager = ctx.getBean("ofertaManager");
				return ofertaManager.checkPosicionamiento(tareaExterna);
			}
			def	checkEjerce = {
				def ofertaManager = ctx.getBean("ofertaManager");
				return ofertaManager.checkEjerce(tareaExterna);
			}
			def checkSuperaPresupuestoActivo = {
            	def trabajoManager = ctx.getBean("trabajoManager");
            	return trabajoManager.checkSuperaPresupuestoActivoTarea(tareaExterna);
            }
            def checkSuperaDelegacion = {
            	def trabajoManager = ctx.getBean("trabajoManager");
            	return trabajoManager.checkSuperaDelegacion(tareaExterna);
            }
			def checkHayPresupuestoEjercicioActual = {
				def activoManager = ctx.getBean("activoManager");
				return activoManager.checkHayPresupuestoEjercicioActual(idActivo);
			}
			def checkImpuestos = {
				def ofertaManager = ctx.getBean("ofertaManager");
				return ofertaManager.checkImpuestos(tareaExterna);
			}
			def checkComercializable = {
            	def activoManager = ctx.getBean("activoManager");
            	return activoManager.checkComercializable(idActivo);
            }
            def checkVendido = {
            	def activoManager = ctx.getBean("activoManager");
            	return activoManager.checkVendido(idActivo);
            }
            def checkImporteParticipacion = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkImporteParticipacion(idTramite);
            }
            def checkInformeJuridicoFinalizado = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkInformeJuridicoFinalizado(idTramite);
            }
            def checkEsMultiactivo = {
            	def trabajoManager = ctx.getBean("trabajoManager");
            	return trabajoManager.checkEsMultiactivo(tareaExterna);
            }
            def checkCompradoresTienenNumeroUrsus = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkCompradoresTienenNumeroUrsus(idTramite);
            }
            def checkEstadoExpedienteDistintoAnulado = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkEstadoExpedienteDistintoAnulado(idTramite);
            }
            def checkActivoEnAgrupacionRestringida = {
            	def activoManager = ctx.getBean("activoManager");
            	return activoManager.isActivoIntegradoAgrupacionRestringida(idActivo);
            }
            def checkActivoEnAgrupacionComercial = {
            	def activoManager = ctx.getBean("activoManager");
            	return activoManager.isActivoIntegradoAgrupacionComercial(idActivo);
            }
            def esPopietarioRemaining = {
            	def activoManager = ctx.getBean("activoManager");
            	return activoManager.esPopietarioRemaining(tareaExterna);
            }
            def esPopietarioArrow = {
            	def activoManager = ctx.getBean("activoManager");
            	return activoManager.esPopietarioArrow(tareaExterna);
            }
            def checkReservaFirmada = {
				def ofertaManager = ctx.getBean("ofertaManager");
				return ofertaManager.checkReservaFirmada(tareaExterna);
			}
			def checkTarifaPlana = {
				def trabajoManager = ctx.getBean("trabajoManager");
                return trabajoManager.trabajoTieneTarifaPlana(tareaExterna);
			}
			def esLiberBank = {
				def activoManager = ctx.getBean("activoManager");
            	return activoManager.esLiberBank(idActivo);
			}
			def esCajamar = {
				def activoManager = ctx.getBean("activoManager");
            	return activoManager.esCajamar(idActivo);
			}
			def esCerberus = {
				def activoManager = ctx.getBean("activoManager");
            	return activoManager.esCerberus(idActivo);
			}
			def esSubcarteraJaipurInmobiliario = {
				def activoManager = ctx.getBean("activoManager");
            	return activoManager.esSubcarteraJaipurInmobiliario(idActivo);
			}
			def esSubcarteraAgoraInmobiliario = {
				def activoManager = ctx.getBean("activoManager");
            	return activoManager.esSubcarteraAgoraInmobiliario(idActivo);
			}
			def esSubcarteraEgeo = {
				def activoManager = ctx.getBean("activoManager");
            	return activoManager.esSubcarteraEgeo(idActivo);
			}
			def esEgeo = {
				def activoManager = ctx.getBean("activoManager");
            	return activoManager.esEgeo(idActivo);
			}
			def esSubcarteraZeus = {
				def activoManager = ctx.getBean("activoManager");
            	return activoManager.esSubcarteraZeus(idActivo);
			}
			def esSubcarteraPromontoria = {
				def activoManager = ctx.getBean("activoManager");
            	return activoManager.esSubcarteraPromontoria(idActivo);
			}
			def esSubcarteraApple = {
				def activoManager = ctx.getBean("activoManager");
            	return activoManager.esSubcarteraApple(idActivo);
			}
			def superaLimiteLiberbank = {
				def trabajoManager = ctx.getBean("trabajoManager");
            	return trabajoManager.superaLimiteLiberbank(idTramite);
            }
			def trabajoEsTarificado = {
				def trabajoManager = ctx.getBean("trabajoManager");
            	return trabajoManager.trabajoEsTarificado(idTramite);
			}
			def checkFechaVenta = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkFechaVenta(idTramite);
            }
            def checkActivoNoFormalizar = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkActivoNoFormalizar(idTramite);
            }
            def checkProvinciaCompradores = {
				def ofertaManager = ctx.getBean("ofertaManager");
				return ofertaManager.checkProvinciaCompradores(tareaExterna);
			}
			def checkPrecontratoSubido = {
				def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkPrecontratoSubido(tareaExterna);
			}
			def checkNifConyugueLBB = {
				def ofertaManager = ctx.getBean("ofertaManager");
				return ofertaManager.checkNifConyugueLBB(tareaExterna);
			}
			def activoEnTramite = {
				def trabajoManager = ctx.getBean("trabajoManager");
            	return trabajoManager.activoEnTramite(idActivo);
			}
			
			def esActivoContabilizado = {
				def trabajoManager = ctx.getBean("trabajoManager");
            	return trabajoManager.esActivoContabilizado(idActivo);
			}
			
			def checkContratoSubido = {
				def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkContratoSubido(tareaExterna);
			}
			def checkEstadoOcupadoTramite = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkEstadoOcupadoTramite(idTramite);
            }
            def checkConTituloTramite = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkConTituloTramite(idTramite);
            }
            def checkConOpcionCompra = {
				def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkConOpcionCompra(tareaExterna);
			}
            def checkAmConUasConOfertasVivas = {
				def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkAmConUasConOfertasVivas(tareaExterna);
			}
            def checkDepositoDespublicacionSubido = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkDepositoDespublicacionSubido(tareaExterna);
            }
	        def esAgora = {
				def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.esAgora(tareaExterna);
			}
			def checkDepositoRelleno = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkDepositoRelleno(tareaExterna);
            }
            def esApple = {
				def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.esApple(tareaExterna);
			}
            def checkCamposComprador = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkCamposComprador(tareaExterna);
            }
            def checkPaseDirectoPendDevol = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkPaseDirectoPendDevol(tareaExterna);
            }
            def checkInquilinos = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkInquilinos(tareaExterna);
            }
            def checkDiscrepanciasUrsus = { 
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkDiscrepanciasUrsus(idTramite);
            }
            def existeEnTablaDeConfiguracion = { 
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.existeEnTablaDeConfiguracion(tareaExterna);
            }
            def esYubai = {
				def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.esYubai(tareaExterna);
			}
			def isOfertaDependiente = { 
				def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.isOfertaDependiente(tareaExterna);
			}
			def checkVieneDeRatificacionCES = {
				def activoTramiteManager = ctx.getBean("activoTramiteManager");
				return activoTramiteManager.checkVieneDeRatificacionCES(idTramite);
			}
			def checkInformeJuridicoYResolucionManzanaCompletadas = {
				def activoTramiteManager = ctx.getBean("activoTramiteManager");
				return activoTramiteManager.checkInformeJuridicoYResolucionManzanaCompletadas(idTramite);
			}
			def isValidateOfertasDependientes = {
				def ofertaManager = ctx.getBean("ofertaManager");
            	return ofertaManager.isValidateOfertasDependientes(tareaExterna, valores);
			}
			def checkExpedienteFechaCheque = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkExpedienteFechaCheque(idTramite);
            }
            def esOmega = {
				def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.esOmega(tareaExterna);
            }
            def checkTipoImpuesto = {
				def ofertaManager = ctx.getBean("ofertaManager");
				return ofertaManager.checkTipoImpuesto(tareaExterna);
			}
			def checkReservaInformada = {
				def ofertaManager = ctx.getBean("ofertaManager");
				return ofertaManager.checkReservaInformada(tareaExterna);
			}
			def checkContabilizacionReserva = {
				def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkContabilizacionReserva(tareaExterna);
			}
			def fechaReservaPBCReserva = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.fechaReservaPBCReserva(tareaExterna);
            }
            def esOfertaPrincipalSinDependientes = {
            	def activoTramiteManager = ctx.getBean("activoTramiteManager");
				return activoTramiteManager.esOfertaPrincipalSinDependientes(tareaExterna);
            }
            def esBBVA = {
				def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.esBBVA(tareaExterna);
            }
            def compruebaEstadoNoSolicitadoPendiente = {
				def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
	           	return expedienteComercialManager.compruebaEstadoNoSolicitadoPendiente(tareaExterna);
            }
            def compruebaEstadoPositivoRealDenegado = {
				def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
	           	return expedienteComercialManager.compruebaEstadoPositivoRealDenegado(tareaExterna);
	        }
            def tipoTratamiento = {
				def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.tipoTratamiento(tareaExterna);
            }          
            def checkVueltaAtras = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkVueltaAtras(idTramite);
            }
            def haPasadoScoring = {
            	def tramiteAlquilerManager = ctx.getBean("tramiteAlquilerManager");
            	return tramiteAlquilerManager.haPasadoScoring(idTramite);
            }
            def esDespuesElevar = {
            	def tramiteAlquilerManager = ctx.getBean("tramiteAlquilerManager");
            	return tramiteAlquilerManager.esDespuesElevar(idTramite);

            }
             def haPasadoScoringBC = {
            	def tramiteAlquilerManager = ctx.getBean("tramiteAlquilerManager");
            	return tramiteAlquilerManager.haPasadoScoringBC(idTramite);
            }
             def tipoTratamientoAlquiler = {
            	def tramiteAlquilerManager = ctx.getBean("tramiteAlquilerManager");
            	return tramiteAlquilerManager.tipoTratamientoAlquiler(idTramite);
            }
             def checkAprobadoRechazadoBC = {
            	def tramiteVentaManager = ctx.getBean("tramiteVentaManager");
            	return tramiteVentaManager.checkAprobadoRechazadoBC(tareaExterna);
            }
            def checkAprobadoRechazadoBCPosicionamiento = {
            	def tramiteVentaManager = ctx.getBean("tramiteVentaManager");
            	return tramiteVentaManager.checkAprobadoRechazadoBCPosicionamiento(tareaExterna);
            }
            def actualizarOfertaBoarding = {
				def ofertaManager = ctx.getBean("ofertaManager");
				return ofertaManager.actualizarOfertaBoarding(tareaExterna);
			}
			def isOfertaContraOfertaMayor10K = {
				def tramiteAlquilerManager = ctx.getBean("tramiteAlquilerManager");
				return tramiteAlquilerManager.isOfertaContraOfertaMayor10K(tareaExterna);
			}
			def isMetodoActualizacionRelleno = {
				def tramiteAlquilerManager = ctx.getBean("tramiteAlquilerManager");
				return tramiteAlquilerManager.isMetodoActualizacionRelleno(tareaExterna);
			}
			def aprobarRevisionBcYCondiciones = {
				def tramiteAlquilerNoComercialManager = ctx.getBean("tramiteAlquilerNoComercialManager");
				return tramiteAlquilerNoComercialManager.aprobarRevisionBcYCondiciones(tareaExterna);
			}
			def checkAvalCondiciones = {
				def tramiteAlquilerManager = ctx.getBean("tramiteAlquilerManager");
				return tramiteAlquilerManager.checkAvalCondiciones(tareaExterna);
			}
			def checkSeguroRentasCondiciones = {
				def tramiteAlquilerManager = ctx.getBean("tramiteAlquilerManager");
				return tramiteAlquilerManager.checkSeguroRentasCondiciones(tareaExterna);
			}
			def validarMesesImporteDeposito = {
				def tramiteAlquilerManager = ctx.getBean("tramiteAlquilerManager");
				return tramiteAlquilerManager.validarMesesImporteDeposito(tareaExterna);
			}
			def avanzaAprobarPbcAlquiler = {
				def tramiteAlquilerNoComercialManager = ctx.getBean("tramiteAlquilerNoComercialManager");
				return tramiteAlquilerNoComercialManager.avanzaAprobarPbcAlquiler(tareaExterna);
			}
			def avanzaScoringBC(String codigo) {
				def tramiteAlquilerNoComercialManager = ctx.getBean("tramiteAlquilerNoComercialManager");
				return tramiteAlquilerNoComercialManager.avanzaScoringBC(tareaExterna, codigo);
			}

			def existeExpedienteComercialByNumExpediente(String expedienteAnterior) {
            	def tramiteAlquilerNoComercialManager = ctx.getBean("tramiteAlquilerNoComercialManager");
            	return tramiteAlquilerNoComercialManager.existeExpedienteComercialByNumExpediente(tareaExterna, expedienteAnterior);
            }  
			            
            def isExpedienteFirmado(String expedienteAnterior) {
            	def tramiteAlquilerNoComercialManager = ctx.getBean("tramiteAlquilerNoComercialManager");
            	return tramiteAlquilerNoComercialManager.isExpedienteFirmado(tareaExterna, expedienteAnterior);
            }  
             def userHasPermisoParaAvanzarTareas = {
            	def tramiteVentaManager = ctx.getBean("tramiteVentaManager");
            	return tramiteVentaManager.userHasPermisoParaAvanzarTareas(tareaExterna);
            }
            
            def checkExpedienteBloqueadoPorFuncion = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkExpedienteBloqueadoPorFuncion(idTramite);
            }
            def tieneRellenosCamposAnulacion = {
            	def funcionesTramitesManager = ctx.getBean("funcionesTramitesManager");
            	return funcionesTramitesManager.tieneRellenosCamposAnulacion(tareaExterna);
            }
            def tieneFechaVencimientoReserva = {
            	def tramiteVentaManager = ctx.getBean("tramiteVentaManager");
            	return tramiteVentaManager.tieneFechaVencimientoReserva(tareaExterna);
            }
            def checkArrasEstadoBCIngreso = {
            	def tramiteVentaManager = ctx.getBean("tramiteVentaManager");
            	return tramiteVentaManager.checkArrasEstadoBCIngreso(tareaExterna);
            }
            def tieneReservaPrevia = {
            	def tramiteVentaManager = ctx.getBean("tramiteVentaManager");
            	return tramiteVentaManager.tieneReservaPrevia(tareaExterna);
            }
             def checkFechaContabilizacionArras = {
            	def tramiteVentaManager = ctx.getBean("tramiteVentaManager");
            	return tramiteVentaManager.checkFechaContabilizacionArras(tareaExterna);
            }
            
             def checkGarantiasNinguna (String expedienteAnterior) {
            	def tramiteAlquilerManager = ctx.getBean("tramiteAlquilerManager");
				return tramiteAlquilerManager.checkGarantiasNinguna(tareaExterna, expedienteAnterior);
            }
            
            def existeTareaT018Scoring = {
				def tramiteAlquilerNoComercialManager = ctx.getBean("tramiteAlquilerNoComercialManager");
				return tramiteAlquilerNoComercialManager.existeTareaT018Scoring(tareaExterna);
			}
			 def isExpedienteDelMismoActivo(String expedienteAnterior) {
            	def tramiteAlquilerNoComercialManager = ctx.getBean("tramiteAlquilerNoComercialManager");
            	return tramiteAlquilerNoComercialManager.isExpedienteDelMismoActivo(tareaExterna, expedienteAnterior);
            } 
            def checkEstadoBC = {
            	def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.checkEstadoBC(tareaExterna);
            }
			
			def esTitulizada = {
				def expedienteComercialManager = ctx.getBean("expedienteComercialManager");
            	return expedienteComercialManager.esTitulizada(tareaExterna);
			}
           
  			 ]]></value>
  		</list>
  	</property>
  </bean>

  <bean parent="clasesDiccionarioAnotadas">
    <property name="sourceList">
           <list merge="true">
              <value>es.pfsgroup.plugin.recovery.coreextension.subasta.model.DDResultadoComite</value>
              <value>es.pfsgroup.plugin.recovery.coreextension.subasta.model.DDDecisionSuspension</value>
              <value>es.pfsgroup.plugin.recovery.coreextension.subasta.model.DDMotivoSuspSubasta</value>
              <value>es.pfsgroup.plugin.recovery.coreextension.model.DDGestoria</value>
              <value>es.pfsgroup.plugin.recovery.coreextension.model.DDResultadoInforme</value>
		      <value>es.pfsgroup.plugin.recovery.coreextension.model.DDTipoFondo</value>  
		      <value>es.pfsgroup.plugin.recovery.coreextension.model.DDResultadoComiteConcursal</value>   
		      <value>es.pfsgroup.plugin.recovery.coreextension.model.DDTipoConcurso</value>   
		      <value>es.pfsgroup.plugin.recovery.coreextension.model.DDMotivoConclusion</value>
		      <value>es.pfsgroup.plugin.rem.model.dd.DDResolucionComite</value>
		      <value>es.pfsgroup.plugin.rem.model.dd.DDRespuestaOfertante</value>
		      <value>es.pfsgroup.plugin.rem.model.dd.DDDevolucionReserva</value>
		      <value>es.pfsgroup.plugin.rem.model.dd.DDApruebaDeniega</value>
		      <value>es.pfsgroup.plugin.rem.model.dd.DDMotivosEstadoBC</value>
		      <value>es.pfsgroup.plugin.rem.model.dd.DDSubtipoOfertaAlquiler</value>
        </list>
    </property>
   </bean>
</beans>
