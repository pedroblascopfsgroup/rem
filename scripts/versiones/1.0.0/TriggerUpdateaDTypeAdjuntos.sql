CREATE OR REPLACE TRIGGER LIN001.UPDATEA_DTYPE_ADJUNTOS
AFTER INSERT
ON LIN001.ADA_ADJUNTOS_ASUNTOS 
REFERENCING NEW AS NEW OLD AS OLD
BEGIN
        UPDATE ADA_ADJUNTOS_ASUNTOS SET DTYPE='EXTAdjuntoAsunto' WHERE DTYPE != 'EXTAdjuntoAsunto';
END;
