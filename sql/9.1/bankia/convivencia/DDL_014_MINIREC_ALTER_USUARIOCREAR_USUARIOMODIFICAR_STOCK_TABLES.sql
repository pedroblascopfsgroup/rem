/*
--##########################################
--## AUTOR=Alejandro I�igo
--## FECHA_CREACION=20151127
--## ARTEFACTO=ETL
--## VERSION_ARTEFACTO=2.0
--## INCIDENCIA_LINK=BKREC-1142
--## PRODUCTO=NO
--##########################################
--*/


WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON;
SET DEFINE OFF;

DECLARE

    V_MSQL VARCHAR2(32000 CHAR);   /* Sentencia a ejecutar    */
    V_ESQUEMA VARCHAR2(25 CHAR):= '#ESQUEMA#'; /*  Configuracion Esquema */
    V_ESQUEMA_M VARCHAR2(25 CHAR):= '#ESQUEMA_MASTER#'; /*  Configuracion Esquema */
    V_TS_INDEX VARCHAR2(25 CHAR):= '#TABLESPACE_INDEX#';/* Configuracion Indice*/
    V_SQL VARCHAR2(4000 CHAR); /*  Vble. para consulta que valida la existencia de una tabla.*/
    V_NUM_TABLAS NUMBER(16); /* Vble. para validar la existencia de una tabla.*/  
    ERR_NUM NUMBER(25); /*  Vble. auxiliar para registrar errores en el script.*/
    ERR_MSG VARCHAR2(1024 CHAR);/*  Vble. auxiliar para registrar errores en el script.*/

    V_TEXT1 VARCHAR2(2400 CHAR); /*   Vble. auxiliar*/
    
    V_ESQUEMA_MIN VARCHAR2(25 CHAR):= '#ESQUEMA_MINIREC#'; /*  Configuracion Esquema */ 
    V_ESQUEMA_DWH VARCHAR2(25 CHAR):= '#ESQUEMA_DWH#'; /*  Configuracion Esquema */ 
    V_ESQUEMA_STG VARCHAR2(25 CHAR):= '#ESQUEMA_STG#'; /*  Configuracion Esquema */ 
	
BEGIN


    DBMS_OUTPUT.PUT_LINE('[START] Modificamos la tabla  MINIREC_PCO_CNT_PROC_VIVOS para ampliar campos USUARIOCREAR y USUARIOMODIFICAR');	

    SELECT COUNT(1) INTO V_NUM_TABLAS FROM ALL_TAB_COLUMNS WHERE UPPER(table_name) = 'MINIREC_PCO_CNT_PROC_VIVOS' and UPPER(column_name) = 'USUARIOCREAR';
	
    IF V_NUM_TABLAS = 1 THEN
        EXECUTE IMMEDIATE 'ALTER TABLE ' || V_ESQUEMA_MIN || '.MINIREC_PCO_CNT_PROC_VIVOS MODIFY USUARIOCREAR VARCHAR2(50 CHAR)';
        DBMS_OUTPUT.PUT_LINE('[INFO] ' || V_ESQUEMA_MIN || '.MINIREC_PCO_CNT_PROC_VIVOS.USUARIOCREAR ampliado');
    ELSE
	  DBMS_OUTPUT.PUT_LINE('[INFO] ' || V_ESQUEMA_MIN || '.MINIREC_PCO_CNT_PROC_VIVOS.USUARIOCREAR no encontrado');
    END IF;	
	
	SELECT COUNT(1) INTO V_NUM_TABLAS FROM ALL_TAB_COLUMNS WHERE UPPER(table_name) = 'MINIREC_PCO_CNT_PROC_VIVOS' and UPPER(column_name) = 'USUARIOMODIFICAR';
	
    IF V_NUM_TABLAS = 1 THEN
        EXECUTE IMMEDIATE 'ALTER TABLE ' || V_ESQUEMA_MIN || '.MINIREC_PCO_CNT_PROC_VIVOS MODIFY USUARIOMODIFICAR VARCHAR2(50 CHAR)';
        DBMS_OUTPUT.PUT_LINE('[INFO] ' || V_ESQUEMA_MIN || '.MINIREC_PCO_CNT_PROC_VIVOS.USUARIOMODIFICAR ampliado');
    ELSE
	  DBMS_OUTPUT.PUT_LINE('[INFO] ' || V_ESQUEMA_MIN || '.MINIREC_PCO_CNT_PROC_VIVOS.USUARIOMODIFICAR no encontrado');
    END IF;	
		

    DBMS_OUTPUT.PUT_LINE('[START] Modificamos la tabla MINIREC_PCO_CNT_PROC_VIVOS');
	

    SELECT COUNT(1) INTO V_NUM_TABLAS FROM ALL_TAB_COLUMNS WHERE UPPER(table_name) = 'MINIREC_PCO_CNT_PROC_VIVOS_A' and UPPER(column_name) = 'USUARIOCREAR';
	
    IF V_NUM_TABLAS = 1 THEN
        EXECUTE IMMEDIATE 'ALTER TABLE ' || V_ESQUEMA_MIN || '.MINIREC_PCO_CNT_PROC_VIVOS_A MODIFY USUARIOCREAR VARCHAR2(50 CHAR)';
        DBMS_OUTPUT.PUT_LINE('[INFO] ' || V_ESQUEMA_MIN || '.MINIREC_PCO_CNT_PROC_VIVOS_A.USUARIOCREAR ampliado');
    ELSE
	  DBMS_OUTPUT.PUT_LINE('[INFO] ' || V_ESQUEMA_MIN || '.MINIREC_PCO_CNT_PROC_VIVOS_A.USUARIOCREAR no encontrado');
    END IF;	
	
    SELECT COUNT(1) INTO V_NUM_TABLAS FROM ALL_TAB_COLUMNS WHERE UPPER(table_name) = 'MINIREC_PCO_CNT_PROC_VIVOS_A' and UPPER(column_name) = 'USUARIOMODIFICAR';
	
    IF V_NUM_TABLAS = 1 THEN
        EXECUTE IMMEDIATE 'ALTER TABLE ' || V_ESQUEMA_MIN || '.MINIREC_PCO_CNT_PROC_VIVOS_A MODIFY USUARIOMODIFICAR VARCHAR2(50 CHAR)';
        DBMS_OUTPUT.PUT_LINE('[INFO] ' || V_ESQUEMA_MIN || '.MINIREC_PCO_CNT_PROC_VIVOS_A.USUARIOMODIFICAR ampliado');
    ELSE
	  DBMS_OUTPUT.PUT_LINE('[INFO] ' || V_ESQUEMA_MIN || '.MINIREC_PCO_CNT_PROC_VIVOS_A.USUARIOMODIFICAR no encontrado');
    END IF;		
	
    DBMS_OUTPUT.PUT_LINE('[START] Modificamos la tabla MINIREC_PCO_CNT_PRC_VIVOS_BAJA');	
	

    SELECT COUNT(1) INTO V_NUM_TABLAS FROM ALL_TAB_COLUMNS WHERE UPPER(table_name) = 'MINIREC_PCO_CNT_PRC_VIVOS_BAJA' and UPPER(column_name) = 'USUARIOCREAR';
	
    IF V_NUM_TABLAS = 1 THEN
        EXECUTE IMMEDIATE 'ALTER TABLE ' || V_ESQUEMA_MIN || '.MINIREC_PCO_CNT_PRC_VIVOS_BAJA MODIFY USUARIOCREAR VARCHAR2(50 CHAR)';
        DBMS_OUTPUT.PUT_LINE('[INFO] ' || V_ESQUEMA_MIN || '.MINIREC_PCO_CNT_PRC_VIVOS_BAJA.USUARIOCREAR ampliado');
    ELSE
	  DBMS_OUTPUT.PUT_LINE('[INFO] ' || V_ESQUEMA_MIN || '.MINIREC_PCO_CNT_PRC_VIVOS_BAJA.USUARIOCREAR no encontrado');
    END IF;		
	
    SELECT COUNT(1) INTO V_NUM_TABLAS FROM ALL_TAB_COLUMNS WHERE UPPER(table_name) = 'MINIREC_PCO_CNT_PRC_VIVOS_BAJA' and UPPER(column_name) = 'USUARIOMODIFICAR';
	
    IF V_NUM_TABLAS = 1 THEN
        EXECUTE IMMEDIATE 'ALTER TABLE ' || V_ESQUEMA_MIN || '.MINIREC_PCO_CNT_PRC_VIVOS_BAJA MODIFY USUARIOMODIFICAR VARCHAR2(50 CHAR)';
        DBMS_OUTPUT.PUT_LINE('[INFO] ' || V_ESQUEMA_MIN || '.MINIREC_PCO_CNT_PRC_VIVOS_BAJA.USUARIOMODIFICAR ampliado');
    ELSE
	  DBMS_OUTPUT.PUT_LINE('[INFO] ' || V_ESQUEMA_MIN || '.MINIREC_PCO_CNT_PRC_VIVOS_BAJA.USUARIOMODIFICAR no encontrado');
    END IF;			
    
DBMS_OUTPUT.PUT_LINE('[INFO] EJECUCION TERMINADA CON EXITO');

EXCEPTION
     WHEN OTHERS THEN
       DBMS_OUTPUT.PUT_LINE('[ERROR] Se ha producido un error en la ejecución: '|| TO_CHAR(SQLCODE) || ' ' || SQLERRM );
    RAISE;
END;
/
exit		
