--/*
--##########################################
--## AUTOR=Agustín Mompó
--## FECHA_CREACION=20151027
--## ARTEFACTO=online
--## VERSION_ARTEFACTO=9.1.12-bk
--## INCIDENCIA_LINK=BKREC-1143
--## PRODUCTO=SI
--## Finalidad: DDL
--##           
--## INSTRUCCIONES: Configurar las variables necesarias en el principio del DECLARE
--## VERSIONES:
--##        0.1 Versión inicial
--##########################################
--*/

--Para permitir la visualización de texto en un bloque PL/SQL utilizando DBMS_OUTPUT.PUT_LINE

WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON; 
SET DEFINE OFF;


DECLARE

    V_MSQL VARCHAR2(32000 CHAR); -- Sentencia a ejecutar    
    V_ESQUEMA VARCHAR2(25 CHAR):= '#ESQUEMA#'; -- Configuracion Esquema
    V_ESQUEMA_M VARCHAR2(25 CHAR):= '#ESQUEMA_MASTER#'; -- Configuracion Esquema Master
    V_SQL VARCHAR2(4000 CHAR); -- Vble. para consulta que valida la existencia de una tabla.
    V_NUM_TABLAS NUMBER(16); -- Vble. para validar la existencia de una tabla.
    V_NUM_SEQ  NUMBER(16); -- Vble. para validar la existencia de una secuencia.
    ERR_NUM NUMBER(25);  -- Vble. auxiliar para registrar errores en el script.
    ERR_MSG VARCHAR2(1024 CHAR); -- Vble. auxiliar para registrar errores en el script.

    V_TEXT1 VARCHAR2(2400 CHAR); -- Vble. auxiliar

BEGIN

--##COMPROBACION EXISTENCIA TABLA, BORRAR PRIMERO
V_NUM_TABLAS := 0;
select count(1) INTO V_NUM_TABLAS from all_tables 
where table_name = 'CNV_AUX_ALTA_BUROFAX' and OWNER = V_ESQUEMA;

if V_NUM_TABLAS > 0 then 
--YA existe una versión de la tabla , se elimina primero

  DBMS_OUTPUT.PUT('[INFO] Ya existe una versión de la tabla CNV_AUX_ALTA_BUROFAX: se ELIMINA...');
	EXECUTE IMMEDIATE 'drop table '||V_ESQUEMA||'.CNV_AUX_ALTA_BUROFAX';
  DBMS_OUTPUT.PUT_LINE('OK');

END IF;

EXECUTE IMMEDIATE 'CREATE TABLE '||V_ESQUEMA||'.CNV_AUX_ALTA_BUROFAX
(	ID_EXPEDIENTE NUMBER(8,0), 
	ID_CUENTA NUMBER(8,0), 
	ID_PERSONA NUMBER(8,0), 
	EMPLEADO VARCHAR2(15 CHAR) NOT NULL ENABLE, 
	TIPO_REGISTRO VARCHAR2(1 CHAR) NOT NULL ENABLE, 
	REFERENCIA_CLIENTE VARCHAR2(50 CHAR) NOT NULL ENABLE, 
	NOMBRE_DEST VARCHAR2(50 CHAR) NOT NULL ENABLE, 
	DIRECCION_DEST VARCHAR2(50 CHAR) NOT NULL ENABLE, 
	CODIGO_POSTAL_DEST VARCHAR2(5 CHAR) NOT NULL ENABLE, 
	LOCALIDAD_DEST VARCHAR2(50 CHAR) NOT NULL ENABLE, 
	PROVINCIA_DEST VARCHAR2(50 CHAR), 
	PAIS_DEST VARCHAR2(50 CHAR) NOT NULL ENABLE, 
	TELEFONO_DEST VARCHAR2(30 CHAR), 
	FAX_DEST VARCHAR2(30 CHAR), 
	TELEX_DEST VARCHAR2(30 CHAR), 
	FICHERO_DOCUMENTO VARCHAR2(30 CHAR) NOT NULL ENABLE, 
	TIPO_FICHERO_DOCUMENTO VARCHAR2(1 CHAR) NOT NULL ENABLE, 
	FECHA_ENVIO_CORREO VARCHAR2(8 CHAR) NOT NULL ENABLE, 
	IDENT_UNIDAD VARCHAR2(8 CHAR) NOT NULL ENABLE, 
	IND_COPIA_CERTIF VARCHAR2(1 CHAR) NOT NULL ENABLE, 
	IND_ACUSE_RECIBO VARCHAR2(1 CHAR) NOT NULL ENABLE, 
	ADMISION_REF_CORREOS VARCHAR2(23 CHAR), 
	ADMISION_FECHA VARCHAR2(8 CHAR), 
	ADMISION_HORA VARCHAR2(6 CHAR), 
	ADMISION_IMPORTE NUMBER(4,0), 
	ERROR_DETECTADO NUMBER(2,0), 
	ESTADO_REF_CORREOS VARCHAR2(23 CHAR), 
	ESTADO_NOMBRE_DESTINATARIO VARCHAR2(50 CHAR), 
	ESTADO_COD_POSTAL VARCHAR2(5 CHAR), 
	ESTADO VARCHAR2(2 CHAR), 
	ESTADO_FECHA VARCHAR2(8 CHAR), 
	ESTADO_HORA VARCHAR2(6 CHAR), 
	ESTADO_PERSONA_RETIRA VARCHAR2(155 CHAR), 
	PROCESADO VARCHAR2(1 CHAR) NOT NULL ENABLE, 
	GESTOR_ADR VARCHAR2(15 CHAR) NOT NULL ENABLE, 
	GESTOR_GDR VARCHAR2(15 CHAR) NOT NULL ENABLE, 
	GESTOR_EMP VARCHAR2(15 CHAR) NOT NULL ENABLE, 
	NO_ENVIO VARCHAR2(20 CHAR) NOT NULL ENABLE, 
	APLIC VARCHAR2(1 CHAR) NOT NULL ENABLE, 
	TIPO VARCHAR2(3 CHAR) NOT NULL ENABLE, 
	EMPLEADO_EMISOR VARCHAR2(15 CHAR) NOT NULL ENABLE, 
	GRUPO_RBLE_EMISOR VARCHAR2(60 CHAR) NOT NULL ENABLE, 
	DIR_GRUPO_RBLE_EMISOR VARCHAR2(60 CHAR) NOT NULL ENABLE, 
	CP_GRUPO_RBLE_EMISOR VARCHAR2(5 CHAR) NOT NULL ENABLE, 
	PROVINCIA_GRUPO_RBLE VARCHAR2(30 CHAR) NOT NULL ENABLE, 
	FAX_EMISOR VARCHAR2(12 CHAR), 
	TRATAMIENTO_EMISOR VARCHAR2(4 CHAR), 
	NOMBRE_EMISOR VARCHAR2(20 CHAR), 
	TELEFONO_EMISOR VARCHAR2(12 CHAR), 
	REF_ENVIO VARCHAR2(30 BYTE), 
	TRATAMIENTO_DEST VARCHAR2(5 CHAR), 
	TEXTO VARCHAR2(4000 CHAR), 
	ID_ACTIVIDAD VARCHAR2(12 CHAR), 
	USUARIO_ORACLE VARCHAR2(15 CHAR), 
	FECHA_PROCESADO DATE, 
	COD_GRUPO_RBLE_EMISOR VARCHAR2(15 CHAR), 
	CODIGO_PREPARACION_NUSE NUMBER(16,0) NOT NULL ENABLE, 
	ID_CUENTA_RCV NUMBER(16,0) NOT NULL ENABLE, 
	ID_EXPEDIENTE_RCV NUMBER(16,0) NOT NULL ENABLE, 
	ID_ASUNTO_RCV NUMBER(16,0) NOT NULL ENABLE, 
	CODIGO_ENTIDAD NUMBER(4,0) NOT NULL ENABLE, 
	CODIGO_PROPIEDAD NUMBER(5,0) NOT NULL ENABLE, 
	TIPO_PRODUCTO VARCHAR2(5 CHAR) NOT NULL ENABLE, 
	NUMERO_CONTRATO NUMBER(17,0) NOT NULL ENABLE, 
	NUMERO_ESPEC NUMBER(15,0) NOT NULL ENABLE
)
TABLESPACE '||V_ESQUEMA||'
PCTUSED    0
PCTFREE    10
INITRANS   1
MAXTRANS   255
STORAGE    (
            INITIAL          64K
            NEXT             1M
            MINEXTENTS       1
            MAXEXTENTS       UNLIMITED
            PCTINCREASE      0
            BUFFER_POOL      DEFAULT
           )
LOGGING 
NOCOMPRESS 
NOCACHE
NOPARALLEL
MONITORING';

DBMS_OUTPUT.PUT_LINE('[INFO] CREADA TABLA CNV_AUX_ALTA_BUROFAX'); 			   			   
			 	
COMMIT;


EXCEPTION
     WHEN OTHERS THEN
          err_num := SQLCODE;
          err_msg := SQLERRM;

          DBMS_OUTPUT.put_line('[ERROR] Se ha producido un error en la ejecución:'||TO_CHAR(err_num));
          DBMS_OUTPUT.put_line('-----------------------------------------------------------'); 
          DBMS_OUTPUT.put_line(err_msg);

          ROLLBACK;
          RAISE;          

END;

/

EXIT	
