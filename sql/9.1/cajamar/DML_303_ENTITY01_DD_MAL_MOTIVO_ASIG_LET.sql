--/*
--##########################################
--## AUTOR=Gonzalo Estellés
--## FECHA_CREACION=20150721
--## ARTEFACTO=online
--## VERSION_ARTEFACTO=9.1
--## INCIDENCIA_LINK=CMREC-368
--## PRODUCTO=NO
--## Finalidad: DDL
--##           
--## INSTRUCCIONES: Configurar las variables necesarias en el principio del DECLARE
--## VERSIONES:
--##        0.1 Versión inicial
--##########################################
--*/
WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON;
SET DEFINE OFF;

DECLARE
	V_MSQL VARCHAR2(32000 CHAR); -- Sentencia a ejecutar    
    V_ESQUEMA VARCHAR2(25 CHAR):= '#ESQUEMA#'; -- Configuracion Esquema
    V_ESQUEMA_M VARCHAR2(25 CHAR):= '#ESQUEMA_MASTER#'; -- Configuracion Esquema Master
    V_SQL VARCHAR2(4000 CHAR); -- Vble. para consulta que valida la existencia de una tabla.
    V_NUM_TABLAS NUMBER(16); -- Vble. para validar la existencia de una tabla.  
    ERR_NUM NUMBER(25);  -- Vble. auxiliar para registrar errores en el script.
    ERR_MSG VARCHAR2(1024 CHAR); -- Vble. auxiliar para registrar errores en el script.
BEGIN
	DBMS_OUTPUT.PUT_LINE('******** DD_MAL_MOTIVO_ASIG_LET ********'); 
    DBMS_OUTPUT.PUT_LINE('[INFO] '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET... Comprobaciones previas'); 

    V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET WHERE DD_MAL_CODIGO = ''TERRITORIALIDAD'' ';
    EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
    IF V_NUM_TABLAS > 0 THEN	  
		DBMS_OUTPUT.PUT_LINE('[INFO] Ya existen el registro en la tabla '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET.');
	ELSE
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET (DD_MAL_ID,DD_MAL_CODIGO,DD_MAL_DESCRIPCION,DD_MAL_DESCRIPCION_LARGA,VERSION,USUARIOCREAR,FECHACREAR,BORRADO) VALUES('||V_ESQUEMA||'.S_DD_MAL_MOTIVO_ASIG_LET.nextval,''TERRITORIALIDAD'',''Territorialidad. Zona de actuación preferente'',''Territorialidad. Zona de actuación preferente'', 0, ''DML'', sysdate, 0)';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET.');
	END IF;

    V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET WHERE DD_MAL_CODIGO = ''CAPACIDAD_TEC'' ';
    EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
    IF V_NUM_TABLAS > 0 THEN	  
		DBMS_OUTPUT.PUT_LINE('[INFO] Ya existen el registro en la tabla '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET.');
	ELSE
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET (DD_MAL_ID,DD_MAL_CODIGO,DD_MAL_DESCRIPCION,DD_MAL_DESCRIPCION_LARGA,VERSION,USUARIOCREAR,FECHACREAR,BORRADO) VALUES('||V_ESQUEMA||'.S_DD_MAL_MOTIVO_ASIG_LET.nextval,''CAPACIDAD_TEC'',''Capacidad técnica y de gestión de asuntos'',''Capacidad técnica y de gestión de asuntos'', 0, ''DML'', sysdate, 0)';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET.');
	END IF;

    V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET WHERE DD_MAL_CODIGO = ''CAPACIDAD_RESP'' ';
    EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
    IF V_NUM_TABLAS > 0 THEN	  
		DBMS_OUTPUT.PUT_LINE('[INFO] Ya existen el registro en la tabla '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET.');
	ELSE
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET (DD_MAL_ID,DD_MAL_CODIGO,DD_MAL_DESCRIPCION,DD_MAL_DESCRIPCION_LARGA,VERSION,USUARIOCREAR,FECHACREAR,BORRADO) VALUES('||V_ESQUEMA||'.S_DD_MAL_MOTIVO_ASIG_LET.nextval,''CAPACIDAD_RESP'',''Capacidad de respuesta'',''Capacidad de respuesta: judicial y a requerimientos de caja'', 0, ''DML'', sysdate, 0)';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET.');
	END IF;
	
	    V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET WHERE DD_MAL_CODIGO = ''TIPOLOGIA'' ';
    EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
    IF V_NUM_TABLAS > 0 THEN	  
		DBMS_OUTPUT.PUT_LINE('[INFO] Ya existen el registro en la tabla '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET.');
	ELSE
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET (DD_MAL_ID,DD_MAL_CODIGO,DD_MAL_DESCRIPCION,DD_MAL_DESCRIPCION_LARGA,VERSION,USUARIOCREAR,FECHACREAR,BORRADO) VALUES('||V_ESQUEMA||'.S_DD_MAL_MOTIVO_ASIG_LET.nextval,''TIPOLOGIA'',''Tipología de asuntos'',''Tipología de asuntos'', 0, ''DML'', sysdate, 0)';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET.');
	END IF;
	
	V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET WHERE DD_MAL_CODIGO = ''PROPORCIONALIDAD'' ';
    EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
    IF V_NUM_TABLAS > 0 THEN	  
		DBMS_OUTPUT.PUT_LINE('[INFO] Ya existen el registro en la tabla '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET.');
	ELSE
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET (DD_MAL_ID,DD_MAL_CODIGO,DD_MAL_DESCRIPCION,DD_MAL_DESCRIPCION_LARGA,VERSION,USUARIOCREAR,FECHACREAR,BORRADO) VALUES('||V_ESQUEMA||'.S_DD_MAL_MOTIVO_ASIG_LET.nextval,''PROPORCIONALIDAD'',''Proporcionalidad. Volumen de asuntos gestionados'',''Proporcionalidad. Volumen de asuntos gestionados'', 0, ''DML'', sysdate, 0)';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET.');
	END IF;
	
	V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET WHERE DD_MAL_CODIGO = ''ACUERDOS'' ';
    EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
    IF V_NUM_TABLAS > 0 THEN	  
		DBMS_OUTPUT.PUT_LINE('[INFO] Ya existen el registro en la tabla '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET.');
	ELSE
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET (DD_MAL_ID,DD_MAL_CODIGO,DD_MAL_DESCRIPCION,DD_MAL_DESCRIPCION_LARGA,VERSION,USUARIOCREAR,FECHACREAR,BORRADO) VALUES('||V_ESQUEMA||'.S_DD_MAL_MOTIVO_ASIG_LET.nextval,''ACUERDOS'',''Acuerdos Institucionales'',''Acuerdos Institucionales'', 0, ''DML'', sysdate, 0)';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET.');
	END IF;
	
	V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET WHERE DD_MAL_CODIGO = ''DIRECCION'' ';
    EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
    IF V_NUM_TABLAS > 0 THEN	  
		DBMS_OUTPUT.PUT_LINE('[INFO] Ya existen el registro en la tabla '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET.');
	ELSE
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET (DD_MAL_ID,DD_MAL_CODIGO,DD_MAL_DESCRIPCION,DD_MAL_DESCRIPCION_LARGA,VERSION,USUARIOCREAR,FECHACREAR,BORRADO) VALUES('||V_ESQUEMA||'.S_DD_MAL_MOTIVO_ASIG_LET.nextval,''DIRECCION'',''Dirección interna de asuntos'',''Dirección interna de asuntos'', 0, ''DML'', sysdate, 0)';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en '||V_ESQUEMA||'.DD_MAL_MOTIVO_ASIG_LET.');
	END IF;
	
    COMMIT;
	
EXCEPTION
  WHEN OTHERS THEN
    ERR_NUM := SQLCODE;
    ERR_MSG := SQLERRM;
    DBMS_OUTPUT.put_line('[ERROR] Se ha producido un error en la ejecución:'||TO_CHAR(ERR_NUM));
    DBMS_OUTPUT.put_line('-----------------------------------------------------------'); 
    DBMS_OUTPUT.put_line(ERR_MSG);
    ROLLBACK;
    RAISE;   
    
END;
/

EXIT;
