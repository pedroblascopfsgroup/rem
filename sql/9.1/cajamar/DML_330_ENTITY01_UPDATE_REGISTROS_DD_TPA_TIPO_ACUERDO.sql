--/*
--##########################################
--## AUTOR=Alberto B
--## FECHA_CREACION=20151126
--## ARTEFACTO=online
--## VERSION_ARTEFACTO=9.1.0-cj-rc23
--## INCIDENCIA_LINK=CMREC-1382
--## PRODUCTO=NO
--## Finalidad: DML
--##           
--## INSTRUCCIONES: Configurar las variables necesarias en el principio del DECLARE
--## VERSIONES:
--##        0.1 Versión inicial
--##########################################
--*/
WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON;
SET DEFINE OFF;

DECLARE
	V_MSQL VARCHAR2(32000 CHAR); -- Sentencia a ejecutar     
    V_ESQUEMA VARCHAR2(25 CHAR):= '#ESQUEMA#'; -- Configuracion Esquema
    V_ESQUEMA_M VARCHAR2(25 CHAR):= '#ESQUEMA_MASTER#'; -- Configuracion Esquema Master
    V_SQL VARCHAR2(4000 CHAR); -- Vble. para consulta que valida la existencia de una tabla.
    V_NUM_TABLAS NUMBER(16); -- Vble. para validar la existencia de una tabla.
    V_NUM_BORRADO NUMBER(16); -- Vble. para validar la existencia de una tabla. 
    ERR_NUM NUMBER(25);  -- Vble. auxiliar para registrar errores en el script.
    ERR_MSG VARCHAR2(1024 CHAR); -- Vble. auxiliar para registrar errores en el script.
BEGIN
	DBMS_OUTPUT.PUT_LINE('******** ACU_CAMPOS_TIPO_ACUERDO ********'); 
    DBMS_OUTPUT.PUT_LINE('[INFO] '||V_ESQUEMA||'.ACU_CAMPOS_TIPO_ACUERDO... Comprobaciones previas'); 

	--COMPROBAMOS CADA TERMINO POR SI NO EXISTIERA O SI ESTUVIERA BORRADO ACTUALIZARLO
	V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''01''';
    EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
    IF V_NUM_TABLAS = 0 THEN	  
		DBMS_OUTPUT.PUT_LINE('[INFO] No existe el registro en la tabla '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO.');
		
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''01'', ''Dación en pago'',''Dación en pago'',0,''CMREC-1382'',SYSDATE,0)';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
	ELSE
		V_SQL := 'SELECT BORRADO FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''01''';
    	EXECUTE IMMEDIATE V_SQL INTO V_NUM_BORRADO;
    	IF V_NUM_BORRADO = 1 THEN
	    	V_MSQL := 'UPDATE '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO SET BORRADO = 0, FECHABORRAR = NULL, USUARIOBORRAR = NULL WHERE DD_TPA_CODIGO = ''01''';
			EXECUTE IMMEDIATE V_MSQL;
			DBMS_OUTPUT.PUT_LINE('[INFO] Registro actualizado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	ELSE
    		DBMS_OUTPUT.PUT_LINE('[INFO] Registro correcto en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	END IF;
	END IF;
	
	V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''05''';
    EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
    IF V_NUM_TABLAS = 0 THEN	  
		DBMS_OUTPUT.PUT_LINE('[INFO] No existe el registro en la tabla '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO.');
		
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''05'', ''Quita sobre deuda'',''Quita sobre deuda'',0,''CMREC-1382'',SYSDATE,0)';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
	ELSE
		V_SQL := 'SELECT BORRADO FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''05''';
    	EXECUTE IMMEDIATE V_SQL INTO V_NUM_BORRADO;
    	IF V_NUM_BORRADO = 1 THEN
	    	V_MSQL := 'UPDATE '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO SET BORRADO = 0, FECHABORRAR = NULL, USUARIOBORRAR = NULL WHERE DD_TPA_CODIGO = ''05''';
			EXECUTE IMMEDIATE V_MSQL;
			DBMS_OUTPUT.PUT_LINE('[INFO] Registro actualizado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	ELSE
    		DBMS_OUTPUT.PUT_LINE('[INFO] Registro correcto en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	END IF;
	END IF;
	
	V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''11''';
    EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
    IF V_NUM_TABLAS = 0 THEN	  
		DBMS_OUTPUT.PUT_LINE('[INFO] No existe el registro en la tabla '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO.');
		
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''11'', ''Cesión remate'',''Cesión remate'',0,''CMREC-1382'',SYSDATE,0)';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
	ELSE
		V_SQL := 'SELECT BORRADO FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''11''';
    	EXECUTE IMMEDIATE V_SQL INTO V_NUM_BORRADO;
    	IF V_NUM_BORRADO = 1 THEN
	    	V_MSQL := 'UPDATE '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO SET BORRADO = 0, FECHABORRAR = NULL, USUARIOBORRAR = NULL WHERE DD_TPA_CODIGO = ''11''';
			EXECUTE IMMEDIATE V_MSQL;
			DBMS_OUTPUT.PUT_LINE('[INFO] Registro actualizado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	ELSE
    		DBMS_OUTPUT.PUT_LINE('[INFO] Registro correcto en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	END IF;
	END IF;
	
	
	V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''12''';
    EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
    IF V_NUM_TABLAS = 0 THEN	  
		DBMS_OUTPUT.PUT_LINE('[INFO] No existe el registro en la tabla '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO.');
		
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''12'', ''Cesión de crédito'',''Cesión de crédito'',0,''CMREC-1382'',SYSDATE,0)';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
	ELSE
		V_SQL := 'SELECT BORRADO FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''12''';
    	EXECUTE IMMEDIATE V_SQL INTO V_NUM_BORRADO;
    	IF V_NUM_BORRADO = 1 THEN
	    	V_MSQL := 'UPDATE '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO SET BORRADO = 0, FECHABORRAR = NULL, USUARIOBORRAR = NULL WHERE DD_TPA_CODIGO = ''12''';
			EXECUTE IMMEDIATE V_MSQL;
			DBMS_OUTPUT.PUT_LINE('[INFO] Registro actualizado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	ELSE
    		DBMS_OUTPUT.PUT_LINE('[INFO] Registro correcto en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	END IF;
	END IF;
	
	V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''17''';
    EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
    IF V_NUM_TABLAS = 0 THEN	  
		DBMS_OUTPUT.PUT_LINE('[INFO] No existe el registro en la tabla '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO.');
		
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''17'', ''Plan de pagos'',''Plan de pagos'',0,''CMREC-1382'',SYSDATE,0)';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
	ELSE
		V_SQL := 'SELECT BORRADO FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''17''';
    	EXECUTE IMMEDIATE V_SQL INTO V_NUM_BORRADO;
    	IF V_NUM_BORRADO = 1 THEN
	    	V_MSQL := 'UPDATE '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO SET BORRADO = 0, FECHABORRAR = NULL, USUARIOBORRAR = NULL WHERE DD_TPA_CODIGO = ''17''';
			EXECUTE IMMEDIATE V_MSQL;
			DBMS_OUTPUT.PUT_LINE('[INFO] Registro actualizado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	ELSE
    		DBMS_OUTPUT.PUT_LINE('[INFO] Registro correcto en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	END IF;
	END IF;
	
	V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''DESCEFECT''';
    EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
    IF V_NUM_TABLAS = 0 THEN	  
		DBMS_OUTPUT.PUT_LINE('[INFO] No existe el registro en la tabla '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO.');
		
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''DESCEFECT'', ''Descuento de Efectos'',''Descuento de Efectos'',0,''CMREC-1382'',SYSDATE,0)';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
	ELSE
		V_SQL := 'SELECT BORRADO FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''DESCEFECT''';
    	EXECUTE IMMEDIATE V_SQL INTO V_NUM_BORRADO;
    	IF V_NUM_BORRADO = 1 THEN
	    	V_MSQL := 'UPDATE '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO SET BORRADO = 0, FECHABORRAR = NULL, USUARIOBORRAR = NULL WHERE DD_TPA_CODIGO = ''DESCEFECT''';
			EXECUTE IMMEDIATE V_MSQL;
			DBMS_OUTPUT.PUT_LINE('[INFO] Registro actualizado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	ELSE
    		DBMS_OUTPUT.PUT_LINE('[INFO] Registro correcto en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	END IF;
	END IF;
	
	V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''DISPPRESPRO''';
    EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
    IF V_NUM_TABLAS = 0 THEN	  
		DBMS_OUTPUT.PUT_LINE('[INFO] No existe el registro en la tabla '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO.');
		
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''DISPPRESPRO'', ''Disposición préstamo Promotor'',''Disposición préstamo Promotor'',0,''CMREC-1382'',SYSDATE,0)';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
	ELSE
		V_SQL := 'SELECT BORRADO FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''DISPPRESPRO''';
    	EXECUTE IMMEDIATE V_SQL INTO V_NUM_BORRADO;
    	IF V_NUM_BORRADO = 1 THEN
	    	V_MSQL := 'UPDATE '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO SET BORRADO = 0, FECHABORRAR = NULL, USUARIOBORRAR = NULL WHERE DD_TPA_CODIGO = ''DISPPRESPRO''';
			EXECUTE IMMEDIATE V_MSQL;
			DBMS_OUTPUT.PUT_LINE('[INFO] Registro actualizado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	ELSE
    		DBMS_OUTPUT.PUT_LINE('[INFO] Registro correcto en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	END IF;
	END IF;
	
	V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''EFECFONPRO''';
    EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
    IF V_NUM_TABLAS = 0 THEN	  
		DBMS_OUTPUT.PUT_LINE('[INFO] No existe el registro en la tabla '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO.');
		
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''EFECFONPRO'', ''Efectivo / Fondos propios'',''Efectivo / Fondos propios'',0,''CMREC-1382'',SYSDATE,0)';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
	ELSE
		V_SQL := 'SELECT BORRADO FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''EFECFONPRO''';
    	EXECUTE IMMEDIATE V_SQL INTO V_NUM_BORRADO;
    	IF V_NUM_BORRADO = 1 THEN
	    	V_MSQL := 'UPDATE '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO SET BORRADO = 0, FECHABORRAR = NULL, USUARIOBORRAR = NULL WHERE DD_TPA_CODIGO = ''EFECFONPRO''';
			EXECUTE IMMEDIATE V_MSQL;
			DBMS_OUTPUT.PUT_LINE('[INFO] Registro actualizado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	ELSE
    		DBMS_OUTPUT.PUT_LINE('[INFO] Registro correcto en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	END IF;
	END IF;
	
	
	V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''LIBINTERV''';
    EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
    IF V_NUM_TABLAS = 0 THEN	  
		DBMS_OUTPUT.PUT_LINE('[INFO] No existe el registro en la tabla '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO.');
		
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''LIBINTERV'', ''Liberación de Intervinientes'',''Liberación de Intervinientes'',0,''CMREC-1382'',SYSDATE,0)';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
	ELSE
		V_SQL := 'SELECT BORRADO FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''LIBINTERV''';
    	EXECUTE IMMEDIATE V_SQL INTO V_NUM_BORRADO;
    	IF V_NUM_BORRADO = 1 THEN
	    	V_MSQL := 'UPDATE '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO SET BORRADO = 0, FECHABORRAR = NULL, USUARIOBORRAR = NULL WHERE DD_TPA_CODIGO = ''LIBINTERV''';
			EXECUTE IMMEDIATE V_MSQL;
			DBMS_OUTPUT.PUT_LINE('[INFO] Registro actualizado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	ELSE
    		DBMS_OUTPUT.PUT_LINE('[INFO] Registro correcto en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	END IF;
	END IF;
	
	
	V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''REFINOVA''';
    EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
    IF V_NUM_TABLAS = 0 THEN	  
		DBMS_OUTPUT.PUT_LINE('[INFO] No existe el registro en la tabla '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO.');
		
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''REFINOVA'', ''Refinanciación / Novación'',''Refinanciación / Novación'',0,''CMREC-1382'',SYSDATE,0)';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
	ELSE
		V_SQL := 'SELECT BORRADO FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''REFINOVA''';
    	EXECUTE IMMEDIATE V_SQL INTO V_NUM_BORRADO;
    	IF V_NUM_BORRADO = 1 THEN
	    	V_MSQL := 'UPDATE '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO SET BORRADO = 0, FECHABORRAR = NULL, USUARIOBORRAR = NULL WHERE DD_TPA_CODIGO = ''REFINOVA''';
			EXECUTE IMMEDIATE V_MSQL;
			DBMS_OUTPUT.PUT_LINE('[INFO] Registro actualizado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	ELSE
    		DBMS_OUTPUT.PUT_LINE('[INFO] Registro correcto en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	END IF;
	END IF;
	
	
	V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''SINSOLUCION''';
    EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
    IF V_NUM_TABLAS = 0 THEN	  
		DBMS_OUTPUT.PUT_LINE('[INFO] No existe el registro en la tabla '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO.');
		
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''SINSOLUCION'', ''Sin solución'',''Sin solución'',0,''CMREC-1382'',SYSDATE,0)';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
	ELSE
		V_SQL := 'SELECT BORRADO FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''SINSOLUCION''';
    	EXECUTE IMMEDIATE V_SQL INTO V_NUM_BORRADO;
    	IF V_NUM_BORRADO = 1 THEN
	    	V_MSQL := 'UPDATE '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO SET BORRADO = 0, FECHABORRAR = NULL, USUARIOBORRAR = NULL WHERE DD_TPA_CODIGO = ''SINSOLUCION''';
			EXECUTE IMMEDIATE V_MSQL;
			DBMS_OUTPUT.PUT_LINE('[INFO] Registro actualizado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	ELSE
    		DBMS_OUTPUT.PUT_LINE('[INFO] Registro correcto en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	END IF;
	END IF;
	
	
	V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''COMPVENDACION''';
    EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
    IF V_NUM_TABLAS = 0 THEN	  
		DBMS_OUTPUT.PUT_LINE('[INFO] No existe el registro en la tabla '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO.');
		
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''COMPVENDACION'', ''Compra venta/Dación'',''Compra venta/Dación'',0,''CMREC-1382'',SYSDATE,0)';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
	ELSE
		V_SQL := 'SELECT BORRADO FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''COMPVENDACION''';
    	EXECUTE IMMEDIATE V_SQL INTO V_NUM_BORRADO;
    	IF V_NUM_BORRADO = 1 THEN
	    	V_MSQL := 'UPDATE '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO SET BORRADO = 0, FECHABORRAR = NULL, USUARIOBORRAR = NULL WHERE DD_TPA_CODIGO = ''COMPVENDACION''';
			EXECUTE IMMEDIATE V_MSQL;
			DBMS_OUTPUT.PUT_LINE('[INFO] Registro actualizado en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	ELSE
    		DBMS_OUTPUT.PUT_LINE('[INFO] Registro correcto en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
    	END IF;
	END IF;
	
	--ACTUALIZAMOS LOS QUE NO ESTAN DENTRO DE ESTOS CODIGOS
	
	V_SQL := 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO NOT IN (''01'',''EFECFONPRO'',''REFINOVA'',''05'', ''DESCEFECT'', ''DISPPRESPRO'', ''11'', ''12'', ''17'', ''SINSOLUCION'', ''LIBINTERV'', ''COMPVENDACION'') AND BORRADO = 0';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	IF V_NUM_TABLAS > 0 THEN
		V_MSQL := 'UPDATE '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO SET BORRADO = 1, FECHABORRAR = SYSDATE, USUARIOBORRAR = ''CMREC-1382'' WHERE DD_TPA_CODIGO NOT IN (''01'',''EFECFONPRO'',''REFINOVA'',''05'', ''DESCEFECT'', ''DISPPRESPRO'', ''11'', ''12'', ''17'', ''SINSOLUCION'', ''LIBINTERV'', ''COMPVENDACION'') AND BORRADO = 0';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] RegistroS actualizados en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
	ELSE
		DBMS_OUTPUT.PUT_LINE('[INFO] No hay RegistroS para actualizar en '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO');
	END IF;
	
    COMMIT;
	
EXCEPTION
  WHEN OTHERS THEN
    ERR_NUM := SQLCODE;
    ERR_MSG := SQLERRM;
    DBMS_OUTPUT.put_line('[ERROR] Se ha producido un error en la ejecución:'||TO_CHAR(ERR_NUM));
    DBMS_OUTPUT.put_line('-----------------------------------------------------------'); 
    DBMS_OUTPUT.put_line(ERR_MSG);
    ROLLBACK;
    RAISE;   
    
END;
/

EXIT;
