--/*
--##########################################
--## AUTOR=Alberto b
--## FECHA_CREACION=20151201
--## ARTEFACTO=web
--## VERSION_ARTEFACTO=9.1.0-cj-rc23
--## INCIDENCIA_LINK=PRODUCTO-496
--## PRODUCTO=SI
--## Finalidad: Insercion registros tabla 
--##           
--## INSTRUCCIONES: Configurar las variables necesarias en el principio del DECLARE
--## VERSIONES:
--##        0.1 Versión inicial
--##########################################
--*/

--Para permitir la visualización de texto en un bloque PL/SQL utilizando DBMS_OUTPUT.PUT_LINE

WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON;
SET DEFINE OFF;

DECLARE

    V_MSQL VARCHAR2(32000 CHAR); -- Sentencia a ejecutar    
    V_ESQUEMA VARCHAR2(25 CHAR):= '#ESQUEMA#'; -- Configuracion Esquema
    V_ESQUEMA_M VARCHAR2(25 CHAR):= '#ESQUEMA_MASTER#'; -- Configuracion Esquema Master
    V_TS_INDEX VARCHAR2(25 CHAR):= '#TABLESPACE_INDEX#'; -- Configuracion Indice
    V_SQL VARCHAR2(4000 CHAR); -- Vble. para consulta que valida la existencia de una tabla.
    V_COUNT NUMBER(16); -- Vble. para validar la existencia de una tabla.  
    V_STRING VARCHAR2(10); -- Vble. para validar la existencia de si el campo es nulo
    ERR_NUM NUMBER(25);  -- Vble. auxiliar para registrar errores en el script.
    ERR_MSG VARCHAR2(1024 CHAR); -- Vble. auxiliar para registrar errores en el script.

    V_TEXT1 VARCHAR2(2400 CHAR); -- Vble. auxiliar
    
    V_ESQUEMA_MIN VARCHAR2(25 CHAR):= '#ESQUEMA_MINI#'; -- Configuracion Esquema minirec
    V_ESQUEMA_DWH VARCHAR2(25 CHAR):= '#ESQUEMA_DWH#'; -- Configuracion Esquema recovery_bankia_dwh
    V_ESQUEMA_STG VARCHAR2(25 CHAR):= '#ESQUEMA_STG#'; -- Configuracion Esquema recovery_bankia_datastage

    BEGIN

	DBMS_OUTPUT.PUT_LINE('[INFO] Tabla ' || V_ESQUEMA || '.DD_TPA_TIPO_ACUERDO...');
	V_SQL := 'UPDATE '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO SET DD_EIN_ID = (SELECT DD_EIN_ID FROM '||V_ESQUEMA_M||'.DD_EIN_ENTIDAD_INFORMACION WHERE DD_EIN_DESCRIPCION = ''Asunto'') WHERE DD_TPA_CODIGO IN (''01'',''05'',''11'',''12'',''17'',''EFECFONPRO'', ''REFINOVA'', ''SINSOLUCION'', ''LIBINTERV'', ''COMPVENDACION'') AND BORRADO = 0';
		  
	EXECUTE IMMEDIATE V_SQL;
	DBMS_OUTPUT.PUT_LINE('[INFO] Tabla ' || V_ESQUEMA || '.DD_TPA_TIPO_ACUERDO ACTUALIZADA...');
	
	
	V_SQL:= 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''EXP_01''';
	EXECUTE IMMEDIATE V_SQL INTO V_COUNT;
	IF V_COUNT > 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] Ya existe el registro en la tabla');
	ELSE
		V_MSQL:= 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO, DD_EIN_ID) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''EXP_01'', ''Dación en pago'', ''Dación en pago'', 0,  ''PRODUCTO-496'', SYSDATE, 0, (SELECT DD_EIN_ID FROM '||V_ESQUEMA_M||'.DD_EIN_ENTIDAD_INFORMACION WHERE DD_EIN_DESCRIPCION = ''Expediente''))';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en la tabla');
	END IF;
	
	V_SQL:= 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''EXP_05''';
	EXECUTE IMMEDIATE V_SQL INTO V_COUNT;
	IF V_COUNT > 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] Ya existe el registro en la tabla');
	ELSE
		V_MSQL:= 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO, DD_EIN_ID) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''EXP_05'', ''Quita sobre deuda'', ''Quita sobre deuda'', 0,  ''PRODUCTO-496'', SYSDATE, 0, (SELECT DD_EIN_ID FROM '||V_ESQUEMA_M||'.DD_EIN_ENTIDAD_INFORMACION WHERE DD_EIN_DESCRIPCION = ''Expediente''))';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en la tabla');
	END IF;
	
	V_SQL:= 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''EXP_12''';
	EXECUTE IMMEDIATE V_SQL INTO V_COUNT;
	IF V_COUNT > 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] Ya existe el registro en la tabla');
	ELSE
		V_MSQL:= 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO, DD_EIN_ID) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''EXP_12'', ''Cesión de crédito'', ''Cesión de crédito'', 0,  ''PRODUCTO-496'', SYSDATE, 0, (SELECT DD_EIN_ID FROM '||V_ESQUEMA_M||'.DD_EIN_ENTIDAD_INFORMACION WHERE DD_EIN_DESCRIPCION = ''Expediente''))';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en la tabla');
	END IF;
	
	V_SQL:= 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''EXP_EFECFONPRO''';
	EXECUTE IMMEDIATE V_SQL INTO V_COUNT;
	IF V_COUNT > 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] Ya existe el registro en la tabla');
	ELSE
		V_MSQL:= 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO, DD_EIN_ID) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''EXP_EFECFONPRO'', ''Efectivo / Fondos propios'', ''Efectivo / Fondos propios'', 0,  ''PRODUCTO-496'', SYSDATE, 0, (SELECT DD_EIN_ID FROM '||V_ESQUEMA_M||'.DD_EIN_ENTIDAD_INFORMACION WHERE DD_EIN_DESCRIPCION = ''Expediente''))';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en la tabla');
	END IF;
	
	V_SQL:= 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''EXP_REFINOVA''';
	EXECUTE IMMEDIATE V_SQL INTO V_COUNT;
	IF V_COUNT > 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] Ya existe el registro en la tabla');
	ELSE
		V_MSQL:= 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO, DD_EIN_ID) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''EXP_REFINOVA'', ''Refinanciación / Novación'', ''Refinanciación / Novación'', 0,  ''PRODUCTO-496'', SYSDATE, 0, (SELECT DD_EIN_ID FROM '||V_ESQUEMA_M||'.DD_EIN_ENTIDAD_INFORMACION WHERE DD_EIN_DESCRIPCION = ''Expediente''))';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en la tabla');
	END IF;
	
	V_SQL:= 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''EXP_DESCEFECT''';
	EXECUTE IMMEDIATE V_SQL INTO V_COUNT;
	IF V_COUNT > 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] Ya existe el registro en la tabla');
	ELSE
		V_MSQL:= 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO, DD_EIN_ID) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''EXP_DESCEFECT'', ''Descuento de Efectos'', ''Descuento de Efectos'', 0,  ''PRODUCTO-496'', SYSDATE, 0, (SELECT DD_EIN_ID FROM '||V_ESQUEMA_M||'.DD_EIN_ENTIDAD_INFORMACION WHERE DD_EIN_DESCRIPCION = ''Expediente''))';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en la tabla');
	END IF;
	
	V_SQL:= 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''EXP_SINSOLUCION''';
	EXECUTE IMMEDIATE V_SQL INTO V_COUNT;
	IF V_COUNT > 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] Ya existe el registro en la tabla');
	ELSE
		V_MSQL:= 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO, DD_EIN_ID) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''EXP_SINSOLUCION'', ''Sin solución'', ''Sin solución'', 0,  ''PRODUCTO-496'', SYSDATE, 0, (SELECT DD_EIN_ID FROM '||V_ESQUEMA_M||'.DD_EIN_ENTIDAD_INFORMACION WHERE DD_EIN_DESCRIPCION = ''Expediente''))';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en la tabla');
	END IF;
	
	V_SQL:= 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''EXP_DISPPRESPRO''';
	EXECUTE IMMEDIATE V_SQL INTO V_COUNT;
	IF V_COUNT > 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] Ya existe el registro en la tabla');
	ELSE
		V_MSQL:= 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO, DD_EIN_ID) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''EXP_DISPPRESPRO'', ''Disposición préstamo Promotor'', ''Disposición préstamo Promotor'', 0,  ''PRODUCTO-496'', SYSDATE, 0, (SELECT DD_EIN_ID FROM '||V_ESQUEMA_M||'.DD_EIN_ENTIDAD_INFORMACION WHERE DD_EIN_DESCRIPCION = ''Expediente''))';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en la tabla');
	END IF;
	
	V_SQL:= 'SELECT COUNT(1) FROM '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO WHERE DD_TPA_CODIGO = ''EXP_COMPVENDACION''';
	EXECUTE IMMEDIATE V_SQL INTO V_COUNT;
	IF V_COUNT > 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] Ya existe el registro en la tabla');
	ELSE
		V_MSQL:= 'INSERT INTO '||V_ESQUEMA||'.DD_TPA_TIPO_ACUERDO (DD_TPA_ID, DD_TPA_CODIGO, DD_TPA_DESCRIPCION, DD_TPA_DESCRIPCION_LARGA, VERSION, USUARIOCREAR, FECHACREAR, BORRADO, DD_EIN_ID) VALUES ('||V_ESQUEMA||'.S_DD_TPA_TIPO_ACUERDO.NEXTVAL, ''EXP_COMPVENDACION'', ''Compra venta/Dación'', ''Compra venta/Dación'', 0,  ''PRODUCTO-496'', SYSDATE, 0, (SELECT DD_EIN_ID FROM '||V_ESQUEMA_M||'.DD_EIN_ENTIDAD_INFORMACION WHERE DD_EIN_DESCRIPCION = ''Expediente''))';
		EXECUTE IMMEDIATE V_MSQL;
		DBMS_OUTPUT.PUT_LINE('[INFO] Registro insertado en la tabla');
	END IF;
	
	COMMIT;
		
EXCEPTION
  WHEN OTHERS THEN
    ERR_NUM := SQLCODE;
    ERR_MSG := SQLERRM;
    DBMS_OUTPUT.put_line('[ERROR] Se ha producido un error en la ejecución:'||TO_CHAR(ERR_NUM));
    DBMS_OUTPUT.put_line('-----------------------------------------------------------'); 
    DBMS_OUTPUT.put_line(ERR_MSG);
    ROLLBACK;
    RAISE;   
END;
/
EXIT;
