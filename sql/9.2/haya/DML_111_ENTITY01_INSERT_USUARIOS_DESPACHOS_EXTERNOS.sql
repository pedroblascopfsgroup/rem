--/*
--##########################################
--## AUTOR=MANUEL MEJIAS
--## FECHA_CREACION=20160624
--## ARTEFACTO=online
--## VERSION_ARTEFACTO=9.2
--## INCIDENCIA_LINK=PRODUCTO-1635
--## PRODUCTO=NO
--## Finalidad: DML
--## 
--## INSTRUCCIONES: Configurar las variables necesarias en el principio del DECLARE
--## VERSIONES:
--##        0.1 Versión inicial
--##########################################
--*/

--Para permitir la visualización de texto en un bloque PL/SQL utilizando DBMS_OUTPUT.PUT_LINE

WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON;
SET DEFINE OFF;

DECLARE
    V_MSQL VARCHAR2(32000 CHAR); -- Sentencia a ejecutar     
    V_ESQUEMA VARCHAR2(25 CHAR):= '#ESQUEMA#'; -- Configuracion Esquema
    V_ESQUEMA_M VARCHAR2(25 CHAR):= '#ESQUEMA_MASTER#'; -- Configuracion Esquema Master
    V_SQL VARCHAR2(4000 CHAR); -- Vble. para consulta que valida la existencia de una tabla.
    V_NUM_TABLAS NUMBER(16); -- Vble. para validar la existencia de una tabla.
    V_TMP NUMBER(16); -- Vble. para validar la existencia de una tabla.      
    V_TMP_2 NUMBER(16); -- Vble. para validar la existencia de una tabla.
    ERR_NUM NUMBER(25);  -- Vble. auxiliar para registrar errores en el script.
    ERR_MSG VARCHAR2(1024 CHAR); -- Vble. auxiliar para registrar errores en el script.

    V_TEXT1 VARCHAR2(2400 CHAR); -- Vble. auxiliar
    V_ENTIDAD_ID NUMBER(16);
    V_DDNAME VARCHAR2(30);
    V_ID_MAX NUMBER(16); -- Vble. para validar la existencia de una tabla.
    V_DD_TDE_ID_OFI NUMBER(16); -- Vble. para almacenar el DD_TDE_ID del tipo despacho oficina.

BEGIN


DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nuevo despacho Notaria de escrituras, Notaria para Acta de liquidación, Notaria para nota simple');
V_NUM_TABLAS := 0;
V_SQL := 'SELECT DD_TDE_ID FROM ' || V_ESQUEMA_M || '.DD_TDE_TIPO_DESPACHO WHERE DD_TDE_CODIGO = ''NOTARI''';
EXECUTE IMMEDIATE V_SQL INTO V_DD_TDE_ID_OFI;
DBMS_OUTPUT.PUT_LINE('[INFO] hay id de DD_TDE_TIPO_DESPACHO: ' || V_DD_TDE_ID_OFI );

DBMS_OUTPUT.PUT_LINE('[INFO] Se consulta tipo despacho Notarias');

V_SQL := 'SELECT COUNT(1) FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DD_TDE_ID = ' || V_DD_TDE_ID_OFI;
EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
IF V_NUM_TABLAS > 0 THEN
  V_SQL := 'SELECT COUNT(1) FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO= ''Notaría''';
  EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
  IF V_NUM_TABLAS > 0 THEN
  	V_MSQL := 'UPDATE ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO SET USUARIOBORRAR = ''PRODUCTO-1635'', FECHABORRAR = SYSDATE, BORRADO = 1 WHERE DES_DESPACHO= ''Notaría'''; 
  	EXECUTE IMMEDIATE V_MSQL;
  END IF;

  V_SQL := 'SELECT COUNT(1) FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO= ''Notaria de escrituras''';
  EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
  IF V_NUM_TABLAS = 0 THEN
  	V_MSQL := 'INSERT INTO ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO (DES_ID, DES_DESPACHO, VERSION, USUARIOCREAR, FECHACREAR, DD_TDE_ID) VALUES (' ||V_ESQUEMA ||'.S_DES_DESPACHO_EXTERNO.NEXTVAL, ''Notaria de escrituras'', 0, ''PRODUCTO-1635'', sysdate, ' || V_DD_TDE_ID_OFI || ')'; 
  	EXECUTE IMMEDIATE V_MSQL;
  END IF;

  V_SQL := 'SELECT COUNT(1) FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO= ''Notaria para Acta de liquidación''';
  EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
  IF V_NUM_TABLAS = 0 THEN
    V_MSQL := 'INSERT INTO ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO (DES_ID, DES_DESPACHO, VERSION, USUARIOCREAR, FECHACREAR, DD_TDE_ID) VALUES (' ||V_ESQUEMA ||'.S_DES_DESPACHO_EXTERNO.NEXTVAL, ''Notaria para Acta de liquidación'', 0, ''PRODUCTO-1635'', sysdate, ' || V_DD_TDE_ID_OFI || ')'; 
    EXECUTE IMMEDIATE V_MSQL;
  END IF;

  V_SQL := 'SELECT COUNT(1) FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO= ''Notaria para nota simple''';
  EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
  IF V_NUM_TABLAS = 0 THEN
    V_MSQL := 'INSERT INTO ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO (DES_ID, DES_DESPACHO, VERSION, USUARIOCREAR, FECHACREAR, DD_TDE_ID) VALUES (' ||V_ESQUEMA ||'.S_DES_DESPACHO_EXTERNO.NEXTVAL, ''Notaria para nota simple'', 0, ''PRODUCTO-1635'', sysdate, ' || V_DD_TDE_ID_OFI || ')'; 
    EXECUTE IMMEDIATE V_MSQL;
  END IF;
END IF;
	
	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nuevo usuario D. Enrique Tejedo Aznar');
	V_NUM_TABLAS := 0;
	V_DDNAME := 'USU_USUARIOS';
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA_M || '.' || V_DDNAME || ' WHERE USU_USERNAME = ''ENTEAZ''';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en usu_usuarios D. Enrique Tejedo Aznar');
		V_MSQL := 'INSERT INTO '|| V_ESQUEMA_M || '.USU_USUARIOS (usu_id, entidad_id, usu_username, usu_password, usu_nombre, usu_apellido1, usu_apellido2, usuariocrear, fechacrear)  values  ('|| V_ESQUEMA_M ||'.s_usu_usuarios.nextval, ''1'',''ENTEAZ'',''1234'',''D. Enrique'', ''Tejedo'', ''Aznar'', ''PRODUCTO-1635'', sysdate)';
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
		EXECUTE IMMEDIATE V_MSQL;
	END IF;

	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nuevo usuario D. Ernesto Rios Segarra');
	V_NUM_TABLAS := 0;
	V_DDNAME := 'USU_USUARIOS';
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA_M || '.' || V_DDNAME || ' WHERE USU_USERNAME = ''ERRISE''';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	IF V_NUM_TABLAS = 0 THEN
	 	DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en usu_usuarios D. Ernesto Rios Segarra');
		V_MSQL := 'INSERT INTO '|| V_ESQUEMA_M || '.USU_USUARIOS (usu_id, entidad_id, usu_username, usu_password, usu_nombre, usu_apellido1, usu_apellido2, usuariocrear, fechacrear)  values  ('|| V_ESQUEMA_M ||'.s_usu_usuarios.nextval, ''1'',''ERRISE'',''1234'',''D. Ernesto'', ''Rios'', ''Segarra'', ''PRODUCTO-1635'', sysdate)';
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
		EXECUTE IMMEDIATE V_MSQL;
	END IF;


	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nuevo usuario D. Fernando Senent Ana');
	V_NUM_TABLAS := 0;
	V_DDNAME := 'USU_USUARIOS';
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA_M || '.' || V_DDNAME || ' WHERE USU_USERNAME = ''FESEAN''';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en usu_usuarios D. Fernando Senent Ana');
		V_MSQL := 'INSERT INTO '|| V_ESQUEMA_M || '.USU_USUARIOS (usu_id, entidad_id, usu_username, usu_password, usu_nombre, usu_apellido1, usu_apellido2, usuariocrear, fechacrear)  values  ('|| V_ESQUEMA_M ||'.s_usu_usuarios.nextval, ''1'',''FESEAN'',''1234'',''D. Fernando'', ''Senent'', ''Ana'', ''PRODUCTO-1635'', sysdate)';
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
		EXECUTE IMMEDIATE V_MSQL;
	END IF;


	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nuevo usuario D. Francisco Javier Guerrero Arias');
	V_NUM_TABLAS := 0;
	V_DDNAME := 'USU_USUARIOS';
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA_M || '.' || V_DDNAME || ' WHERE USU_USERNAME = ''FRJAGUAR''';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en usu_usuarios D. Francisco Javier Guerrero Arias');
		V_MSQL := 'INSERT INTO '|| V_ESQUEMA_M || '.USU_USUARIOS (usu_id, entidad_id, usu_username, usu_password, usu_nombre, usu_apellido1, usu_apellido2, usuariocrear, fechacrear)  values  ('|| V_ESQUEMA_M ||'.s_usu_usuarios.nextval, ''1'',''FRJAGUAR'',''1234'',''D. Francisco Javier'', ''Guerrero'', ''Arias'', ''PRODUCTO-1635'', sysdate)';
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
		EXECUTE IMMEDIATE V_MSQL;
	END IF;

	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nuevo usuario D. Francisco José Mondaray Pérez');
	V_NUM_TABLAS := 0;
	V_DDNAME := 'USU_USUARIOS';
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA_M || '.' || V_DDNAME || ' WHERE USU_USERNAME = ''FRJOMOPE''';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en usu_usuarios D. Francisco José Mondaray Pérez');
		V_MSQL := 'INSERT INTO '|| V_ESQUEMA_M || '.USU_USUARIOS (usu_id, entidad_id, usu_username, usu_password, usu_nombre, usu_apellido1, usu_apellido2, usuariocrear, fechacrear)  values  ('|| V_ESQUEMA_M ||'.s_usu_usuarios.nextval, ''1'',''FRJOMOPE'',''1234'',''D. Francisco José'', ''Mondaray'', ''Pérez'', ''PRODUCTO-1635'', sysdate)';
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
		EXECUTE IMMEDIATE V_MSQL;
	END IF;

	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nuevo usuario D. Jesús Toledano García');
	V_NUM_TABLAS := 0;
	V_DDNAME := 'USU_USUARIOS';
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA_M || '.' || V_DDNAME || ' WHERE USU_USERNAME = ''JETOGA''';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en usu_usuarios D. Jesús Toledano García');
		V_MSQL := 'INSERT INTO '|| V_ESQUEMA_M || '.USU_USUARIOS (usu_id, entidad_id, usu_username, usu_password, usu_nombre, usu_apellido1, usu_apellido2, usuariocrear, fechacrear)  values  ('|| V_ESQUEMA_M ||'.s_usu_usuarios.nextval, ''1'',''JETOGA'',''1234'',''D. Jesús'', ''Toledano'', ''García'', ''PRODUCTO-1635'', sysdate)';
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
		EXECUTE IMMEDIATE V_MSQL;
	END IF;

	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nuevo usuario D. José Chust Ballester');
	V_NUM_TABLAS := 0;
	V_DDNAME := 'USU_USUARIOS';
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA_M || '.' || V_DDNAME || ' WHERE USU_USERNAME = ''JOCHUBA''';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en usu_usuarios D. José Chust Ballester');
		V_MSQL := 'INSERT INTO '|| V_ESQUEMA_M || '.USU_USUARIOS (usu_id, entidad_id, usu_username, usu_password, usu_nombre, usu_apellido1, usu_apellido2, usuariocrear, fechacrear)  values  ('|| V_ESQUEMA_M ||'.s_usu_usuarios.nextval, ''1'',''JOCHUBA'',''1234'',''D. José'', ''Chust'', ''Ballester'', ''PRODUCTO-1635'', sysdate)';
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
		EXECUTE IMMEDIATE V_MSQL;
	END IF;

	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nuevo usuario D. José Manuel Sánchez Almela');
	V_NUM_TABLAS := 0;
	V_DDNAME := 'USU_USUARIOS';
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA_M || '.' || V_DDNAME || ' WHERE USU_USERNAME = ''JOMASAAL''';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en usu_usuarios D. José Manuel Sánchez Almela');
		V_MSQL := 'INSERT INTO '|| V_ESQUEMA_M || '.USU_USUARIOS (usu_id, entidad_id, usu_username, usu_password, usu_nombre, usu_apellido1, usu_apellido2, usuariocrear, fechacrear)  values  ('|| V_ESQUEMA_M ||'.s_usu_usuarios.nextval, ''1'',''JOMASAAL'',''1234'',''D. José Manuel'', ''Sánchez'', ''Almela'', ''PRODUCTO-1635'', sysdate)';
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
		EXECUTE IMMEDIATE V_MSQL;
	END IF;

	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nuevo usuario D. Juan Antonio Noguera Velez');
	V_NUM_TABLAS := 0;
	V_DDNAME := 'USU_USUARIOS';
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA_M || '.' || V_DDNAME || ' WHERE USU_USERNAME = ''JUANNOVE''';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en usu_usuarios D. Juan Antonio Noguera Velez');
		V_MSQL := 'INSERT INTO '|| V_ESQUEMA_M || '.USU_USUARIOS (usu_id, entidad_id, usu_username, usu_password, usu_nombre, usu_apellido1, usu_apellido2, usuariocrear, fechacrear)  values  ('|| V_ESQUEMA_M ||'.s_usu_usuarios.nextval, ''1'',''JUANNOVE'',''1234'',''D. Juan Antonio'', ''Noguera'', ''Velez'', ''PRODUCTO-1635'', sysdate)';
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
		EXECUTE IMMEDIATE V_MSQL;
	END IF;

	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nuevo usuario D. Santiago Augusto/Ernesto Ruiz Rodriguez');
	V_NUM_TABLAS := 0;
	V_DDNAME := 'USU_USUARIOS';
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA_M || '.' || V_DDNAME || ' WHERE USU_USERNAME = ''SAAUERRURO''';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en usu_usuarios D. Santiago Augusto/Ernesto Ruiz Rodriguez');
		V_MSQL := 'INSERT INTO '|| V_ESQUEMA_M || '.USU_USUARIOS (usu_id, entidad_id, usu_username, usu_password, usu_nombre, usu_apellido1, usu_apellido2, usuariocrear, fechacrear)  values  ('|| V_ESQUEMA_M ||'.s_usu_usuarios.nextval, ''1'',''SAAUERRURO'',''1234'',''D. Santiago Augusto/Ernesto'', ''Ruiz'', ''Rodriguez'', ''PRODUCTO-1635'', sysdate)';
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
		EXECUTE IMMEDIATE V_MSQL;
	END IF;

	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nuevo usuario D. Vicente Sorribes Gisbert');
	V_NUM_TABLAS := 0;
	V_DDNAME := 'USU_USUARIOS';
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA_M || '.' || V_DDNAME || ' WHERE USU_USERNAME = ''VISOGI''';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en usu_usuarios D. Vicente Sorribes Gisbert');
		V_MSQL := 'INSERT INTO '|| V_ESQUEMA_M || '.USU_USUARIOS (usu_id, entidad_id, usu_username, usu_password, usu_nombre, usu_apellido1, usu_apellido2, usuariocrear, fechacrear)  values  ('|| V_ESQUEMA_M ||'.s_usu_usuarios.nextval, ''1'',''VISOGI'',''1234'',''D. Vicente'', ''Sorribes'', ''Gisbert'', ''PRODUCTO-1635'', sysdate)';
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
		EXECUTE IMMEDIATE V_MSQL;
	END IF;

	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nuevo usuario Dña. Mª. Elena Sánchez Calvo');
	V_NUM_TABLAS := 0;
	V_DDNAME := 'USU_USUARIOS';
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA_M || '.' || V_DDNAME || ' WHERE USU_USERNAME = ''ELSACA''';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en usu_usuarios D. Mª. Elena Sánchez Calvo');
		V_MSQL := 'INSERT INTO '|| V_ESQUEMA_M || '.USU_USUARIOS (usu_id, entidad_id, usu_username, usu_password, usu_nombre, usu_apellido1, usu_apellido2, usuariocrear, fechacrear)  values  ('|| V_ESQUEMA_M ||'.s_usu_usuarios.nextval, ''1'',''ELSACA'',''1234'',''Dña. Mª. Elena'', ''Sánchez'', ''Calvo'', ''PRODUCTO-1635'', sysdate)';
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
		EXECUTE IMMEDIATE V_MSQL;
	END IF;

	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nuevo usuario Dña. María Pilar Nácher Martí');
	V_NUM_TABLAS := 0;
	V_DDNAME := 'USU_USUARIOS';
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA_M || '.' || V_DDNAME || ' WHERE USU_USERNAME = ''MAPINAMA''';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en usu_usuarios D. María Pilar Nácher Martí');
		V_MSQL := 'INSERT INTO '|| V_ESQUEMA_M || '.USU_USUARIOS (usu_id, entidad_id, usu_username, usu_password, usu_nombre, usu_apellido1, usu_apellido2, usuariocrear, fechacrear)  values  ('|| V_ESQUEMA_M ||'.s_usu_usuarios.nextval, ''1'',''MAPINAMA'',''1234'',''Dña. María Pilar'', ''Nácher'', ''Martí'', ''PRODUCTO-1635'', sysdate)';
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
		EXECUTE IMMEDIATE V_MSQL;
	END IF;

	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nuevo usuario Sres Badia/Orts');
	V_NUM_TABLAS := 0;
	V_DDNAME := 'USU_USUARIOS';
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA_M || '.' || V_DDNAME || ' WHERE USU_USERNAME = ''BADIA_ORTS''';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en usu_usuarios Sres Badia/Orts');
		V_MSQL := 'INSERT INTO '|| V_ESQUEMA_M || '.USU_USUARIOS (usu_id, entidad_id, usu_username, usu_password, usu_nombre, usu_apellido1, usu_apellido2, usuariocrear, fechacrear)  values  ('|| V_ESQUEMA_M ||'.s_usu_usuarios.nextval, ''1'',''BADIA_ORTS'',''1234'',''Sres Badia/Orts'', '''', '''', ''PRODUCTO-1635'', sysdate)';
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
		EXECUTE IMMEDIATE V_MSQL;
	END IF;

	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nuevo usuario Sres Corbi/Piquer');
	V_NUM_TABLAS := 0;
	V_DDNAME := 'USU_USUARIOS';
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA_M || '.' || V_DDNAME || ' WHERE USU_USERNAME = ''CORBI_PIQUER''';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en usu_usuarios Sres Corbi/Piquer');
		V_MSQL := 'INSERT INTO '|| V_ESQUEMA_M || '.USU_USUARIOS (usu_id, entidad_id, usu_username, usu_password, usu_nombre, usu_apellido1, usu_apellido2, usuariocrear, fechacrear)  values  ('|| V_ESQUEMA_M ||'.s_usu_usuarios.nextval, ''1'',''CORBI_PIQUER'',''1234'',''Sres Corbi/Piquer'', '''', '''', ''PRODUCTO-1635'', sysdate)';
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
		EXECUTE IMMEDIATE V_MSQL;
	END IF;

	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nuevo usuario Sres. Cachon/ Mellado');
	V_NUM_TABLAS := 0;
	V_DDNAME := 'USU_USUARIOS';
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA_M || '.' || V_DDNAME || ' WHERE USU_USERNAME = ''CACHON_MELLADO''';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en usu_usuarios Sres Cachon/ Mellado');
		V_MSQL := 'INSERT INTO '|| V_ESQUEMA_M || '.USU_USUARIOS (usu_id, entidad_id, usu_username, usu_password, usu_nombre, usu_apellido1, usu_apellido2, usuariocrear, fechacrear)  values  ('|| V_ESQUEMA_M ||'.s_usu_usuarios.nextval, ''1'',''CACHON_MELLADO'',''1234'',''Sres Cachon/ Mellado'', '''', '''', ''PRODUCTO-1635'', sysdate)';
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
		EXECUTE IMMEDIATE V_MSQL;
	END IF;


	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nueva entrada USD_USUARIOS_DESPACHOS');
	V_NUM_TABLAS := 0;
	V_TMP := 0;
	V_TMP_2 := 0;
	V_DDNAME := 'USD_USUARIOS_DESPACHOS';
	V_SQL := 'SELECT USU_ID FROM ' || V_ESQUEMA_M || '.USU_USUARIOS WHERE USU_USERNAME = ''ENTEAZ''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL USUARIO ES ' || V_TMP);
	V_SQL := 'SELECT DES_ID FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO = ''Notaria para Acta de liquidación''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP_2;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL DESPACHO ES ' || V_TMP_2);
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA || '.' || V_DDNAME || ' WHERE USU_ID = '|| V_TMP ||' AND DES_ID = '|| V_TMP_2 ||'';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	DBMS_OUTPUT.PUT_LINE('EL TOTAL PARA USUARIO-DESPACHO ES ' || V_NUM_TABLAS);
	
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en USD_USUARIOS_DESPACHOS');
	V_MSQL := 'INSERT INTO ' || V_ESQUEMA || '.' || V_DDNAME || '(USD_ID,USU_ID,DES_ID,USD_GESTOR_DEFECTO,USD_SUPERVISOR,USUARIOCREAR,FECHACREAR) VALUES('|| V_ESQUEMA ||'.s_usd_usuarios_despachos.nextval,'|| V_TMP ||','|| V_TMP_2 ||',1,1,''PRODUCTO-1635'',sysdate)';
	DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
	EXECUTE IMMEDIATE V_MSQL;
	END IF;	

	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nueva entrada USD_USUARIOS_DESPACHOS');
	V_NUM_TABLAS := 0;
	V_TMP := 0;
	V_TMP_2 := 0;
	V_DDNAME := 'USD_USUARIOS_DESPACHOS';
	V_SQL := 'SELECT USU_ID FROM ' || V_ESQUEMA_M || '.USU_USUARIOS WHERE USU_USERNAME = ''ERRISE''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL USUARIO ES ' || V_TMP);
	V_SQL := 'SELECT DES_ID FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO = ''Notaria para Acta de liquidación''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP_2;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL DESPACHO ES ' || V_TMP_2);
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA || '.' || V_DDNAME || ' WHERE USU_ID = '|| V_TMP ||' AND DES_ID = '|| V_TMP_2 ||'';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	DBMS_OUTPUT.PUT_LINE('EL TOTAL PARA USUARIO-DESPACHO ES ' || V_NUM_TABLAS);
	
	IF V_NUM_TABLAS = 1 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] UPDATEANDO en USD_USUARIOS_DESPACHOS');
	V_MSQL := 'UPDATE ' || V_ESQUEMA || '.' || V_DDNAME || ' SEE(USD_ID,USU_ID,DES_ID,USD_GESTOR_DEFECTO,USD_SUPERVISOR,USUARIOCREAR,FECHACREAR) VALUES('|| V_ESQUEMA ||'.s_usd_usuarios_despachos.nextval,'|| V_TMP ||','|| V_TMP_2 ||',1,1,''PRODUCTO-1635'',sysdate)';
	DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
	EXECUTE IMMEDIATE V_MSQL;
	END IF;	
	
	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nueva entrada USD_USUARIOS_DESPACHOS');
	V_NUM_TABLAS := 0;
	V_TMP := 0;
	V_TMP_2 := 0;
	V_DDNAME := 'USD_USUARIOS_DESPACHOS';
	V_SQL := 'SELECT USU_ID FROM ' || V_ESQUEMA_M || '.USU_USUARIOS WHERE USU_USERNAME = ''FESEAN''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL USUARIO ES ' || V_TMP);
	V_SQL := 'SELECT DES_ID FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO = ''Notaria para Acta de liquidación''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP_2;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL DESPACHO ES ' || V_TMP_2);
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA || '.' || V_DDNAME || ' WHERE USU_ID = '|| V_TMP ||' AND DES_ID = '|| V_TMP_2 ||'';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	DBMS_OUTPUT.PUT_LINE('EL TOTAL PARA USUARIO-DESPACHO ES ' || V_NUM_TABLAS);
	
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en USD_USUARIOS_DESPACHOS');
	V_MSQL := 'INSERT INTO ' || V_ESQUEMA || '.' || V_DDNAME || '(USD_ID,USU_ID,DES_ID,USD_GESTOR_DEFECTO,USD_SUPERVISOR,USUARIOCREAR,FECHACREAR) VALUES('|| V_ESQUEMA ||'.s_usd_usuarios_despachos.nextval,'|| V_TMP ||','|| V_TMP_2 ||',1,1,''PRODUCTO-1635'',sysdate)';
	DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
	EXECUTE IMMEDIATE V_MSQL;
	END IF;	
	
	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nueva entrada USD_USUARIOS_DESPACHOS');
	V_NUM_TABLAS := 0;
	V_TMP := 0;
	V_TMP_2 := 0;
	V_DDNAME := 'USD_USUARIOS_DESPACHOS';
	V_SQL := 'SELECT USU_ID FROM ' || V_ESQUEMA_M || '.USU_USUARIOS WHERE USU_USERNAME = ''FRJAGUAR''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL USUARIO ES ' || V_TMP);
	V_SQL := 'SELECT DES_ID FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO = ''Notaria para Acta de liquidación''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP_2;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL DESPACHO ES ' || V_TMP_2);
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA || '.' || V_DDNAME || ' WHERE USU_ID = '|| V_TMP ||' AND DES_ID = '|| V_TMP_2 ||'';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	DBMS_OUTPUT.PUT_LINE('EL TOTAL PARA USUARIO-DESPACHO ES ' || V_NUM_TABLAS);
	
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en USD_USUARIOS_DESPACHOS');
	V_MSQL := 'INSERT INTO ' || V_ESQUEMA || '.' || V_DDNAME || '(USD_ID,USU_ID,DES_ID,USD_GESTOR_DEFECTO,USD_SUPERVISOR,USUARIOCREAR,FECHACREAR) VALUES('|| V_ESQUEMA ||'.s_usd_usuarios_despachos.nextval,'|| V_TMP ||','|| V_TMP_2 ||',1,1,''PRODUCTO-1635'',sysdate)';
	DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
	EXECUTE IMMEDIATE V_MSQL;
	END IF;	

	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nueva entrada USD_USUARIOS_DESPACHOS');
	V_NUM_TABLAS := 0;
	V_TMP := 0;
	V_TMP_2 := 0;
	V_DDNAME := 'USD_USUARIOS_DESPACHOS';
	V_SQL := 'SELECT USU_ID FROM ' || V_ESQUEMA_M || '.USU_USUARIOS WHERE USU_USERNAME = ''FRJOMOPE''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL USUARIO ES ' || V_TMP);
	V_SQL := 'SELECT DES_ID FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO = ''Notaria para Acta de liquidación''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP_2;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL DESPACHO ES ' || V_TMP_2);
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA || '.' || V_DDNAME || ' WHERE USU_ID = '|| V_TMP ||' AND DES_ID = '|| V_TMP_2 ||'';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	DBMS_OUTPUT.PUT_LINE('EL TOTAL PARA USUARIO-DESPACHO ES ' || V_NUM_TABLAS);
	
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en USD_USUARIOS_DESPACHOS');
	V_MSQL := 'INSERT INTO ' || V_ESQUEMA || '.' || V_DDNAME || '(USD_ID,USU_ID,DES_ID,USD_GESTOR_DEFECTO,USD_SUPERVISOR,USUARIOCREAR,FECHACREAR) VALUES('|| V_ESQUEMA ||'.s_usd_usuarios_despachos.nextval,'|| V_TMP ||','|| V_TMP_2 ||',1,1,''PRODUCTO-1635'',sysdate)';
	DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
	EXECUTE IMMEDIATE V_MSQL;
	END IF;	
	
	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nueva entrada USD_USUARIOS_DESPACHOS');
	V_NUM_TABLAS := 0;
	V_TMP := 0;
	V_TMP_2 := 0;
	V_DDNAME := 'USD_USUARIOS_DESPACHOS';
	V_SQL := 'SELECT USU_ID FROM ' || V_ESQUEMA_M || '.USU_USUARIOS WHERE USU_USERNAME = ''JETOGA''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL USUARIO ES ' || V_TMP);
	V_SQL := 'SELECT DES_ID FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO = ''Notaria para Acta de liquidación''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP_2;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL DESPACHO ES ' || V_TMP_2);
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA || '.' || V_DDNAME || ' WHERE USU_ID = '|| V_TMP ||' AND DES_ID = '|| V_TMP_2 ||'';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	DBMS_OUTPUT.PUT_LINE('EL TOTAL PARA USUARIO-DESPACHO ES ' || V_NUM_TABLAS);
	
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en USD_USUARIOS_DESPACHOS');
	V_MSQL := 'INSERT INTO ' || V_ESQUEMA || '.' || V_DDNAME || '(USD_ID,USU_ID,DES_ID,USD_GESTOR_DEFECTO,USD_SUPERVISOR,USUARIOCREAR,FECHACREAR) VALUES('|| V_ESQUEMA ||'.s_usd_usuarios_despachos.nextval,'|| V_TMP ||','|| V_TMP_2 ||',1,1,''PRODUCTO-1635'',sysdate)';
	DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
	EXECUTE IMMEDIATE V_MSQL;
	END IF;
	
	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nueva entrada USD_USUARIOS_DESPACHOS');
	V_NUM_TABLAS := 0;
	V_TMP := 0;
	V_TMP_2 := 0;
	V_DDNAME := 'USD_USUARIOS_DESPACHOS';
	V_SQL := 'SELECT USU_ID FROM ' || V_ESQUEMA_M || '.USU_USUARIOS WHERE USU_USERNAME = ''JOCHUBA''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL USUARIO ES ' || V_TMP);
	V_SQL := 'SELECT DES_ID FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO = ''Notaria para Acta de liquidación''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP_2;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL DESPACHO ES ' || V_TMP_2);
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA || '.' || V_DDNAME || ' WHERE USU_ID = '|| V_TMP ||' AND DES_ID = '|| V_TMP_2 ||'';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	DBMS_OUTPUT.PUT_LINE('EL TOTAL PARA USUARIO-DESPACHO ES ' || V_NUM_TABLAS);
	
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en USD_USUARIOS_DESPACHOS');
	V_MSQL := 'INSERT INTO ' || V_ESQUEMA || '.' || V_DDNAME || '(USD_ID,USU_ID,DES_ID,USD_GESTOR_DEFECTO,USD_SUPERVISOR,USUARIOCREAR,FECHACREAR) VALUES('|| V_ESQUEMA ||'.s_usd_usuarios_despachos.nextval,'|| V_TMP ||','|| V_TMP_2 ||',1,1,''PRODUCTO-1635'',sysdate)';
	DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
	EXECUTE IMMEDIATE V_MSQL;
	END IF;
	
	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nueva entrada USD_USUARIOS_DESPACHOS');
	V_NUM_TABLAS := 0;
	V_TMP := 0;
	V_TMP_2 := 0;
	V_DDNAME := 'USD_USUARIOS_DESPACHOS';
	V_SQL := 'SELECT USU_ID FROM ' || V_ESQUEMA_M || '.USU_USUARIOS WHERE USU_USERNAME = ''JOMASAAL''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL USUARIO ES ' || V_TMP);
	V_SQL := 'SELECT DES_ID FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO = ''Notaria para Acta de liquidación''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP_2;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL DESPACHO ES ' || V_TMP_2);
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA || '.' || V_DDNAME || ' WHERE USU_ID = '|| V_TMP ||' AND DES_ID = '|| V_TMP_2 ||'';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	DBMS_OUTPUT.PUT_LINE('EL TOTAL PARA USUARIO-DESPACHO ES ' || V_NUM_TABLAS);
	
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en USD_USUARIOS_DESPACHOS');
	V_MSQL := 'INSERT INTO ' || V_ESQUEMA || '.' || V_DDNAME || '(USD_ID,USU_ID,DES_ID,USD_GESTOR_DEFECTO,USD_SUPERVISOR,USUARIOCREAR,FECHACREAR) VALUES('|| V_ESQUEMA ||'.s_usd_usuarios_despachos.nextval,'|| V_TMP ||','|| V_TMP_2 ||',1,1,''PRODUCTO-1635'',sysdate)';
	DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
	EXECUTE IMMEDIATE V_MSQL;
	END IF;
	
	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nueva entrada USD_USUARIOS_DESPACHOS');
	V_NUM_TABLAS := 0;
	V_TMP := 0;
	V_TMP_2 := 0;
	V_DDNAME := 'USD_USUARIOS_DESPACHOS';
	V_SQL := 'SELECT USU_ID FROM ' || V_ESQUEMA_M || '.USU_USUARIOS WHERE USU_USERNAME = ''JUANNOVE''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL USUARIO ES ' || V_TMP);
	V_SQL := 'SELECT DES_ID FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO = ''Notaria para Acta de liquidación''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP_2;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL DESPACHO ES ' || V_TMP_2);
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA || '.' || V_DDNAME || ' WHERE USU_ID = '|| V_TMP ||' AND DES_ID = '|| V_TMP_2 ||'';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	DBMS_OUTPUT.PUT_LINE('EL TOTAL PARA USUARIO-DESPACHO ES ' || V_NUM_TABLAS);
	
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en USD_USUARIOS_DESPACHOS');
	V_MSQL := 'INSERT INTO ' || V_ESQUEMA || '.' || V_DDNAME || '(USD_ID,USU_ID,DES_ID,USD_GESTOR_DEFECTO,USD_SUPERVISOR,USUARIOCREAR,FECHACREAR) VALUES('|| V_ESQUEMA ||'.s_usd_usuarios_despachos.nextval,'|| V_TMP ||','|| V_TMP_2 ||',1,1,''PRODUCTO-1635'',sysdate)';
	DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
	EXECUTE IMMEDIATE V_MSQL;
	END IF;
	
	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nueva entrada USD_USUARIOS_DESPACHOS');
	V_NUM_TABLAS := 0;
	V_TMP := 0;
	V_TMP_2 := 0;
	V_DDNAME := 'USD_USUARIOS_DESPACHOS';
	V_SQL := 'SELECT USU_ID FROM ' || V_ESQUEMA_M || '.USU_USUARIOS WHERE USU_USERNAME = ''SAAUERRURO''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL USUARIO ES ' || V_TMP);
	V_SQL := 'SELECT DES_ID FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO = ''Notaria para Acta de liquidación''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP_2;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL DESPACHO ES ' || V_TMP_2);
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA || '.' || V_DDNAME || ' WHERE USU_ID = '|| V_TMP ||' AND DES_ID = '|| V_TMP_2 ||'';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	DBMS_OUTPUT.PUT_LINE('EL TOTAL PARA USUARIO-DESPACHO ES ' || V_NUM_TABLAS);
	
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en USD_USUARIOS_DESPACHOS');
	V_MSQL := 'INSERT INTO ' || V_ESQUEMA || '.' || V_DDNAME || '(USD_ID,USU_ID,DES_ID,USD_GESTOR_DEFECTO,USD_SUPERVISOR,USUARIOCREAR,FECHACREAR) VALUES('|| V_ESQUEMA ||'.s_usd_usuarios_despachos.nextval,'|| V_TMP ||','|| V_TMP_2 ||',1,1,''PRODUCTO-1635'',sysdate)';
	DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
	EXECUTE IMMEDIATE V_MSQL;
	END IF;
	
	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nueva entrada USD_USUARIOS_DESPACHOS');
	V_NUM_TABLAS := 0;
	V_TMP := 0;
	V_TMP_2 := 0;
	V_DDNAME := 'USD_USUARIOS_DESPACHOS';
	V_SQL := 'SELECT USU_ID FROM ' || V_ESQUEMA_M || '.USU_USUARIOS WHERE USU_USERNAME = ''VISOGI''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL USUARIO ES ' || V_TMP);
	V_SQL := 'SELECT DES_ID FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO = ''Notaria para Acta de liquidación''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP_2;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL DESPACHO ES ' || V_TMP_2);
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA || '.' || V_DDNAME || ' WHERE USU_ID = '|| V_TMP ||' AND DES_ID = '|| V_TMP_2 ||'';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	DBMS_OUTPUT.PUT_LINE('EL TOTAL PARA USUARIO-DESPACHO ES ' || V_NUM_TABLAS);
	
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en USD_USUARIOS_DESPACHOS');
	V_MSQL := 'INSERT INTO ' || V_ESQUEMA || '.' || V_DDNAME || '(USD_ID,USU_ID,DES_ID,USD_GESTOR_DEFECTO,USD_SUPERVISOR,USUARIOCREAR,FECHACREAR) VALUES('|| V_ESQUEMA ||'.s_usd_usuarios_despachos.nextval,'|| V_TMP ||','|| V_TMP_2 ||',1,1,''PRODUCTO-1635'',sysdate)';
	DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
	EXECUTE IMMEDIATE V_MSQL;
	END IF;
	
	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nueva entrada USD_USUARIOS_DESPACHOS');
	V_NUM_TABLAS := 0;
	V_TMP := 0;
	V_TMP_2 := 0;
	V_DDNAME := 'USD_USUARIOS_DESPACHOS';
	V_SQL := 'SELECT USU_ID FROM ' || V_ESQUEMA_M || '.USU_USUARIOS WHERE USU_USERNAME = ''ELSACA''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL USUARIO ES ' || V_TMP);
	V_SQL := 'SELECT DES_ID FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO = ''Notaria para Acta de liquidación''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP_2;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL DESPACHO ES ' || V_TMP_2);
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA || '.' || V_DDNAME || ' WHERE USU_ID = '|| V_TMP ||' AND DES_ID = '|| V_TMP_2 ||'';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	DBMS_OUTPUT.PUT_LINE('EL TOTAL PARA USUARIO-DESPACHO ES ' || V_NUM_TABLAS);
	
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en USD_USUARIOS_DESPACHOS');
	V_MSQL := 'INSERT INTO ' || V_ESQUEMA || '.' || V_DDNAME || '(USD_ID,USU_ID,DES_ID,USD_GESTOR_DEFECTO,USD_SUPERVISOR,USUARIOCREAR,FECHACREAR) VALUES('|| V_ESQUEMA ||'.s_usd_usuarios_despachos.nextval,'|| V_TMP ||','|| V_TMP_2 ||',1,1,''PRODUCTO-1635'',sysdate)';
	DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
	EXECUTE IMMEDIATE V_MSQL;
	END IF;

	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nueva entrada USD_USUARIOS_DESPACHOS');
	V_NUM_TABLAS := 0;
	V_TMP := 0;
	V_TMP_2 := 0;
	V_DDNAME := 'USD_USUARIOS_DESPACHOS';
	V_SQL := 'SELECT USU_ID FROM ' || V_ESQUEMA_M || '.USU_USUARIOS WHERE USU_USERNAME = ''MAPINAMA''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL USUARIO ES ' || V_TMP);
	V_SQL := 'SELECT DES_ID FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO = ''Notaria para Acta de liquidación''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP_2;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL DESPACHO ES ' || V_TMP_2);
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA || '.' || V_DDNAME || ' WHERE USU_ID = '|| V_TMP ||' AND DES_ID = '|| V_TMP_2 ||'';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	DBMS_OUTPUT.PUT_LINE('EL TOTAL PARA USUARIO-DESPACHO ES ' || V_NUM_TABLAS);
	
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en USD_USUARIOS_DESPACHOS');
	V_MSQL := 'INSERT INTO ' || V_ESQUEMA || '.' || V_DDNAME || '(USD_ID,USU_ID,DES_ID,USD_GESTOR_DEFECTO,USD_SUPERVISOR,USUARIOCREAR,FECHACREAR) VALUES('|| V_ESQUEMA ||'.s_usd_usuarios_despachos.nextval,'|| V_TMP ||','|| V_TMP_2 ||',1,1,''PRODUCTO-1635'',sysdate)';
	DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
	EXECUTE IMMEDIATE V_MSQL;
	END IF;
	
	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nueva entrada USD_USUARIOS_DESPACHOS');
	V_NUM_TABLAS := 0;
	V_TMP := 0;
	V_TMP_2 := 0;
	V_DDNAME := 'USD_USUARIOS_DESPACHOS';
	V_SQL := 'SELECT USU_ID FROM ' || V_ESQUEMA_M || '.USU_USUARIOS WHERE USU_USERNAME = ''BADIA_ORTS''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL USUARIO ES ' || V_TMP);
	V_SQL := 'SELECT DES_ID FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO = ''Notaria para Acta de liquidación''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP_2;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL DESPACHO ES ' || V_TMP_2);
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA || '.' || V_DDNAME || ' WHERE USU_ID = '|| V_TMP ||' AND DES_ID = '|| V_TMP_2 ||'';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	DBMS_OUTPUT.PUT_LINE('EL TOTAL PARA USUARIO-DESPACHO ES ' || V_NUM_TABLAS);
	
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en USD_USUARIOS_DESPACHOS');
	V_MSQL := 'INSERT INTO ' || V_ESQUEMA || '.' || V_DDNAME || '(USD_ID,USU_ID,DES_ID,USD_GESTOR_DEFECTO,USD_SUPERVISOR,USUARIOCREAR,FECHACREAR) VALUES('|| V_ESQUEMA ||'.s_usd_usuarios_despachos.nextval,'|| V_TMP ||','|| V_TMP_2 ||',1,1,''PRODUCTO-1635'',sysdate)';
	DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
	EXECUTE IMMEDIATE V_MSQL;
	END IF;
	
	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nueva entrada USD_USUARIOS_DESPACHOS');
	V_NUM_TABLAS := 0;
	V_TMP := 0;
	V_TMP_2 := 0;
	V_DDNAME := 'USD_USUARIOS_DESPACHOS';
	V_SQL := 'SELECT USU_ID FROM ' || V_ESQUEMA_M || '.USU_USUARIOS WHERE USU_USERNAME = ''CORBI_PIQUER''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL USUARIO ES ' || V_TMP);
	V_SQL := 'SELECT DES_ID FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO = ''Notaria para Acta de liquidación''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP_2;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL DESPACHO ES ' || V_TMP_2);
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA || '.' || V_DDNAME || ' WHERE USU_ID = '|| V_TMP ||' AND DES_ID = '|| V_TMP_2 ||'';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	DBMS_OUTPUT.PUT_LINE('EL TOTAL PARA USUARIO-DESPACHO ES ' || V_NUM_TABLAS);
	
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en USD_USUARIOS_DESPACHOS');
	V_MSQL := 'INSERT INTO ' || V_ESQUEMA || '.' || V_DDNAME || '(USD_ID,USU_ID,DES_ID,USD_GESTOR_DEFECTO,USD_SUPERVISOR,USUARIOCREAR,FECHACREAR) VALUES('|| V_ESQUEMA ||'.s_usd_usuarios_despachos.nextval,'|| V_TMP ||','|| V_TMP_2 ||',1,1,''PRODUCTO-1635'',sysdate)';
	DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
	EXECUTE IMMEDIATE V_MSQL;
	END IF;
	
	DBMS_OUTPUT.PUT_LINE('[INFO] Insertar nueva entrada USD_USUARIOS_DESPACHOS');
	V_NUM_TABLAS := 0;
	V_TMP := 0;
	V_TMP_2 := 0;
	V_DDNAME := 'USD_USUARIOS_DESPACHOS';
	V_SQL := 'SELECT USU_ID FROM ' || V_ESQUEMA_M || '.USU_USUARIOS WHERE USU_USERNAME = ''CACHON_MELLADO''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL USUARIO ES ' || V_TMP);
	V_SQL := 'SELECT DES_ID FROM ' || V_ESQUEMA || '.DES_DESPACHO_EXTERNO WHERE DES_DESPACHO = ''Notaria para Acta de liquidación''';
	EXECUTE IMMEDIATE V_SQL INTO V_TMP_2;
	DBMS_OUTPUT.PUT_LINE('EL ID DEL DESPACHO ES ' || V_TMP_2);
	V_SQL := 'SELECT COUNT(*) FROM ' || V_ESQUEMA || '.' || V_DDNAME || ' WHERE USU_ID = '|| V_TMP ||' AND DES_ID = '|| V_TMP_2 ||'';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	DBMS_OUTPUT.PUT_LINE('EL TOTAL PARA USUARIO-DESPACHO ES ' || V_NUM_TABLAS);
	
	IF V_NUM_TABLAS = 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO en USD_USUARIOS_DESPACHOS');
	V_MSQL := 'INSERT INTO ' || V_ESQUEMA || '.' || V_DDNAME || '(USD_ID,USU_ID,DES_ID,USD_GESTOR_DEFECTO,USD_SUPERVISOR,USUARIOCREAR,FECHACREAR) VALUES('|| V_ESQUEMA ||'.s_usd_usuarios_despachos.nextval,'|| V_TMP ||','|| V_TMP_2 ||',1,1,''PRODUCTO-1635'',sysdate)';
	DBMS_OUTPUT.PUT_LINE('[INFO] INSERTANDO esta query: ' || V_MSQL);
	EXECUTE IMMEDIATE V_MSQL;
	END IF;
	
	
COMMIT;

DBMS_OUTPUT.PUT_LINE('[FIN]');

EXCEPTION
     WHEN OTHERS THEN
          err_num := SQLCODE;
          err_msg := SQLERRM;

          DBMS_OUTPUT.PUT_LINE('KO no modificado');
          DBMS_OUTPUT.put_line('[ERROR] Se ha producido un error en la ejecuciÃ³n:'||TO_CHAR(err_num));
          DBMS_OUTPUT.put_line('-----------------------------------------------------------'); 
          DBMS_OUTPUT.put_line(err_msg);

          ROLLBACK;
          RAISE;          

END;

/

EXIT

