--/*
--##########################################
--## AUTOR=Enrique Badenes
--## FECHA_CREACION=20160609
--## ARTEFACTO=online
--## VERSION_ARTEFACTO=v9.2.6
--## INCIDENCIA_LINK= PRODUCTO-1366
--## PRODUCTO=NO
--##
--## Finalidad: Cambiar tipo de las columnas tpc_importe_desde y tpc_importe_hasta de number(16) a number(16,2)
--## INSTRUCCIONES: 
--## VERSIONES:
--##        0.1 Versión inicial
--##########################################
--*/
WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON; 
SET DEFINE OFF;
DECLARE
    V_MSQL VARCHAR2(32000 CHAR); -- Sentencia a ejecutar     
    V_ESQUEMA VARCHAR2(25 CHAR):= '#ESQUEMA#'; -- Configuracion Esquemas  
    V_ESQUEMA_M VARCHAR2(25 CHAR):= '#ESQUEMA_MASTER#'; -- Configuracion Esquemas  
    V_SQL VARCHAR2(4000 CHAR); -- Vble. para consulta que valida la existencia de una tabla.  
    V_NUM_TABLAS NUMBER(16); -- Vble. para validar la existencia de una tabla.     
    V_NUM_TABLAS2 NUMBER(16); -- Vble. para validar la existencia de una tabla.
    ERR_NUM NUMBER(25);  -- Vble. auxiliar para registrar errores en el script.
    ERR_MSG VARCHAR2(1024 CHAR); -- Vble. auxiliar para registrar errores en el script.
BEGIN	

	V_SQL := 'SELECT COUNT (1) FROM '||V_ESQUEMA||'.TUP_TPC_TURNADO_PROCU_CONFIG WHERE TPC_IMPORTE_DESDE IS NOT NULL';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	
	IF V_NUM_TABLAS > 0 THEN
      DBMS_OUTPUT.PUT_LINE('[INFO] CREANDO Y COMPIANDO VALORES A UNA COLUMNA TEMPORAL');
      V_MSQL := 'ALTER TABLE '||V_ESQUEMA||'.TUP_TPC_TURNADO_PROCU_CONFIG ADD TPC_IMPORTE_DESDE_TMP NUMBER(16,2)';
			EXECUTE IMMEDIATE V_MSQL;
      V_MSQL := 'UPDATE '||V_ESQUEMA||'.TUP_TPC_TURNADO_PROCU_CONFIG SET TPC_IMPORTE_DESDE_TMP = TPC_IMPORTE_DESDE';
			EXECUTE IMMEDIATE V_MSQL;
      DBMS_OUTPUT.PUT_LINE('[INFO] SETEAR VALORE COLUMNA ORIGINAL A NULL Y CAMBIANDO TIPO DE LA COLUMNA, VALORES RESTABLECIDOS NUEVAMENTE');
      V_MSQL := 'UPDATE '||V_ESQUEMA||'.TUP_TPC_TURNADO_PROCU_CONFIG SET TPC_IMPORTE_DESDE = NULL';
			EXECUTE IMMEDIATE V_MSQL;
			V_MSQL := 'ALTER TABLE '||V_ESQUEMA||'.TUP_TPC_TURNADO_PROCU_CONFIG MODIFY TPC_IMPORTE_DESDE NUMBER(16,2)';
			EXECUTE IMMEDIATE V_MSQL;
      V_MSQL := 'UPDATE '||V_ESQUEMA||'.TUP_TPC_TURNADO_PROCU_CONFIG SET TPC_IMPORTE_DESDE = TPC_IMPORTE_DESDE_TMP';
			EXECUTE IMMEDIATE V_MSQL;
      DBMS_OUTPUT.PUT_LINE('[INFO] BORRANDO COLUMNA TEMPORAL');
      V_MSQL := 'ALTER TABLE '||V_ESQUEMA||'.TUP_TPC_TURNADO_PROCU_CONFIG DROP COLUMN TPC_IMPORTE_DESDE_TMP';
			EXECUTE IMMEDIATE V_MSQL;
			DBMS_OUTPUT.PUT_LINE('[INFO] OK MODIFICACION');
  ELSE
		 	DBMS_OUTPUT.PUT_LINE('[INFO] NO EXISTEN DATOS EN LA COLUMNA');		
      DBMS_OUTPUT.PUT_LINE('[INFO] SE UPDATEA EL TIPO...');
      V_MSQL := 'ALTER TABLE '||V_ESQUEMA||'.TUP_TPC_TURNADO_PROCU_CONFIG MODIFY TPC_IMPORTE_DESDE NUMBER(16,2)';
			EXECUTE IMMEDIATE V_MSQL;
  END IF;
		
  V_SQL := 'SELECT COUNT (1) FROM '||V_ESQUEMA||'.TUP_TPC_TURNADO_PROCU_CONFIG WHERE TPC_IMPORTE_HASTA IS NOT NULL';
	EXECUTE IMMEDIATE V_SQL INTO V_NUM_TABLAS;
	
	IF V_NUM_TABLAS > 0 THEN
      DBMS_OUTPUT.PUT_LINE('[INFO] CREANDO Y COMPIANDO VALORES A UNA COLUMNA TEMPORAL');
      V_MSQL := 'ALTER TABLE '||V_ESQUEMA||'.TUP_TPC_TURNADO_PROCU_CONFIG ADD TPC_IMPORTE_HASTA_TMP NUMBER(16,2)';
			EXECUTE IMMEDIATE V_MSQL;
      V_MSQL := 'UPDATE '||V_ESQUEMA||'.TUP_TPC_TURNADO_PROCU_CONFIG SET TPC_IMPORTE_HASTA_TMP = TPC_IMPORTE_HASTA';
			EXECUTE IMMEDIATE V_MSQL;
      DBMS_OUTPUT.PUT_LINE('[INFO] SETEAR VALORE COLUMNA ORIGINAL A NULL Y CAMBIANDO TIPO DE LA COLUMNA, VALORES RESTABLECIDOS NUEVAMENTE');
      V_MSQL := 'UPDATE '||V_ESQUEMA||'.TUP_TPC_TURNADO_PROCU_CONFIG SET TPC_IMPORTE_HASTA = NULL';
			EXECUTE IMMEDIATE V_MSQL;
			V_MSQL := 'ALTER TABLE '||V_ESQUEMA||'.TUP_TPC_TURNADO_PROCU_CONFIG MODIFY TPC_IMPORTE_HASTA NUMBER(16,2)';
			EXECUTE IMMEDIATE V_MSQL;
      V_MSQL := 'UPDATE '||V_ESQUEMA||'.TUP_TPC_TURNADO_PROCU_CONFIG SET TPC_IMPORTE_HASTA = TPC_IMPORTE_HASTA_TMP';
			EXECUTE IMMEDIATE V_MSQL;
      DBMS_OUTPUT.PUT_LINE('[INFO] BORRANDO COLUMNA TEMPORAL');
      V_MSQL := 'ALTER TABLE '||V_ESQUEMA||'.TUP_TPC_TURNADO_PROCU_CONFIG DROP COLUMN TPC_IMPORTE_HASTA_TMP';
			EXECUTE IMMEDIATE V_MSQL;
			DBMS_OUTPUT.PUT_LINE('[INFO] OK MODIFICACION');
  ELSE
		 	DBMS_OUTPUT.PUT_LINE('[INFO] NO EXISTEN DATOS EN LA COLUMNA');		
      DBMS_OUTPUT.PUT_LINE('[INFO] SE UPDATEA EL TIPO...');
      V_MSQL := 'ALTER TABLE '||V_ESQUEMA||'.TUP_TPC_TURNADO_PROCU_CONFIG MODIFY TPC_IMPORTE_HASTA NUMBER(16,2)';
			EXECUTE IMMEDIATE V_MSQL;
  END IF;
  
	COMMIT;
	    
    
	DBMS_OUTPUT.PUT_LINE('[INFO] Fin.');


EXCEPTION
  WHEN OTHERS THEN
    ERR_NUM := SQLCODE;
    ERR_MSG := SQLERRM;
    DBMS_OUTPUT.put_line('[ERROR] Se ha producido un error en la ejecución:'||TO_CHAR(ERR_NUM));
    DBMS_OUTPUT.put_line('-----------------------------------------------------------'); 
    DBMS_OUTPUT.put_line(ERR_MSG);
    ROLLBACK;
    RAISE;   
    
END;
/

EXIT;
  	