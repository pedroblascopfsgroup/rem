--/*
--##########################################
--## AUTOR=SIDRO SOTOCA
--## FECHA_CREACION=20170912
--## ARTEFACTO=online
--## VERSION_ARTEFACTO=9.2
--## INCIDENCIA_LINK=HREOS-4478
--## PRODUCTO=NO
--## Finalidad:
--##           
--## VERSIONES:
--##        0.1 Versión inicial
--##########################################
--*/


WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON; 
SET DEFINE OFF;


DECLARE

    V_MSQL VARCHAR2(32000 CHAR); -- Sentencia a ejecutar    
    V_ESQUEMA VARCHAR2(25 CHAR):= '#ESQUEMA#'; -- Configuracion Esquema
    V_SQL VARCHAR2(4000 CHAR); -- Vble. para consulta que valida la existencia de una tabla.
    V_NUM_TABLAS NUMBER(16); -- Vble. para validar la existencia de una tabla.  
    V_NUM_SEQ NUMBER(16); -- Vble. para validar la existencia de una secuencia.  
    ERR_NUM NUMBER(25);  -- Vble. auxiliar para registrar errores en el script.
    ERR_MSG VARCHAR2(1024 CHAR); -- Vble. auxiliar para registrar errores en el script.

    V_TEXT_TABLA VARCHAR2(2400 CHAR) := 'ACT_PRY_PROYECTO'; -- Vble. auxiliar para almacenar el nombre de la tabla de ref.

BEGIN




V_MSQL := 'select count(1) from all_constraints where OWNER = '''||V_ESQUEMA||''' and table_name = '''||V_TEXT_TABLA||''' and constraint_name = ''PK_ACT_PRY_PROYECTO''';
		EXECUTE IMMEDIATE V_MSQL INTO V_NUM_TABLAS;	              

    IF V_NUM_TABLAS > 0 THEN
        V_MSQL := 'ALTER TABLE '||V_ESQUEMA||'.'||V_TEXT_TABLA||' DROP CONSTRAINT PK_ACT_PRY_PROYECTO ';
        EXECUTE IMMEDIATE V_MSQL;
        DBMS_OUTPUT.PUT_LINE('[INFO] ' ||V_ESQUEMA||'.PK_ACT_PRY_PROYECTO... UNIQUE KEY BORRADA.');
    ELSE
        DBMS_OUTPUT.PUT_LINE('[INFO] NO EXISTE LA RESTRICCION, NO SE PUEDE BORRAR AL NO EXISTIR');
    END IF;
    
    V_MSQL :=  'SELECT COUNT(1) FROM ALL_TAB_COLUMNS where OWNER = '''||V_ESQUEMA||''' and TABLE_NAME = '''||V_TEXT_TABLA||''' and COLUMN_NAME = ''PRY_ID''';
    		EXECUTE IMMEDIATE V_MSQL INTO V_NUM_TABLAS;	    
    IF V_NUM_TABLAS > 0 THEN
        V_MSQL := 'ALTER TABLE '||V_ESQUEMA||'.'||V_TEXT_TABLA||' DROP COLUMN PRY_ID ';
        EXECUTE IMMEDIATE V_MSQL;
        DBMS_OUTPUT.PUT_LINE('[INFO] Columna' ||V_TEXT_TABLA||'.PRY_ID BORRADA.');
    ELSE
        DBMS_OUTPUT.PUT_LINE('[INFO] NO EXISTE LA COLUMNA, NO SE PUEDE BORRAR AL NO EXISTIR');
    END IF;
    
    V_MSQL :=  'SELECT COUNT(1) FROM ALL_TAB_COLUMNS where OWNER = '''||V_ESQUEMA||''' and TABLE_NAME = '''||V_TEXT_TABLA||''' and COLUMN_NAME = ''VERSION''';
    		EXECUTE IMMEDIATE V_MSQL INTO V_NUM_TABLAS;	    
    IF V_NUM_TABLAS > 0 THEN
        V_MSQL := 'ALTER TABLE '||V_ESQUEMA||'.'||V_TEXT_TABLA||' DROP COLUMN VERSION ';
        EXECUTE IMMEDIATE V_MSQL;
        DBMS_OUTPUT.PUT_LINE('[INFO] Columna' ||V_TEXT_TABLA||'.VERSION BORRADA.');
    ELSE
        DBMS_OUTPUT.PUT_LINE('[INFO] NO EXISTE LA COLUMNA, NO SE PUEDE BORRAR AL NO EXISTIR');
    END IF;
    
    V_MSQL :=  'SELECT COUNT(1) FROM ALL_TAB_COLUMNS where OWNER = '''||V_ESQUEMA||''' and TABLE_NAME = '''||V_TEXT_TABLA||''' and COLUMN_NAME = ''USUARIOCREAR''';
    		EXECUTE IMMEDIATE V_MSQL INTO V_NUM_TABLAS;	    
    IF V_NUM_TABLAS > 0 THEN
        V_MSQL := 'ALTER TABLE '||V_ESQUEMA||'.'||V_TEXT_TABLA||' DROP COLUMN USUARIOCREAR ';
        EXECUTE IMMEDIATE V_MSQL;
        DBMS_OUTPUT.PUT_LINE('[INFO] Columna' ||V_TEXT_TABLA||'.USUARIOCREAR BORRADA.');
    ELSE
        DBMS_OUTPUT.PUT_LINE('[INFO] NO EXISTE LA COLUMNA, NO SE PUEDE BORRAR AL NO EXISTIR');
    END IF;
    
    V_MSQL :=  'SELECT COUNT(1) FROM ALL_TAB_COLUMNS where OWNER = '''||V_ESQUEMA||''' and TABLE_NAME = '''||V_TEXT_TABLA||''' and COLUMN_NAME = ''FECHACREAR''';
    		EXECUTE IMMEDIATE V_MSQL INTO V_NUM_TABLAS;	    
    IF V_NUM_TABLAS > 0 THEN
        V_MSQL := 'ALTER TABLE '||V_ESQUEMA||'.'||V_TEXT_TABLA||' DROP COLUMN FECHACREAR ';
        EXECUTE IMMEDIATE V_MSQL;
        DBMS_OUTPUT.PUT_LINE('[INFO] Columna' ||V_TEXT_TABLA||'.FECHACREAR BORRADA.');
    ELSE
        DBMS_OUTPUT.PUT_LINE('[INFO] NO EXISTE LA COLUMNA, NO SE PUEDE BORRAR AL NO EXISTIR');
    END IF;
    
    V_MSQL :=  'SELECT COUNT(1) FROM ALL_TAB_COLUMNS where OWNER = '''||V_ESQUEMA||''' and TABLE_NAME = '''||V_TEXT_TABLA||''' and COLUMN_NAME = ''USUARIOMODIFICAR''';
    		EXECUTE IMMEDIATE V_MSQL INTO V_NUM_TABLAS;	    
    IF V_NUM_TABLAS > 0 THEN
        V_MSQL := 'ALTER TABLE '||V_ESQUEMA||'.'||V_TEXT_TABLA||' DROP COLUMN USUARIOMODIFICAR ';
        EXECUTE IMMEDIATE V_MSQL;
        DBMS_OUTPUT.PUT_LINE('[INFO] Columna' ||V_TEXT_TABLA||'.USUARIOMODIFICAR BORRADA.');
    ELSE
        DBMS_OUTPUT.PUT_LINE('[INFO] NO EXISTE LA COLUMNA, NO SE PUEDE BORRAR AL NO EXISTIR');
    END IF;
    
    V_MSQL :=  'SELECT COUNT(1) FROM ALL_TAB_COLUMNS where OWNER = '''||V_ESQUEMA||''' and TABLE_NAME = '''||V_TEXT_TABLA||''' and COLUMN_NAME = ''FECHAMODIFICAR''';
    		EXECUTE IMMEDIATE V_MSQL INTO V_NUM_TABLAS;	    
    IF V_NUM_TABLAS > 0 THEN
        V_MSQL := 'ALTER TABLE '||V_ESQUEMA||'.'||V_TEXT_TABLA||' DROP COLUMN FECHAMODIFICAR ';
        EXECUTE IMMEDIATE V_MSQL;
        DBMS_OUTPUT.PUT_LINE('[INFO] Columna' ||V_TEXT_TABLA||'.FECHAMODIFICAR BORRADA.');
    ELSE
        DBMS_OUTPUT.PUT_LINE('[INFO] NO EXISTE LA COLUMNA, NO SE PUEDE BORRAR AL NO EXISTIR');
    END IF;
    
    V_MSQL :=  'SELECT COUNT(1) FROM ALL_TAB_COLUMNS where OWNER = '''||V_ESQUEMA||''' and TABLE_NAME = '''||V_TEXT_TABLA||''' and COLUMN_NAME = ''USUARIOBORRAR''';
    		EXECUTE IMMEDIATE V_MSQL INTO V_NUM_TABLAS;	    
    IF V_NUM_TABLAS > 0 THEN
        V_MSQL := 'ALTER TABLE '||V_ESQUEMA||'.'||V_TEXT_TABLA||' DROP COLUMN USUARIOBORRAR ';
        EXECUTE IMMEDIATE V_MSQL;
        DBMS_OUTPUT.PUT_LINE('[INFO] Columna' ||V_TEXT_TABLA||'.USUARIOBORRAR BORRADA.');
    ELSE
        DBMS_OUTPUT.PUT_LINE('[INFO] NO EXISTE LA COLUMNA, NO SE PUEDE BORRAR AL NO EXISTIR');
    END IF;
    
    V_MSQL :=  'SELECT COUNT(1) FROM ALL_TAB_COLUMNS where OWNER = '''||V_ESQUEMA||''' and TABLE_NAME = '''||V_TEXT_TABLA||''' and COLUMN_NAME = ''FECHABORRAR''';
    		EXECUTE IMMEDIATE V_MSQL INTO V_NUM_TABLAS;	    
    IF V_NUM_TABLAS > 0 THEN
        V_MSQL := 'ALTER TABLE '||V_ESQUEMA||'.'||V_TEXT_TABLA||' DROP COLUMN FECHABORRAR ';
        EXECUTE IMMEDIATE V_MSQL;
        DBMS_OUTPUT.PUT_LINE('[INFO] Columna' ||V_TEXT_TABLA||'.FECHABORRAR BORRADA.');
    ELSE
        DBMS_OUTPUT.PUT_LINE('[INFO] NO EXISTE LA COLUMNA, NO SE PUEDE BORRAR AL NO EXISTIR');
    END IF;
    
    V_MSQL :=  'SELECT COUNT(1) FROM ALL_TAB_COLUMNS where OWNER = '''||V_ESQUEMA||''' and TABLE_NAME = '''||V_TEXT_TABLA||''' and COLUMN_NAME = ''BORRADO''';
    		EXECUTE IMMEDIATE V_MSQL INTO V_NUM_TABLAS;	    
    IF V_NUM_TABLAS > 0 THEN
        V_MSQL := 'ALTER TABLE '||V_ESQUEMA||'.'||V_TEXT_TABLA||' DROP COLUMN BORRADO ';
        EXECUTE IMMEDIATE V_MSQL;
        DBMS_OUTPUT.PUT_LINE('[INFO] Columna' ||V_TEXT_TABLA||'.BORRADO BORRADA.');
    ELSE
        DBMS_OUTPUT.PUT_LINE('[INFO] NO EXISTE LA COLUMNA, NO SE PUEDE BORRAR AL NO EXISTIR');
    END IF;
        

EXCEPTION
     WHEN OTHERS THEN 
         DBMS_OUTPUT.PUT_LINE('[ERROR] ...KO!');
          err_num := SQLCODE;
          err_msg := SQLERRM;

          DBMS_OUTPUT.put_line('[ERROR] Se ha producido un error en la ejecución:'||TO_CHAR(err_num));
          DBMS_OUTPUT.put_line('-----------------------------------------------------------'); 
          DBMS_OUTPUT.put_line(err_msg);

          ROLLBACK;
          RAISE;          

END;
/

EXIT

