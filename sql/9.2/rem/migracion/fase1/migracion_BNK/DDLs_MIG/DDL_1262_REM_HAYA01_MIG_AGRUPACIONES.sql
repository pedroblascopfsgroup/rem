--/*
--#########################################
--## AUTOR=CLV
--## FECHA_CREACION=201600802
--## ARTEFACTO=batch
--## VERSION_ARTEFACTO=0.1
--## INCIDENCIA_LINK=HREOS-719
--## PRODUCTO=NO
--## 
--## Finalidad: Creación de tabla de migración 'MIG_AAG_AGRUPACIONES'
--##			
--## INSTRUCCIONES:  
--## VERSIONES:
--##        0.1 Versión inicial
--#########################################
--*/

--Para permitir la visualización de texto en un bloque PL/SQL utilizando DBMS_OUTPUT.PUT_LINE

WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON;
SET DEFINE OFF;


DECLARE

TABLE_COUNT NUMBER(1,0) := 0;
V_ESQUEMA_1 VARCHAR2(20 CHAR) := '#ESQUEMA#';
V_ESQUEMA_2 VARCHAR2(20 CHAR) := '#ESQUEMA#'; --SE CREA UNA SEGUNDA VARIABLE DE ESQUEMA POR SI EN ALGÚN MOMENTO QUEREMOS CREAR LA TABLA EN UN ESQUEMA DIFERENTE AL DEL USUARIO QUE LA ACCEDE O VICEVERSA
V_TABLESPACE_IDX VARCHAR2(30 CHAR) := '#TABLESPACE_INDEX#';
V_TABLA VARCHAR2(40 CHAR) := 'MIG_AAG_AGRUPACIONES_BNK';

BEGIN

SELECT COUNT(1) INTO TABLE_COUNT FROM ALL_TABLES WHERE TABLE_NAME = ''||V_TABLA||'' AND OWNER= ''||V_ESQUEMA_1||'';

IF TABLE_COUNT > 0 THEN

    DBMS_OUTPUT.PUT_LINE('[INFO] TABLA '||V_ESQUEMA_1||'.'||V_TABLA||' YA EXISTENTE. SE PROCEDE A BORRAR Y CREAR DE NUEVO.');

    EXECUTE IMMEDIATE 'DROP TABLE '||V_ESQUEMA_1||'.'||V_TABLA||'';
    
END IF;

EXECUTE IMMEDIATE '
CREATE TABLE '||V_ESQUEMA_1||'.'||V_TABLA||'
(
	AGR_UVEM										NUMBER(16,0)			NOT NULL,
	AGR_GESTOR										VARCHAR2(40 CHAR),
	AGR_MEDIADOR									VARCHAR2(17 CHAR),
	TIPO_AGRUPACION									VARCHAR2(20 CHAR),
	AGR_NOMBRE										VARCHAR2(250 CHAR),
	AGR_DESCRIPCION									VARCHAR2(250 CHAR),
	AGR_FECHA_ALTA									DATE,
	AGR_ELIMINADO									NUMBER(1,0),
	AGR_FECHA_BAJA									DATE,
	AGR_PUBLICADO									NUMBER(1,0),
	AGR_TEXTO_WEB									VARCHAR2(2500 CHAR),
	AGR_ACT_PRINCIPAL								NUMBER(16,0),
	LOC_AGRUP_OBRA_NUEVA							VARCHAR2(20 CHAR),
	PROV_AGRUP_OBRA_NUEVA							VARCHAR2(20 CHAR),
	ESTADO_AGRUP_OBRA_NUEVA							VARCHAR2(20 CHAR),
	ONV_DIR_AGRUP_OBRA_NUEVA						VARCHAR2(255 CHAR),
	ONV_CP_AGRUP_OBRA_NUEVA							NUMBER(8,0),
	ONV_ACR_PDV_AGRUP_OBRA_NUEVA					VARCHAR2(255 CHAR),
	LOC_AGRUP_LOTE_RESTRINGIDO						VARCHAR2(20 CHAR),
	PRO_AGRUP_LOTE_RESTRINGIDO						VARCHAR2(20 CHAR),
	RES_DIR_AGRUP_LOTE_RESTRINGIDO					VARCHAR2(255 CHAR),
	RES_CP_AGRUP_LOTE_RESTRINGIDO					NUMBER(8,0),
	RES_ACR_PDV_AGRUP_LOTE_RESTRIN					VARCHAR2(255 CHAR)
)'
;

DBMS_OUTPUT.PUT_LINE('[INFO] '||V_ESQUEMA_1||'.'||V_TABLA||' CREADA');  

IF V_ESQUEMA_2 != V_ESQUEMA_1 THEN

EXECUTE IMMEDIATE 'GRANT ALL ON "'||V_ESQUEMA_1||'"."'||V_TABLA||'" TO "'||V_ESQUEMA_2||'" WITH GRANT OPTION';

DBMS_OUTPUT.PUT_LINE('[INFO] PERMISOS SOBRE LA TABLA '||V_ESQUEMA_1||'.'||V_TABLA||' OTORGADOS A '||V_ESQUEMA_2||''); 

END IF;

EXCEPTION

    WHEN OTHERS THEN
        DBMS_OUTPUT.put_line('[ERROR] Se ha producido un error en la ejecucion:'||TO_CHAR(SQLCODE));
        DBMS_OUTPUT.put_line('-----------------------------------------------------------');
        DBMS_OUTPUT.put_line(SQLERRM);
        ROLLBACK;
        RAISE;
END;
/

EXIT;
