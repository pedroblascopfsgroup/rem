SELECT 
  (SELECT 'CLIENTES_COMERCIALES' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_CLC_CLIENTE_COMERCIAL) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM CLC_CLIENTE_COMERCIAL WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_CLC_CLIENTE_COMERCIAL') AS OBSERVACIONES
FROM DUAL
    
UNION ALL

SELECT 
  (SELECT 'VISITAS' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_VIS_VISITAS) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM VIS_VISITAS WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_VIS_VISITAS') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'OFERTAS' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_OFR_OFERTAS) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM OFR_OFERTAS WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_OFR_OFERTAS') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'ECO_EXPEDIENTE_COMERCIAL (OFERTAS ACEPTADAS)' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_OFR_OFERTAS WHERE SUBSTR(OFR_COD_ESTADO_OFERTA,0,2) = '01') AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM ECO_EXPEDIENTE_COMERCIAL WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_OFR_OFERTAS') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'OFERTAS_ACTIVO' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_OFA_OFERTAS_ACTIVO) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM ACT_OFR AO WHERE EXISTS (SELECT 1 FROM OFR_OFERTAS OFR WHERE OFR.OFR_ID = AO.OFR_ID AND USUARIOCREAR = 'MIG2')) AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_OFA_OFERTAS_ACTIVO') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'CONDICIONANTE_OFERTA_ACEPTADA' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_COE_CONDICIONAN_OFR_ACEP) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM COE_CONDICIONANTES_EXPEDIENTE WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_COE_CONDICIONAN_OFR_ACEP') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'RESERVAS' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_RES_RESERVAS) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM RES_RESERVAS WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_RES_RESERVAS') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'TITULARES_ADICIONALES_OFERTA' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_OFR_TIA_TITULARES_ADI) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM OFR_TIA_TITULARES_ADICIONALES WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_OFR_TIA_TITULARES_ADI') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'COMPRADORES' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_COM_COMPRADORES) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM COM_COMPRADOR WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_COM_COMPRADORES') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'COMPRADOR_EXPEDIENTE' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_CEX_COMPRADOR_EXPEDIENTE) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM CEX_COMPRADOR_EXPEDIENTE CEX WHERE EXISTS (SELECT 1 FROM COM_COMPRADOR COM WHERE COM.COM_ID = CEX.COM_ID AND USUARIOCREAR = 'MIG2')) AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_CEX_COMPRADOR_EXPEDIENTE') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'COMISIONES_GASTOS' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_GEX_GASTOS_EXPEDIENTE) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM GEX_GASTOS_EXPEDIENTE WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_GEX_GASTOS_EXPEDIENTE') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'OFERTAS_OBSERVACIONES' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_OBF_OBSERVACIONES_OFERTAS) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM OEX_OBS_EXPEDIENTE WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_OBF_OBSERVACIONES_OFERTAS') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'FORMALIZACIONES' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_FOR_FORMALIZACIONES) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM FOR_FORMALIZACION WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_FOR_FORMALIZACIONES') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'POSICIONAMIENTOS' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_POS_POSICIONAMIENTO) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM POS_POSICIONAMIENTO WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_POS_POSICIONAMIENTO') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'SUBSANACIONES' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_SUB_SUBSANACIONES) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM SUB_SUBSANACIONES WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_SUB_SUBSANACIONES') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'HIST_ESTADOS_PUBLICACIONES' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_ACT_HEP_HIST_EST_PUBLI) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM ACT_HEP_HIST_EST_PUBLICACION WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_ACT_HEP_HIST_EST_PUBLI') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'COND_ESPEC_ACTIVOS_PUBLICADOS' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_ACT_COE_CONDICIONES_ESPEC) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM ACT_COE_CONDICION_ESPECIFICA WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_ACT_COE_CONDICIONES_ESPEC') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'HIST_ACTIVOS_PRECIOS' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_ACT_HVA_HIST_VALORACIONES) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM ACT_HVA_HIST_VALORACIONES WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_ACT_HVA_HIST_VALORACIONES') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'PROPUESTAS_PRECIOS' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_PRP_PROPUESTAS_PRECIOS) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM PRP_PROPUESTAS_PRECIOS WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_PRP_PROPUESTAS_PRECIOS') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'ACTIVO_PROPUESTAS' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_ACT_PRP) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM ACT_PRP AP WHERE EXISTS (SELECT COUNT(1) FROM PRP_PROPUESTAS_PRECIOS PRP WHERE PRP.PRP_ID = AP.PRP_ID AND USUARIOCREAR = 'MIG2')) AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_ACT_PRP') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'PROVEEDORES (NUEVOS)' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_PVE_PROVEEDORES) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM ACT_PVE_PROVEEDOR WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_PVE_PROVEEDORES') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'PROVEEDORES (ACTUALIZADOS)' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_PVE_PROVEEDORES) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM ACT_PVE_PROVEEDOR WHERE USUARIOMODIFICAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_PVE_PROVEEDORES') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'PROVEEDORES_DIRECCIONES' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_PRD_PROVEEDOR_DIRECCION) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM ACT_PRD_PROVEEDOR_DIRECCION WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_PRD_PROVEEDOR_DIRECCION') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'PROVEEDOR_CONTACTO' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_PVC_PROVEEDOR_CONTACTO) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM ACT_PVC_PROVEEDOR_CONTACTO WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_PVC_PROVEEDOR_CONTACTO') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'GASTOS_PROVEEDORES' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_GPV_GASTOS_PROVEEDORES) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM GPV_GASTOS_PROVEEDOR WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_GPV_GASTOS_PROVEEDORES') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'GASTOS_PROVEEDORES_ACTIVOS_TRABAJOS (GPV_ACT)' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_GPV_ACT_TBJ) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM GPV_ACT GT WHERE EXISTS (SELECT 1 FROM GPV_GASTOS_PROVEEDOR GPV WHERE GPV.GPV_ID = GT.GPV_ID AND USUARIOCREAR = 'MIG2')) AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_GPV_TBJ') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'GASTOS_PROVEEDORES_ACTIVOS_TRABAJOS (GPV_TBJ)' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_GPV_ACT_TBJ) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM GPV_TBJ GT WHERE EXISTS (SELECT 1 FROM GPV_GASTOS_PROVEEDOR GPV WHERE GPV.GPV_ID = GT.GPV_ID AND USUARIOCREAR = 'MIG2')) AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_GPV_TBJ') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'DETALLE_ECONOMICO_GASTOS' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_GDE_GASTOS_DET_ECONOMICO) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM GDE_GASTOS_DETALLE_ECONOMICO WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_GDE_GASTOS_DET_ECONOMICO') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'GESTION_GASTOS' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_GGE_GASTOS_GESTION) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM GGE_GASTOS_GESTION WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_GGE_GASTOS_GESTION') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'GASTOS_IMPUGNACIONES' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_GIM_GASTOS_IMPUGNACION) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM GIM_GASTOS_IMPUGNACION WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_GIM_GASTOS_IMPUGNACION') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'GASTOS_INFORMACION_CONTABILIDAD' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_GIC_GASTOS_INFO_CONTABI) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM GIC_GASTOS_INFO_CONTABILIDAD WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_GIC_GASTOS_INFO_CONTABI') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'GASTOS_PROVISION' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_GPR_PROVISION_GASTOS) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM PRG_PROVISION_GASTOS WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_GPR_PROVISION_GASTOS') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'PERIMETRO_ACTIVOS' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_PAC_PERIMETRO_ACTIVO) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM ACT_PAC_PERIMETRO_ACTIVO WHERE USUARIOCREAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_PAC_PERIMETRO_ACTIVO') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'ACTIVOS (MERGE)' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_ACT_ACTIVO) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM ACT_ACTIVO WHERE USUARIOMODIFICAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_ACT_ACTIVO') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'ACTIVOS_ESTADOS' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_ACH_ACTIVOS_HITO) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT NULL FROM DUAL) AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_ACH_ACTIVOS_HITO') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'ACTIVOS_DATOS_ALQUILERES' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_ACQ_ACTIVO_ALQUILER) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1)  FROM ACT_HAL_HIST_ALQUILERES) AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_ACQ_ACTIVO_ALQUILER') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'AGRUPACIONES (MERGE)' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_AGR_AGRUPACIONES) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM ACT_AGR_AGRUPACION WHERE USUARIOMODIFICAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_AGR_AGRUPACIONES') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'PROPIETARIOS (MERGE)' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_PRO_PROPIETARIOS) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM ACT_PRO_PROPIETARIO WHERE USUARIOMODIFICAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_PRO_PROPIETARIOS') AS OBSERVACIONES
FROM DUAL

UNION ALL

SELECT 
  (SELECT 'PROPIETARIOS_HISTORICO (NO SE VUELCA A REM)' FROM DUAL) AS INTERFAZ,
  (SELECT COUNT(1) FROM MIG2_PRH_PROP_HIST) AS "VOLUMETRIA TABLAS MIG2",
  (SELECT COUNT(1) FROM ACT_PAC_PROPIETARIO_ACTIVO WHERE USUARIOMODIFICAR = 'MIG2') AS "VOLUMETRIA TABLAS REM",
  (SELECT OBSERVACIONES FROM MIG_INFO_TABLE WHERE TABLA_MIG = 'MIG2_PRH_PROP_HIST') AS OBSERVACIONES
FROM DUAL
;
