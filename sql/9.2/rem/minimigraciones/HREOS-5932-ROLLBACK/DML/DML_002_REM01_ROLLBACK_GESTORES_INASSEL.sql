--/*
--##########################################
--## AUTOR=Guillermo Llidó Parra
--## FECHA_CREACION=20190531
--## ARTEFACTO=online
--## VERSION_ARTEFACTO=9.2
--## INCIDENCIA_LINK=HREOS-5932
--## PRODUCTO=NO
--## Finalidad: Rollback Gestores.
--##           
--## INSTRUCCIONES: Configurar las variables necesarias en el principio del DECLARE
--## VERSIONES:
--##        0.1 Versión inicial
--##########################################
--*/

WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON; 

DECLARE
    err_num NUMBER; -- Numero de error.
    err_msg VARCHAR2(2048); -- Mensaje de error.
    V_ESQUEMA VARCHAR2(25 CHAR):= 'REM01'; -- Configuracion Esquemas.
    V_ESQUEMA_MASTER VARCHAR2(25 CHAR):= 'REMMASTER'; -- Configuracion Esquemas.
    V_USUARIOMODIFICAR VARCHAR(100 CHAR):= '';
    V_MSQL VARCHAR2(4000 CHAR);
    
	V_TABLA_BACKUP VARCHAR2(100 CHAR);
	V_ESQUEMA_BACKUP VARCHAR2(100 CHAR);
	
BEGIN

  DBMS_OUTPUT.put_line('[INICIO] Ejecutando [GESTORES]  ...........');


/**
 ROLLBACK GESTORES DE LOS ACTIVOS
**/

		DBMS_OUTPUT.put_line('	[INFO] Se realiza rollback sobre tabla GAC_GESTOR_ADD_ACTIVO ');
		
		-- OBTENEMOS LA TABLA BACKUP
		V_MSQL := 'SELECT REGISTROS_BACKUP FROM '||V_ESQUEMA||'.AUX_MIG_ALQUILERES_TAB_AFEC  WHERE TABLA = ''GAC_GESTOR_ADD_ACTIVO'' ';
		EXECUTE IMMEDIATE V_MSQL INTO V_TABLA_BACKUP;
		-- OBTENEMOS EL ESQUEMA TABLA BACKUP
		V_MSQL := 'SELECT TABLESPACE_NAME FROM SYS.ALL_TABLES WHERE TABLE_NAME = '''||V_TABLA_BACKUP||''' ';
		EXECUTE IMMEDIATE V_MSQL INTO V_ESQUEMA_BACKUP;
		
		-- BORRAMOS LOS GESTORES GAC
		
		V_MSQL := 'TRUNCATE TABLE '||V_ESQUEMA||'.GAC_GESTOR_ADD_ACTIVO';
							
		EXECUTE IMMEDIATE V_MSQL;
  		
  		DBMS_OUTPUT.PUT_LINE('	[INFO] Se han borado '||SQL%ROWCOUNT||' registros de la GAC_GESTOR_ADD_ACTIVO.'); 
  		
  		-- ACTUALIZAMOS CON EL BACKUP
		
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.GAC_GESTOR_ADD_ACTIVO VALUES (SELECT * FROM  '||V_ESQUEMA_BACKUP||'.'||V_TABLA_BACKUP||')';
				
  		EXECUTE IMMEDIATE V_MSQL;
  		
  		DBMS_OUTPUT.PUT_LINE('	[INFO] Se ha hecho rollback de  '||SQL%ROWCOUNT||' GAC_GESTOR_ADD_ACTIVO.'); 
		
		DBMS_OUTPUT.put_line('	[INFO] Se realiza rollback sobre tabla GEE_GESTOR_ENTIDAD ');
		
		-- OBTENEMOS LA TABLA BACKUP
		V_MSQL := 'SELECT REGISTROS_BACKUP FROM '||V_ESQUEMA||'.AUX_MIG_ALQUILERES_TAB_AFEC  WHERE TABLA = ''GEE_GESTOR_ENTIDAD'' ';
		EXECUTE IMMEDIATE V_MSQL INTO V_TABLA_BACKUP;
		-- OBTENEMOS EL ESQUEMA TABLA BACKUP
		V_MSQL := 'SELECT TABLESPACE_NAME FROM SYS.ALL_TABLES WHERE TABLE_NAME = '''||V_TABLA_BACKUP||''' ';
		EXECUTE IMMEDIATE V_MSQL INTO V_ESQUEMA_BACKUP;
		
		-- BORRAMOS LOS GESTORES GEE
		
		V_MSQL := 'TRUNCATE TABLE '||V_ESQUEMA||'.GEE_GESTOR_ENTIDAD';
							
		EXECUTE IMMEDIATE V_MSQL;
  		
  		DBMS_OUTPUT.PUT_LINE('	[INFO] Se han borado '||SQL%ROWCOUNT||' registros de la GEE_GESTOR_ENTIDAD.'); 
  		
  		-- ACTUALIZAMOS CON EL BACKUP
		
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.GEE_GESTOR_ENTIDAD VALUES (SELECT * FROM  '||V_ESQUEMA_BACKUP||'.'||V_TABLA_BACKUP||')';
				
  		EXECUTE IMMEDIATE V_MSQL;
  		
  		DBMS_OUTPUT.PUT_LINE('	[INFO] Se ha hecho rollback de  '||SQL%ROWCOUNT||' GEE_GESTOR_ENTIDAD.');
				
  		
  		
  		DBMS_OUTPUT.put_line('	[INFO] Se realiza rollback sobre tabla GAH_GESTOR_ACTIVO_HISTORICO ');
		
		-- OBTENEMOS LA TABLA BACKUP
		V_MSQL := 'SELECT REGISTROS_BACKUP FROM '||V_ESQUEMA||'.AUX_MIG_ALQUILERES_TAB_AFEC  WHERE TABLA = ''GAH_GESTOR_ACTIVO_HISTORICO'' ';
		EXECUTE IMMEDIATE V_MSQL INTO V_TABLA_BACKUP;
		-- OBTENEMOS EL ESQUEMA TABLA BACKUP
		V_MSQL := 'SELECT TABLESPACE_NAME FROM SYS.ALL_TABLES WHERE TABLE_NAME = '''||V_TABLA_BACKUP||''' ';
		EXECUTE IMMEDIATE V_MSQL INTO V_ESQUEMA_BACKUP;
		
		-- BORRAMOS LOS GESTORES GAH
		
		V_MSQL := 'TRUNCATE TABLE '||V_ESQUEMA||'.GAH_GESTOR_ACTIVO_HISTORICO';
							
		EXECUTE IMMEDIATE V_MSQL;
  		
  		DBMS_OUTPUT.PUT_LINE('	[INFO] Se han borado '||SQL%ROWCOUNT||' registros de la GAH_GESTOR_ACTIVO_HISTORICO.'); 
  		
  		-- ACTUALIZAMOS CON EL BACKUP
		
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.GAH_GESTOR_ACTIVO_HISTORICO VALUES (SELECT * FROM  '||V_ESQUEMA_BACKUP||'.'||V_TABLA_BACKUP||')';
				
  		EXECUTE IMMEDIATE V_MSQL;
  		
  		DBMS_OUTPUT.PUT_LINE('	[INFO] Se ha hecho rollback de  '||SQL%ROWCOUNT||' GAH_GESTOR_ACTIVO_HISTORICO.');
		
		-- OBTENEMOS LA TABLA BACKUP
		V_MSQL := 'SELECT REGISTROS_BACKUP FROM '||V_ESQUEMA||'.AUX_MIG_ALQUILERES_TAB_AFEC  WHERE TABLA = ''GEH_GESTOR_ENTIDAD_HIST'' ';
		EXECUTE IMMEDIATE V_MSQL INTO V_TABLA_BACKUP;
		-- OBTENEMOS EL ESQUEMA TABLA BACKUP
		V_MSQL := 'SELECT TABLESPACE_NAME FROM SYS.ALL_TABLES WHERE TABLE_NAME = '''||V_TABLA_BACKUP||''' ';
		EXECUTE IMMEDIATE V_MSQL INTO V_ESQUEMA_BACKUP;
		
		-- BORRAMOS LOS GESTORES GAH
		
		V_MSQL := 'TRUNCATE TABLE '||V_ESQUEMA||'.GEH_GESTOR_ENTIDAD_HIST';
							
		EXECUTE IMMEDIATE V_MSQL;
  		
  		DBMS_OUTPUT.PUT_LINE('	[INFO] Se han borado '||SQL%ROWCOUNT||' registros de la GEH_GESTOR_ENTIDAD_HIST.'); 
  		
  		-- ACTUALIZAMOS CON EL BACKUP
		
		V_MSQL := 'INSERT INTO '||V_ESQUEMA||'.GEH_GESTOR_ENTIDAD_HIST VALUES (SELECT * FROM  '||V_ESQUEMA_BACKUP||'.'||V_TABLA_BACKUP||')';
				
  		EXECUTE IMMEDIATE V_MSQL;
  		
  		DBMS_OUTPUT.PUT_LINE('	[INFO] Se ha hecho rollback de  '||SQL%ROWCOUNT||' GEH_GESTOR_ENTIDAD_HIST.');
  		
COMMIT;
  
 DBMS_OUTPUT.put_line('[FIN] Ejecutando [ROLLBACK GESTORES ]  ...........');

EXCEPTION
     WHEN OTHERS THEN
          ERR_NUM := SQLCODE;
          ERR_MSG := SQLERRM;
          DBMS_OUTPUT.put_line('[ERROR] Se ha producido un error en la ejecución:'||TO_CHAR(ERR_NUM));
          DBMS_OUTPUT.put_line('-----------------------------------------------------------'); 
          DBMS_OUTPUT.put_line(ERR_MSG);
          ROLLBACK;
          RAISE;   
END;
/
EXIT;
