--/*
--##########################################
--## AUTOR=Marco Munoz
--## FECHA_CREACION=20181003
--## ARTEFACTO=online
--## VERSION_ARTEFACTO=9.2
--## INCIDENCIA_LINK=REMVIP-2144
--## PRODUCTO=NO
--##
--## Finalidad: Carga masiva de fechas.
--##
--## INSTRUCCIONES:
--## VERSIONES:
--##        0.1 Versión inicial
--##########################################
--*/


WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON; 
SET DEFINE OFF;


DECLARE
    V_MSQL VARCHAR2(32000 CHAR); -- Sentencia a ejecutar     
    V_ESQUEMA VARCHAR2(25 CHAR):= 'REM01'; -- Configuracion Esquema
    V_ESQUEMA_M VARCHAR2(25 CHAR):= 'REMMASTER'; -- Configuracion Esquema Master
    V_SQL VARCHAR2(4000 CHAR); -- Vble. para consulta que valida la existencia de una tabla.
    V_NUM_TABLAS NUMBER(16); -- Vble. para validar la existencia de una tabla.   
    ERR_NUM NUMBER(25);  -- Vble. auxiliar para registrar errores en el script.
    ERR_MSG VARCHAR2(1024 CHAR); -- Vble. auxiliar para registrar errores en el script.
	
    V_TEXT1 VARCHAR2(2400 CHAR); -- Vble. auxiliar
    V_TEXT_TABLA VARCHAR2(2400 CHAR) := ''; -- Vble. auxiliar para almacenar el nombre de la tabla de ref.
    V_ENTIDAD_ID NUMBER(16);
    V_ID NUMBER(16);
    V_USU VARCHAR2(2400 CHAR) := 'REMVIP-2144';

    
BEGIN	
	
	DBMS_OUTPUT.PUT_LINE('[INICIO] ');
	
	EXECUTE IMMEDIATE 
    'MERGE INTO REM01.BIE_ADJ_ADJUDICACION        ADJ
	USING (
			SELECT ADJ.BIE_ADJ_ID,
				   TO_DATE(AUX.FECHA,''dd/mm/yyyy'') AS FECHA_ADJ
			FROM REM01.AUX_MMC_FECHAS_ACTIVOS_POST AUX
			JOIN REM01.ACT_ACTIVO                  ACT
			ON ACT.ACT_NUM_aCTIVO = AUX.ACTIVO
			JOIN REM01.BIE_BIEN                    BIE
			ON BIE.BIE_ID = ACT.BIE_ID
			JOIN REM01.BIE_ADJ_ADJUDICACION        ADJ
			ON ADJ.BIE_ID = BIE.BIE_ID
			WHERE AUX.TIPO = ''CALIFICACION_NEGATIVA''
	) T2
	ON (T2.BIE_ADJ_ID = ADJ.BIE_ADJ_ID)
	WHEN MATCHED THEN UPDATE 
	SET
		ADJ.BIE_ADJ_F_ENVIO_ADICION = T2.FECHA_ADJ,
		ADJ.USUARIOMODIFICAR = ''REMVIP-2144'',
		ADJ.FECHAMODIFICAR = SYSDATE
    ';   
    DBMS_OUTPUT.PUT_LINE('[INFO]: Updateamos '||SQL%ROWCOUNT||' registros.');
    
    EXECUTE IMMEDIATE 
    'MERGE INTO REM01.BIE_ADJ_ADJUDICACION        ADJ
	USING (
			SELECT ADJ.BIE_ADJ_ID,
				   TO_DATE(AUX.FECHA,''dd/mm/yyyy'') AS FECHA_ADJ
			FROM REM01.AUX_MMC_FECHAS_ACTIVOS_POST AUX
			JOIN REM01.ACT_ACTIVO                  ACT
			ON ACT.ACT_NUM_aCTIVO = AUX.ACTIVO
			JOIN REM01.BIE_BIEN                    BIE
			ON BIE.BIE_ID = ACT.BIE_ID
			JOIN REM01.BIE_ADJ_ADJUDICACION        ADJ
			ON ADJ.BIE_ID = BIE.BIE_ID
			WHERE AUX.TIPO = ''DE_TESTIMONIO''
	) T2
	ON (T2.BIE_ADJ_ID = ADJ.BIE_ADJ_ID)
	WHEN MATCHED THEN UPDATE 
	SET
		ADJ.BIE_ADJ_F_DECRETO_FIRME = T2.FECHA_ADJ,
		ADJ.USUARIOMODIFICAR = ''REMVIP-2144'',
		ADJ.FECHAMODIFICAR = SYSDATE
    ';   
    DBMS_OUTPUT.PUT_LINE('[INFO]: Updateamos '||SQL%ROWCOUNT||' registros.');
    
    EXECUTE IMMEDIATE 
    'MERGE INTO REM01.BIE_ADJ_ADJUDICACION        ADJ
	USING (
			SELECT ADJ.BIE_ADJ_ID,
				   TO_DATE(AUX.FECHA,''dd/mm/yyyy'') AS FECHA_ADJ
			FROM REM01.AUX_MMC_FECHAS_ACTIVOS_POST AUX
			JOIN REM01.ACT_ACTIVO                  ACT
			ON ACT.ACT_NUM_aCTIVO = AUX.ACTIVO
			JOIN REM01.BIE_BIEN                    BIE
			ON BIE.BIE_ID = ACT.BIE_ID
			JOIN REM01.BIE_ADJ_ADJUDICACION        ADJ
			ON ADJ.BIE_ID = BIE.BIE_ID
			WHERE AUX.TIPO = ''SEGUNDA_PRESENTACION''
	) T2
	ON (T2.BIE_ADJ_ID = ADJ.BIE_ADJ_ID)
	WHEN MATCHED THEN UPDATE 
	SET
		ADJ.BIE_ADJ_F_SEGUNDA_PRESEN = T2.FECHA_ADJ,
		ADJ.USUARIOMODIFICAR = ''REMVIP-2144'',
		ADJ.FECHAMODIFICAR = SYSDATE
    ';   
    DBMS_OUTPUT.PUT_LINE('[INFO]: Updateamos '||SQL%ROWCOUNT||' registros.');
    
    EXECUTE IMMEDIATE 
    'MERGE INTO REM01.BIE_ADJ_ADJUDICACION        ADJ
	USING (
			SELECT ADJ.BIE_ADJ_ID,
				   TO_DATE(AUX.FECHA,''dd/mm/yyyy'') AS FECHA_ADJ
			FROM REM01.AUX_MMC_FECHAS_ACTIVOS_POST AUX
			JOIN REM01.ACT_ACTIVO                  ACT
			ON ACT.ACT_NUM_aCTIVO = AUX.ACTIVO
			JOIN REM01.BIE_BIEN                    BIE
			ON BIE.BIE_ID = ACT.BIE_ID
			JOIN REM01.BIE_ADJ_ADJUDICACION        ADJ
			ON ADJ.BIE_ID = BIE.BIE_ID
			WHERE AUX.TIPO = ''PRESENTACION_REGISTRO''
	) T2
	ON (T2.BIE_ADJ_ID = ADJ.BIE_ADJ_ID)
	WHEN MATCHED THEN UPDATE 
	SET
		ADJ.BIE_ADJ_F_PRESENT_REGISTRO = T2.FECHA_ADJ,
		ADJ.USUARIOMODIFICAR = ''REMVIP-2144'',
		ADJ.FECHAMODIFICAR = SYSDATE
    ';   
    DBMS_OUTPUT.PUT_LINE('[INFO]: Updateamos '||SQL%ROWCOUNT||' registros.');
    
    EXECUTE IMMEDIATE 
    'MERGE INTO REM01.ACT_TIT_TITULO                    TIT
	USING (
			SELECT TIT.TIT_ID,
				   TO_DATE(AUX.FECHA,''dd/mm/yyyy'') AS FECHA_TIT
			FROM REM01.AUX_MMC_FECHAS_ACTIVOS_POST AUX
			JOIN REM01.ACT_ACTIVO                  ACT
			ON ACT.ACT_NUM_aCTIVO = AUX.ACTIVO
			JOIN REM01.ACT_TIT_TITULO              TIT
			ON TIT.ACT_ID = ACT.ACT_ID
			WHERE AUX.TIPO = ''RECEPCION_TESTIMONIO''
	) T2
	ON (T2.TIT_ID = TIT.TIT_ID)
	WHEN MATCHED THEN UPDATE 
	SET
		TIT.TIT_FECHA_ENTREGA_GESTORIA = T2.FECHA_TIT,
		TIT.USUARIOMODIFICAR = ''REMVIP-2144'',
		TIT.FECHAMODIFICAR = SYSDATE
    ';   
    DBMS_OUTPUT.PUT_LINE('[INFO]: Updateamos '||SQL%ROWCOUNT||' registros.');
    
    EXECUTE IMMEDIATE 
    'MERGE INTO REM01.ACT_TIT_TITULO                    TIT
	USING (
			SELECT TIT.TIT_ID,
				   TO_DATE(AUX.FECHA,''dd/mm/yyyy'') AS FECHA_TIT
			FROM REM01.AUX_MMC_FECHAS_ACTIVOS_POST AUX
			JOIN REM01.ACT_ACTIVO                  ACT
			ON ACT.ACT_NUM_aCTIVO = AUX.ACTIVO
			JOIN REM01.ACT_TIT_TITULO              TIT
			ON TIT.ACT_ID = ACT.ACT_ID
			WHERE AUX.TIPO = ''LIQUIDACION_IMPUESTO''
	) T2
	ON (T2.TIT_ID = TIT.TIT_ID)
	WHEN MATCHED THEN UPDATE 
	SET
		TIT.TIT_FECHA_PRESENT_HACIENDA = T2.FECHA_TIT,
		TIT.USUARIOMODIFICAR = ''REMVIP-2144'',
		TIT.FECHAMODIFICAR = SYSDATE
    ';   
    DBMS_OUTPUT.PUT_LINE('[INFO]: Updateamos '||SQL%ROWCOUNT||' registros.');
    
    COMMIT;
    DBMS_OUTPUT.PUT_LINE('[FIN]: TABLAS ACTUALIZADA CORRECTAMENTE ');


EXCEPTION
     WHEN OTHERS THEN
          err_num := SQLCODE;
          err_msg := SQLERRM;
          DBMS_OUTPUT.put_line('[ERROR] Se ha producido un error en la ejecución:'||TO_CHAR(err_num));
          DBMS_OUTPUT.put_line('-----------------------------------------------------------'); 
          DBMS_OUTPUT.put_line(err_msg);
          ROLLBACK;
          RAISE;          

END;
/
EXIT
