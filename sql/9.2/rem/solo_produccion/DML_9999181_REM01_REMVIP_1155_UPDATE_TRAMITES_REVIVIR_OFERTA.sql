--/*
--##########################################
--## AUTOR=VIOREL REMUS OVIDIU
--## FECHA_CREACION=20180813
--## ARTEFACTO=online
--## VERSION_ARTEFACTO=9.2
--## INCIDENCIA_LINK=REMVIP-1155
--## PRODUCTO=NO
--##
--## Finalidad: Revivir oferta para poder avanzarla, update estados y modificacion tramites 
--## INSTRUCCIONES:
--## VERSIONES:
--##        0.1 Versión inicial
--##########################################
--*/

WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON;
SET DEFINE OFF;


DECLARE
    V_SQL VARCHAR2(32000 CHAR); -- Sentencia a ejecutar         
    V_ESQUEMA VARCHAR2(25 CHAR):= '#ESQUEMA#'; -- Configuracion Esquema
    V_ESQUEMA_M VARCHAR2(25 CHAR):= '#ESQUEMA_MASTER#'; -- Configuracion Esquema Master
    --V_COUNT NUMBER(16); -- Vble. para contar.
    ERR_NUM NUMBER(25);  -- Vble. auxiliar para registrar errores en el script.
    ERR_MSG VARCHAR2(1024 CHAR); -- Vble. auxiliar para registrar errores en el script.
    V_TABLA VARCHAR2(27 CHAR):= 'ECO_EXPEDIENTE_COMERCIAL'; -- Vble. auxiliar para almacenar el nombre de la tabla de ref.
    PL_OUTPUT VARCHAR2 (32000);
    V_USUARIO VARCHAR2(32 CHAR):= 'REMVIP-1155';
    V_NUM_OFERTA NUMBER(16,0):= 73044101;
    V_NUM_EXPEDIENTE NUMBER(16,0):= 85405;
    V_COD_OFERTA VARCHAR2(20 CHAR):= '01';
    V_COD_EXPEDIENTE VARCHAR2(20 CHAR):= '06';
    V_TAR_ID_ELIMINAR NUMBER(16,0);
    V_TAR_ID_REVIVIR NUMBER(16,0);
    V_TEX_ID_REVIVIR NUMBER(16,0);
    V_TRA_ID_REVIVIR NUMBER(16,0);

 BEGIN

	DBMS_OUTPUT.PUT_LINE('INICIO');

	--SACAMNOS EL TAR_ID DEL TRAMITE QUE QUEREMOS ELIMINAR, EN ESTE CASO RESOLUCION EXPEDIENTE 

	V_SQL := 'SELECT TAR.TAR_ID 
        FROM '||V_ESQUEMA||'.ECO_EXPEDIENTE_COMERCIAL ECO
        JOIN '||V_ESQUEMA||'.OFR_OFERTAS OFR ON OFR.OFR_ID = ECO.OFR_ID
        JOIN '||V_ESQUEMA||'.DD_TOF_TIPOS_OFERTA TOF ON TOF.DD_TOF_ID = OFR.DD_TOF_ID AND TOF.BORRADO = 0
        JOIN '||V_ESQUEMA||'.ACT_OFR AO ON AO.OFR_ID = OFR.OFR_ID
        JOIN '||V_ESQUEMA||'.ACT_ACTIVO ACT ON ACT.ACT_ID = AO.ACT_ID AND ACT.BORRADO = 0
        LEFT JOIN '||V_ESQUEMA||'.ACT_TRA_TRAMITE TRA ON TRA.TBJ_ID = ECO.TBJ_ID
        LEFT JOIN '||V_ESQUEMA||'.TAC_TAREAS_ACTIVOS TAC ON TAC.TRA_ID = TRA.TRA_ID
        LEFT JOIN '||V_ESQUEMA||'.TAR_TAREAS_NOTIFICACIONES TAR ON TAR.TAR_ID = TAC.TAR_ID
        LEFT JOIN '||V_ESQUEMA||'.TEX_TAREA_EXTERNA TEX ON TEX.TAR_ID = TAR.TAR_ID
        LEFT JOIN '||V_ESQUEMA||'.TAP_TAREA_PROCEDIMIENTO TAP ON TAP.TAP_ID = TEX.TAP_ID
        LEFT JOIN '||V_ESQUEMA||'.ACT_TBJ_TRABAJO TBJ ON TBJ.TBJ_ID = ECO.TBJ_ID
        WHERE ECO.ECO_NUM_EXPEDIENTE = '||V_NUM_EXPEDIENTE||' AND TAR.TAR_TAREA LIKE ''%Resolución expediente%''';
	EXECUTE IMMEDIATE V_SQL INTO V_TAR_ID_ELIMINAR;
	DBMS_OUTPUT.PUT_LINE('Guardamos tar_id '||V_TAR_ID_ELIMINAR||'');

	--SACARMOS TAR_ID, TEX_ID, TRA_ID PARA EL TRAMITE QUE QUEREMOS REVIVIR, EN ESTE CASO POSICIONAMIENTO Y FIRMA

	--TAR
	V_SQL := 'SELECT TAR.TAR_ID 
        FROM '||V_ESQUEMA||'.ECO_EXPEDIENTE_COMERCIAL ECO
        JOIN '||V_ESQUEMA||'.OFR_OFERTAS OFR ON OFR.OFR_ID = ECO.OFR_ID
        JOIN '||V_ESQUEMA||'.DD_TOF_TIPOS_OFERTA TOF ON TOF.DD_TOF_ID = OFR.DD_TOF_ID AND TOF.BORRADO = 0
        JOIN '||V_ESQUEMA||'.ACT_OFR AO ON AO.OFR_ID = OFR.OFR_ID
        JOIN '||V_ESQUEMA||'.ACT_ACTIVO ACT ON ACT.ACT_ID = AO.ACT_ID AND ACT.BORRADO = 0
        LEFT JOIN '||V_ESQUEMA||'.ACT_TRA_TRAMITE TRA ON TRA.TBJ_ID = ECO.TBJ_ID
        LEFT JOIN '||V_ESQUEMA||'.TAC_TAREAS_ACTIVOS TAC ON TAC.TRA_ID = TRA.TRA_ID
        LEFT JOIN '||V_ESQUEMA||'.TAR_TAREAS_NOTIFICACIONES TAR ON TAR.TAR_ID = TAC.TAR_ID
        LEFT JOIN '||V_ESQUEMA||'.TEX_TAREA_EXTERNA TEX ON TEX.TAR_ID = TAR.TAR_ID
        LEFT JOIN '||V_ESQUEMA||'.TAP_TAREA_PROCEDIMIENTO TAP ON TAP.TAP_ID = TEX.TAP_ID
        LEFT JOIN '||V_ESQUEMA||'.ACT_TBJ_TRABAJO TBJ ON TBJ.TBJ_ID = ECO.TBJ_ID
        WHERE ECO.ECO_NUM_EXPEDIENTE = '||V_NUM_EXPEDIENTE||' AND TAR.TAR_TAREA LIKE ''%Posicionamiento y firma%''';
	EXECUTE IMMEDIATE V_SQL INTO V_TAR_ID_REVIVIR;
	DBMS_OUTPUT.PUT_LINE('Guardamos tar_id '||V_TAR_ID_REVIVIR||'');	

	--TEX
	V_SQL := 'SELECT TEX.TEX_ID 
        FROM '||V_ESQUEMA||'.ECO_EXPEDIENTE_COMERCIAL ECO
        JOIN '||V_ESQUEMA||'.OFR_OFERTAS OFR ON OFR.OFR_ID = ECO.OFR_ID
        JOIN '||V_ESQUEMA||'.DD_TOF_TIPOS_OFERTA TOF ON TOF.DD_TOF_ID = OFR.DD_TOF_ID AND TOF.BORRADO = 0
        JOIN '||V_ESQUEMA||'.ACT_OFR AO ON AO.OFR_ID = OFR.OFR_ID
        JOIN '||V_ESQUEMA||'.ACT_ACTIVO ACT ON ACT.ACT_ID = AO.ACT_ID AND ACT.BORRADO = 0
        LEFT JOIN '||V_ESQUEMA||'.ACT_TRA_TRAMITE TRA ON TRA.TBJ_ID = ECO.TBJ_ID
        LEFT JOIN '||V_ESQUEMA||'.TAC_TAREAS_ACTIVOS TAC ON TAC.TRA_ID = TRA.TRA_ID
        LEFT JOIN '||V_ESQUEMA||'.TAR_TAREAS_NOTIFICACIONES TAR ON TAR.TAR_ID = TAC.TAR_ID
        LEFT JOIN '||V_ESQUEMA||'.TEX_TAREA_EXTERNA TEX ON TEX.TAR_ID = TAR.TAR_ID
        LEFT JOIN '||V_ESQUEMA||'.TAP_TAREA_PROCEDIMIENTO TAP ON TAP.TAP_ID = TEX.TAP_ID
        LEFT JOIN '||V_ESQUEMA||'.ACT_TBJ_TRABAJO TBJ ON TBJ.TBJ_ID = ECO.TBJ_ID
        WHERE ECO.ECO_NUM_EXPEDIENTE = '||V_NUM_EXPEDIENTE||' AND TAR.TAR_TAREA LIKE ''%Posicionamiento y firma%''';
	EXECUTE IMMEDIATE V_SQL INTO V_TEX_ID_REVIVIR;
	DBMS_OUTPUT.PUT_LINE('Guardamos tar_id '||V_TEX_ID_REVIVIR||'');

	--TRA
	V_SQL := 'SELECT TRA.TRA_ID 
        FROM '||V_ESQUEMA||'.ECO_EXPEDIENTE_COMERCIAL ECO
        JOIN '||V_ESQUEMA||'.OFR_OFERTAS OFR ON OFR.OFR_ID = ECO.OFR_ID
        JOIN '||V_ESQUEMA||'.DD_TOF_TIPOS_OFERTA TOF ON TOF.DD_TOF_ID = OFR.DD_TOF_ID AND TOF.BORRADO = 0
        JOIN '||V_ESQUEMA||'.ACT_OFR AO ON AO.OFR_ID = OFR.OFR_ID
        JOIN '||V_ESQUEMA||'.ACT_ACTIVO ACT ON ACT.ACT_ID = AO.ACT_ID AND ACT.BORRADO = 0
        LEFT JOIN '||V_ESQUEMA||'.ACT_TRA_TRAMITE TRA ON TRA.TBJ_ID = ECO.TBJ_ID
        LEFT JOIN '||V_ESQUEMA||'.TAC_TAREAS_ACTIVOS TAC ON TAC.TRA_ID = TRA.TRA_ID
        LEFT JOIN '||V_ESQUEMA||'.TAR_TAREAS_NOTIFICACIONES TAR ON TAR.TAR_ID = TAC.TAR_ID
        LEFT JOIN '||V_ESQUEMA||'.TEX_TAREA_EXTERNA TEX ON TEX.TAR_ID = TAR.TAR_ID
        LEFT JOIN '||V_ESQUEMA||'.TAP_TAREA_PROCEDIMIENTO TAP ON TAP.TAP_ID = TEX.TAP_ID
        LEFT JOIN '||V_ESQUEMA||'.ACT_TBJ_TRABAJO TBJ ON TBJ.TBJ_ID = ECO.TBJ_ID
        WHERE ECO.ECO_NUM_EXPEDIENTE = '||V_NUM_EXPEDIENTE||' AND TAR.TAR_TAREA LIKE ''%Posicionamiento y firma%''';
	EXECUTE IMMEDIATE V_SQL INTO V_TRA_ID_REVIVIR;
	DBMS_OUTPUT.PUT_LINE('Guardamos tar_id '||V_TRA_ID_REVIVIR||'');

	--BORRAMOS LOS REGISTROS DE LA TAREA A ELIMINAR
	
	--TAC
	V_SQL := 'DELETE FROM '||V_ESQUEMA||'.TAC_TAREAS_ACTIVOS 
		  WHERE TAR_ID = '||V_TAR_ID_ELIMINAR||'';
	EXECUTE IMMEDIATE V_SQL;
	DBMS_OUTPUT.PUT_LINE('Se han borrado '||SQL%ROWCOUNT||' registro de TAC_TAREAS_ACTIVOS');

	--TEV
	V_SQL := 'DELETE FROM '||V_ESQUEMA||'.TEV_TAREA_EXTERNA_VALOR
		  WHERE TEX_ID IN (SELECT TEX_ID
		  FROM '||V_ESQUEMA||'.TEX_TAREA_EXTERNA
		  WHERE TAR_ID = '||V_TAR_ID_ELIMINAR||')';
	EXECUTE IMMEDIATE V_SQL;
	DBMS_OUTPUT.PUT_LINE('Se han borrado '||SQL%ROWCOUNT||' registro de TEV_TAREA_EXTERNA_VALOR');

	--TEX
	V_SQL := 'DELETE FROM '||V_ESQUEMA||'.TEX_TAREA_EXTERNA 
		  WHERE TAR_ID = '||V_TAR_ID_ELIMINAR||'';
	EXECUTE IMMEDIATE V_SQL;
	DBMS_OUTPUT.PUT_LINE('Se han borrado '||SQL%ROWCOUNT||' registro de TEX_TAREA_EXTERNA');

	--TAR
	V_SQL := 'DELETE FROM '||V_ESQUEMA||'.TAR_TAREAS_NOTIFICACIONES 
		  WHERE TAR_ID = '||V_TAR_ID_ELIMINAR||'';
	EXECUTE IMMEDIATE V_SQL;
	DBMS_OUTPUT.PUT_LINE('Se han borrado '||SQL%ROWCOUNT||' registro de TAR_TAREAS_NOTIFICACIONES');
	
	--ACTUALIZAMOS LOS REGISTROS DE LA TAREA A REVIVIR

	--TEX
	V_SQL := 'UPDATE '||V_ESQUEMA||'.TEX_TAREA_EXTERNA SET 
		  TEX_TOKEN_ID_BPM = NULL 
		  , BORRADO = 0 
		  , USUARIOMODIFICAR = '''||V_USUARIO||'''  
		  , FECHAMODIFICAR = SYSDATE   
		  WHERE TEX_ID = '||V_TEX_ID_REVIVIR||'';

	EXECUTE IMMEDIATE V_SQL;
	DBMS_OUTPUT.PUT_LINE('Se han actualizado '||SQL%ROWCOUNT||' registro en TEX_TAREA_EXTERNA');

	--TAR
	V_SQL := 'UPDATE '||V_ESQUEMA||'.TAR_TAREAS_NOTIFICACIONES SET 
		  TAR_FECHA_FIN = NULL 
		  , BORRADO = 0 
		  , USUARIOMODIFICAR = '''||V_USUARIO||''' 
		  , FECHAMODIFICAR = SYSDATE 
		  , TAR_TAREA_FINALIZADA = 0  
		  WHERE TAR_ID = '||V_TAR_ID_REVIVIR||'';

	EXECUTE IMMEDIATE V_SQL;
	DBMS_OUTPUT.PUT_LINE('Se han actualizado '||SQL%ROWCOUNT||' registro en TAR_TAREAS_NOTIFICACIONES');

	--TRA
        V_SQL := 'UPDATE '||V_ESQUEMA||'.ACT_TRA_TRAMITE SET 
		  TRA_PROCESS_BPM = NULL 
	          , USUARIOMODIFICAR = '''||V_USUARIO||''' 
		  , FECHAMODIFICAR = SYSDATE 
		  , DD_EPR_ID = (SELECT DD_EPR_ID  
				 FROM REMMASTER.DD_EPR_ESTADO_PROCEDIMIENTO 
				 WHERE DD_EPR_CODIGO = ''10'' 
				)  
		  WHERE TRA_ID = '||V_TRA_ID_REVIVIR||'';

	EXECUTE IMMEDIATE V_SQL;
	DBMS_OUTPUT.PUT_LINE('Se han actualizado '||SQL%ROWCOUNT||' registro en ACT_TRA_TRAMITE');

    
        --ACTUALIZO ESTADO EXPEDIENTE
	V_SQL := 'UPDATE '||V_ESQUEMA||'.ECO_EXPEDIENTE_COMERCIAL SET 
				 DD_EEC_ID = (SELECT DD_EEC_ID FROM '||V_ESQUEMA||'.DD_EEC_EST_EXP_COMERCIAL WHERE DD_EEC_CODIGO = '''||V_COD_EXPEDIENTE||''') 
				, ECO_FECHA_ANULACION = null 
				, USUARIOMODIFICAR  = '''||V_USUARIO||''' 
				, FECHAMODIFICAR    = SYSDATE 
				WHERE ECO_NUM_EXPEDIENTE  = '||V_NUM_EXPEDIENTE||' 
				';

	EXECUTE IMMEDIATE V_SQL;
	DBMS_OUTPUT.PUT_LINE('Se han actualizado '||SQL%ROWCOUNT||' registro en ECO_EXPEDIENTE_COMERCIAL');


      --ACTUALIZO ESTADO OFERTA
      V_SQL := 'UPDATE '||V_ESQUEMA||'.OFR_OFERTAS SET 
		DD_EOF_ID = (SELECT DD_EOF_ID FROM '||V_ESQUEMA||'.DD_EOF_ESTADOS_OFERTA WHERE DD_EOF_CODIGO = '''||V_COD_OFERTA||''') 
		, USUARIOMODIFICAR = '''||V_USUARIO||''' 
		, FECHAMODIFICAR = SYSDATE 
		WHERE OFR_NUM_OFERTA = '||V_NUM_OFERTA||' 
		';

      EXECUTE IMMEDIATE V_SQL;
		
      DBMS_OUTPUT.PUT_LINE('Se han actualizado '||SQL%ROWCOUNT||' registro en OFR_OFERTAS');

      --LANZO SP DE ALTA BPM 
      ALTA_BPM_INSTANCES('REMVIP-1155',PL_OUTPUT); 
      DBMS_OUTPUT.PUT_LINE(PL_OUTPUT); 

      COMMIT;

      DBMS_OUTPUT.PUT_LINE('FIN'); 

 
EXCEPTION
     WHEN OTHERS THEN
          ERR_NUM := SQLCODE;
          ERR_MSG := SQLERRM;
          DBMS_OUTPUT.PUT_LINE('[ERROR] Se ha producido un error en la ejecución:'||TO_CHAR(ERR_NUM));
          DBMS_OUTPUT.PUT_LINE('-----------------------------------------------------------'); 
          DBMS_OUTPUT.PUT_LINE(ERR_MSG);
          ROLLBACK;
          RAISE;   
END;
/
EXIT;
