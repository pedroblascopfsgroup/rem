--/*
--#########################################
--## AUTOR=Ivan Rubio
--## FECHA_CREACION=20200313
--## ARTEFACTO=batch
--## VERSION_ARTEFACTO=9.3
--## INCIDENCIA_LINK=HREOS-9803
--## PRODUCTO=NO
--## 
--## Finalidad: Creación de tabla 'AUX_GEE_HREOS_9803' , 'AUX_GAC_HREOS_9803' , 'AUX_GEH_HREOS_9803' , 'AUX_GAH_HREOS_9803'
--##			
--## INSTRUCCIONES:  
--## VERSIONES:
--##        0.1 Versión inicial
--#########################################
--*/

--Para permitir la visualización de texto en un bloque PL/SQL utilizando DBMS_OUTPUT.PUT_LINE

WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON;
SET DEFINE OFF;

DECLARE

V_SQL VARCHAR2(32000 CHAR); -- Sentencia a ejecutar
V_ESQUEMA VARCHAR2(25 CHAR):= '#ESQUEMA#'; -- Configuracion Esquema
V_ESQUEMA_M VARCHAR2(25 CHAR):= '#ESQUEMA_MASTER#'; -- Configuracion Esquema Master

V_TABLA1 VARCHAR2(40 CHAR) := 'AUX_GEE_HREOS_9803';
V_TABLA2 VARCHAR2(40 CHAR) := 'AUX_GAC_HREOS_9803';
V_TABLA3 VARCHAR2(40 CHAR) := 'AUX_GEH_HREOS_9803';
V_TABLA4 VARCHAR2(40 CHAR) := 'AUX_GAH_HREOS_9803';

BEGIN

    DBMS_OUTPUT.PUT_LINE('[INICIO]');

	V_SQL :=  'DELETE FROM '||V_ESQUEMA||'.GAH_GESTOR_ACTIVO_HISTORICO GAH
                WHERE GAH.GEH_ID IN(
                SELECT GEH_ID FROM '||V_ESQUEMA||'.AUX_GAH_HREOS_9803)';
                
	EXECUTE IMMEDIATE V_SQL;
	DBMS_OUTPUT.PUT_LINE('[INFO] ELIMINADOS '||SQL%ROWCOUNT||' REGISTROS DE GAH_GESTOR_ACTIVO_HISTORICO'); 
    
    V_SQL :=  'DELETE FROM '||V_ESQUEMA||'.GAC_GESTOR_ADD_ACTIVO GAC
                WHERE GAC.GEE_ID IN(
                SELECT GEE_ID FROM '||V_ESQUEMA||'.AUX_GAC_HREOS_9803)';
                
	EXECUTE IMMEDIATE V_SQL;
	DBMS_OUTPUT.PUT_LINE('[INFO] ELIMINADOS '||SQL%ROWCOUNT||' REGISTROS DE GAC_GESTOR_ADD_ACTIVO'); 
    
    V_SQL :=  'DELETE FROM '||V_ESQUEMA||'.GEH_GESTOR_ENTIDAD_HIST GEH 
                WHERE GEH.GEH_ID IN(
                SELECT GEH_ID FROM '||V_ESQUEMA||'.AUX_GEH_HREOS_9803)';
                
	EXECUTE IMMEDIATE V_SQL;
	DBMS_OUTPUT.PUT_LINE('[INFO] ELIMINADOS '||SQL%ROWCOUNT||' REGISTROS DE GEH_GESTOR_ENTIDAD_HIST'); 
    
    V_SQL :=  'DELETE FROM '||V_ESQUEMA||'.GEE_GESTOR_ENTIDAD GEE 
                WHERE GEE.GEE_ID IN(
                SELECT GEE_ID FROM '||V_ESQUEMA||'.AUX_GEE_HREOS_9803)';
                
	EXECUTE IMMEDIATE V_SQL;
	DBMS_OUTPUT.PUT_LINE('[INFO] ELIMINADOS '||SQL%ROWCOUNT||' REGISTROS DE GEE_GESTOR_ENTIDAD'); 
    
    
    COMMIT;

    DBMS_OUTPUT.PUT_LINE('[FIN BORRADO GESTORES]');

    DBMS_OUTPUT.PUT_LINE('[FIN]');

EXCEPTION

    WHEN OTHERS THEN
        DBMS_OUTPUT.put_line('[ERROR] Se ha producido un error en la ejecucion:'||TO_CHAR(SQLCODE));
        DBMS_OUTPUT.put_line('-----------------------------------------------------------');
        DBMS_OUTPUT.put_line(SQLERRM);
        ROLLBACK;
        RAISE;
END;
/

EXIT;

