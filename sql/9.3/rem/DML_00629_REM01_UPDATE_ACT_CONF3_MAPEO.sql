--/*
--#########################################
--## AUTOR=Daniel Algaba
--## FECHA_CREACION=20210508
--## ARTEFACTO=online
--## VERSION_ARTEFACTO=9.3
--## INCIDENCIA_LINK=HREOS-13784
--## PRODUCTO=NO
--## 
--## Finalidad: 
--##            
--## INSTRUCCIONES:  
--## VERSIONES:
--##        0.1 Versión inicial
--#########################################
--*/
--Para permitir la visualización de texto en un bloque PL/SQL utilizando DBMS_OUTPUT.PUT_LINE
WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON;
SET DEFINE OFF;

DECLARE
  V_SQL VARCHAR2(32000 CHAR); -- Sentencia a ejecutar         
  V_ESQUEMA VARCHAR2(25 CHAR):= '#ESQUEMA#'; -- Configuracion Esquema
  V_ESQUEMA_M VARCHAR2(25 CHAR):= '#ESQUEMA_MASTER#'; -- Configuracion Esquema Master
  V_TABLA VARCHAR2(50 CHAR):= 'ACT_CONF3_MAPEO';
  V_COUNT NUMBER(16); -- Vble. para contar.
  V_COUNT_UPDATE NUMBER(16):= 0; -- Vble. para contar updates
  ERR_NUM NUMBER(25);  -- Vble. auxiliar para registrar errores en el script.
  ERR_MSG VARCHAR2(1024 CHAR); -- Vble. auxiliar para registrar errores en el script.
  V_USUARIO VARCHAR2(32 CHAR):= 'HREOS-13784';
  ACT_NUM_ACTIVO_UVEM NUMBER(32);
  DD_SCR_DESCRIPCION VARCHAR2(72 CHAR);
  ACT_NUM_ACTIVO_OLD NUMBER(32);
  ACT_NUM_ACTIVO_NEW NUMBER(32);


BEGIN

  V_SQL := 'UPDATE '||V_ESQUEMA||'.'||V_TABLA||' SET 
          USUARIOMODIFICAR = '''||V_USUARIO||'''
        , FECHAMODIFICAR = SYSDATE
        , AC3_TRANSFORMACION = ''LEFT JOIN (SELECT CASE WHEN AUX.VALOR_NUEVO = 1 THEN 1 WHEN AUX.VALOR_NUEVO = 0 THEN 0 END PAC_INCLUIDO, AUX.ACT_NUM_ACTIVO FROM '||V_ESQUEMA||'.ESPARTA_EXCEL1 AUX WHERE AUX.CAMPO = ''''178'''') CRUCE ON CRUCE.ACT_NUM_ACTIVO = ACT.ACT_NUM_ACTIVO''
        WHERE DD_CCS_ID = (SELECT CCS.DD_CCS_ID FROM '||V_ESQUEMA||'.DD_CCS_CAMPOS_CONV_SAREB CCS 
        JOIN '||V_ESQUEMA||'.DD_COS_CAMPOS_ORIGEN_CONV_SAREB COS ON COS.DD_COS_ID = CCS.DD_COS_ID AND COS.BORRADO = 0
        WHERE DD_CCS_CAMPO = ''PAC_INCLUIDO'' AND CCS.BORRADO = 0 AND COS.DD_COS_CODIGO = ''178'')
        ';
  EXECUTE IMMEDIATE V_SQL;
  
  V_SQL := 'UPDATE '||V_ESQUEMA||'.'||V_TABLA||' SET 
          USUARIOMODIFICAR = '''||V_USUARIO||'''
        , FECHAMODIFICAR = SYSDATE
        , AC3_TRANSFORMACION = ''LEFT JOIN (SELECT CASE WHEN AUX.VALOR_NUEVO = 1 THEN 1 WHEN AUX.VALOR_NUEVO = 0 THEN 0 END PAC_CHECK_GESTIONAR, AUX.ACT_NUM_ACTIVO FROM '||V_ESQUEMA||'.ESPARTA_EXCEL1 AUX WHERE AUX.CAMPO = ''''179'''') CRUCE ON CRUCE.ACT_NUM_ACTIVO = ACT.ACT_NUM_ACTIVO''
        WHERE DD_CCS_ID = (SELECT CCS.DD_CCS_ID FROM '||V_ESQUEMA||'.DD_CCS_CAMPOS_CONV_SAREB CCS 
        JOIN '||V_ESQUEMA||'.DD_COS_CAMPOS_ORIGEN_CONV_SAREB COS ON COS.DD_COS_ID = CCS.DD_COS_ID AND COS.BORRADO = 0
        WHERE DD_CCS_CAMPO = ''PAC_CHECK_GESTIONAR'' AND CCS.BORRADO = 0 AND COS.DD_COS_CODIGO = ''179'')
        ';
  EXECUTE IMMEDIATE V_SQL;

  V_SQL := 'UPDATE '||V_ESQUEMA||'.'||V_TABLA||' SET 
          USUARIOMODIFICAR = '''||V_USUARIO||'''
        , FECHAMODIFICAR = SYSDATE
        , AC3_TRANSFORMACION = ''LEFT JOIN (SELECT CASE WHEN AUX.VALOR_NUEVO = 1 THEN 1 WHEN AUX.VALOR_NUEVO = 0 THEN 0 END PAC_CHECK_PUBLICAR, AUX.ACT_NUM_ACTIVO FROM '||V_ESQUEMA||'.ESPARTA_EXCEL1 AUX WHERE AUX.CAMPO = ''''181'''') CRUCE ON CRUCE.ACT_NUM_ACTIVO = ACT.ACT_NUM_ACTIVO''
        WHERE DD_CCS_ID = (SELECT CCS.DD_CCS_ID FROM '||V_ESQUEMA||'.DD_CCS_CAMPOS_CONV_SAREB CCS 
        JOIN '||V_ESQUEMA||'.DD_COS_CAMPOS_ORIGEN_CONV_SAREB COS ON COS.DD_COS_ID = CCS.DD_COS_ID AND COS.BORRADO = 0
        WHERE DD_CCS_CAMPO = ''PAC_CHECK_PUBLICAR'' AND CCS.BORRADO = 0 AND COS.DD_COS_CODIGO = ''181'')
        ';
  EXECUTE IMMEDIATE V_SQL;

  V_SQL := 'UPDATE '||V_ESQUEMA||'.'||V_TABLA||' SET 
          USUARIOMODIFICAR = '''||V_USUARIO||'''
        , FECHAMODIFICAR = SYSDATE
        , AC3_TRANSFORMACION = ''LEFT JOIN (SELECT CASE WHEN AUX.VALOR_NUEVO = 1 THEN 1 WHEN AUX.VALOR_NUEVO = 0 THEN 0 END PAC_CHECK_COMERCIALIZAR, AUX.ACT_NUM_ACTIVO FROM '||V_ESQUEMA||'.ESPARTA_EXCEL1 AUX WHERE AUX.CAMPO = ''''183'''') CRUCE ON CRUCE.ACT_NUM_ACTIVO = ACT.ACT_NUM_ACTIVO''
        WHERE DD_CCS_ID = (SELECT CCS.DD_CCS_ID FROM '||V_ESQUEMA||'.DD_CCS_CAMPOS_CONV_SAREB CCS 
        JOIN '||V_ESQUEMA||'.DD_COS_CAMPOS_ORIGEN_CONV_SAREB COS ON COS.DD_COS_ID = CCS.DD_COS_ID AND COS.BORRADO = 0
        WHERE DD_CCS_CAMPO = ''PAC_CHECK_COMERCIALIZAR'' AND CCS.BORRADO = 0 AND COS.DD_COS_CODIGO = ''183'')
        ';
  EXECUTE IMMEDIATE V_SQL;

  V_SQL := 'UPDATE '||V_ESQUEMA||'.'||V_TABLA||' SET 
          USUARIOMODIFICAR = '''||V_USUARIO||'''
        , FECHAMODIFICAR = SYSDATE
        , AC3_TRANSFORMACION = ''LEFT JOIN (SELECT CASE WHEN AUX.VALOR_NUEVO = 1 THEN 1 WHEN AUX.VALOR_NUEVO = 0 THEN 0 END PAC_CHECK_FORMALIZAR, AUX.ACT_NUM_ACTIVO FROM '||V_ESQUEMA||'.ESPARTA_EXCEL1 AUX WHERE AUX.CAMPO = ''''185'''') CRUCE ON CRUCE.ACT_NUM_ACTIVO = ACT.ACT_NUM_ACTIVO''
        WHERE DD_CCS_ID = (SELECT CCS.DD_CCS_ID FROM '||V_ESQUEMA||'.DD_CCS_CAMPOS_CONV_SAREB CCS 
        JOIN '||V_ESQUEMA||'.DD_COS_CAMPOS_ORIGEN_CONV_SAREB COS ON COS.DD_COS_ID = CCS.DD_COS_ID AND COS.BORRADO = 0
        WHERE DD_CCS_CAMPO = ''PAC_CHECK_FORMALIZAR'' AND CCS.BORRADO = 0 AND COS.DD_COS_CODIGO = ''185'')
        ';
  EXECUTE IMMEDIATE V_SQL;

  V_SQL := 'UPDATE '||V_ESQUEMA||'.'||V_TABLA||' SET 
          USUARIOMODIFICAR = '''||V_USUARIO||'''
        , FECHAMODIFICAR = SYSDATE
        , AC3_TRANSFORMACION = ''LEFT JOIN '||V_ESQUEMA||'.DD_MPT_MTV_IN_EX_PERIMETRO MPT ON MPT.DD_MPT_CODIGO = TMP.VALOR_NUEVO AND MPT.BORRADO = 0''
        WHERE DD_CCS_ID = (SELECT CCS.DD_CCS_ID FROM '||V_ESQUEMA||'.DD_CCS_CAMPOS_CONV_SAREB CCS 
        JOIN '||V_ESQUEMA||'.DD_COS_CAMPOS_ORIGEN_CONV_SAREB COS ON COS.DD_COS_ID = CCS.DD_COS_ID AND COS.BORRADO = 0
        WHERE DD_CCS_CAMPO = ''PAC_MOTIVO_GESTIONAR'' AND CCS.BORRADO = 0 AND COS.DD_COS_CODIGO = ''180'')
        ';
  EXECUTE IMMEDIATE V_SQL;

  V_SQL := 'UPDATE '||V_ESQUEMA||'.'||V_TABLA||' SET 
          USUARIOMODIFICAR = '''||V_USUARIO||'''
        , FECHAMODIFICAR = SYSDATE
        , AC3_TRANSFORMACION = ''LEFT JOIN '||V_ESQUEMA||'.DD_MPT_MTV_IN_EX_PERIMETRO MPT ON MPT.DD_MPT_CODIGO = TMP.VALOR_NUEVO AND MPT.BORRADO = 0''
        WHERE DD_CCS_ID = (SELECT CCS.DD_CCS_ID FROM '||V_ESQUEMA||'.DD_CCS_CAMPOS_CONV_SAREB CCS 
        JOIN '||V_ESQUEMA||'.DD_COS_CAMPOS_ORIGEN_CONV_SAREB COS ON COS.DD_COS_ID = CCS.DD_COS_ID AND COS.BORRADO = 0
        WHERE DD_CCS_CAMPO = ''PAC_MOTIVO_PUBLICAR'' AND CCS.BORRADO = 0 AND COS.DD_COS_CODIGO = ''182'')
        ';
  EXECUTE IMMEDIATE V_SQL;

  V_SQL := 'UPDATE '||V_ESQUEMA||'.'||V_TABLA||' SET 
          USUARIOMODIFICAR = '''||V_USUARIO||'''
        , FECHAMODIFICAR = SYSDATE
        , AC3_TRANSFORMACION = ''LEFT JOIN '||V_ESQUEMA||'.DD_MPT_MTV_IN_EX_PERIMETRO MPT ON MPT.DD_MPT_CODIGO = TMP.VALOR_NUEVO AND MPT.BORRADO = 0''
        WHERE DD_CCS_ID = (SELECT CCS.DD_CCS_ID FROM '||V_ESQUEMA||'.DD_CCS_CAMPOS_CONV_SAREB CCS 
        JOIN '||V_ESQUEMA||'.DD_COS_CAMPOS_ORIGEN_CONV_SAREB COS ON COS.DD_COS_ID = CCS.DD_COS_ID AND COS.BORRADO = 0
        WHERE DD_CCS_CAMPO = ''PAC_MOTIVO_FORMALIZAR'' AND CCS.BORRADO = 0 AND COS.DD_COS_CODIGO = ''186'')
        ';
  EXECUTE IMMEDIATE V_SQL;

    V_SQL := 'UPDATE '||V_ESQUEMA||'.'||V_TABLA||' SET 
          USUARIOMODIFICAR = '''||V_USUARIO||'''
        , FECHAMODIFICAR = SYSDATE
        , AC3_TRANSFORMACION = ''LEFT JOIN '||V_ESQUEMA||'.DD_MPT_MTV_IN_EX_PERIMETRO MPT ON MPT.DD_MPT_CODIGO = TMP.VALOR_NUEVO AND MPT.BORRADO = 0 JOIN (SELECT CHCK.ACT_NUM_ACTIVO FROM '||V_ESQUEMA||'.ESPARTA_EXCEL1 CHCK WHERE CHCK.CAMPO = ''''183'''' AND CHCK.VALOR_NUEVO = 0) AUX ON AUX.ACT_NUM_ACTIVO = TMP.ACT_NUM_ACTIVO''
        WHERE DD_CCS_ID = (SELECT CCS.DD_CCS_ID FROM '||V_ESQUEMA||'.DD_CCS_CAMPOS_CONV_SAREB CCS 
        JOIN '||V_ESQUEMA||'.DD_COS_CAMPOS_ORIGEN_CONV_SAREB COS ON COS.DD_COS_ID = CCS.DD_COS_ID AND COS.BORRADO = 0
        WHERE DD_CCS_CAMPO = ''PAC_MOT_EXCL_COMERCIALIZAR'' AND CCS.BORRADO = 0 AND COS.DD_COS_CODIGO = ''184'')
        ';
  EXECUTE IMMEDIATE V_SQL;

      V_SQL := 'UPDATE '||V_ESQUEMA||'.'||V_TABLA||' SET 
          USUARIOMODIFICAR = '''||V_USUARIO||'''
        , FECHAMODIFICAR = SYSDATE
        , AC3_TRANSFORMACION = ''LEFT JOIN '||V_ESQUEMA||'.DD_MPT_MTV_IN_EX_PERIMETRO MPT ON MPT.DD_MPT_CODIGO = TMP.VALOR_NUEVO AND MPT.BORRADO = 0 LEFT JOIN '||V_ESQUEMA||'.DD_MCO_MOTIVO_COMERCIALIZACION CRUCE ON MPT.DD_MPT_ID = CRUCE.DD_MCO_ID AND CRUCE.BORRADO = 0 JOIN (SELECT CHCK.ACT_NUM_ACTIVO FROM '||V_ESQUEMA||'.ESPARTA_EXCEL1 CHCK WHERE CHCK.CAMPO = ''''183'''' AND CHCK.VALOR_NUEVO = 1) AUX ON AUX.ACT_NUM_ACTIVO = TMP.ACT_NUM_ACTIVO''
        WHERE DD_CCS_ID = (SELECT CCS.DD_CCS_ID FROM '||V_ESQUEMA||'.DD_CCS_CAMPOS_CONV_SAREB CCS 
        JOIN '||V_ESQUEMA||'.DD_COS_CAMPOS_ORIGEN_CONV_SAREB COS ON COS.DD_COS_ID = CCS.DD_COS_ID AND COS.BORRADO = 0
        WHERE DD_CCS_CAMPO = ''DD_MCO_ID'' AND CCS.BORRADO = 0 AND COS.DD_COS_CODIGO = ''184'')
        ';
  EXECUTE IMMEDIATE V_SQL;
    
  COMMIT;    

EXCEPTION
    WHEN OTHERS THEN
      DBMS_OUTPUT.PUT_LINE('[ERROR] Se ha producido un error en la ejecución:'||TO_CHAR(SQLCODE));
      DBMS_OUTPUT.PUT_LINE('-----------------------------------------------------------');
      DBMS_OUTPUT.PUT_LINE(SQLERRM);
      ROLLBACK;
      RAISE;
END;
/
EXIT;
