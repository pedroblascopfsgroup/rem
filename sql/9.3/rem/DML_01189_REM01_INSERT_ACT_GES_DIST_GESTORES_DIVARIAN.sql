 --/*
--##########################################
--## AUTOR=IVAN REPISO
--## FECHA_CREACION=20221129
--## ARTEFACTO=online
--## VERSION_ARTEFACTO=9.3
--## INCIDENCIA_LINK=REMVIP-12798
--## PRODUCTO=NO
--##
--## Finalidad:	Añade en la tabla ACT_GES_DIST_GESTORES los datos del array T_ARRAY_DATA
--## INSTRUCCIONES:
--## VERSIONES:
--##########################################
--*/

WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON; 
SET DEFINE OFF;

DECLARE
    V_MSQL VARCHAR2(32000 CHAR); -- Sentencia a ejecutar     
    V_ESQUEMA VARCHAR2(25 CHAR):= '#ESQUEMA#'; -- Configuracion Esquema
    V_ESQUEMA_M VARCHAR2(25 CHAR):= '#ESQUEMA_MASTER#'; -- Configuracion Esquema Master
    V_NUM_TABLAS NUMBER(16); -- Vble. para validar la existencia de una tabla.
    ERR_NUM NUMBER(25);  -- Vble. auxiliar para registrar errores en el script.
    ERR_MSG VARCHAR2(1024 CHAR); -- Vble. auxiliar para registrar errores en el script.
	
    V_TEXT_TABLA VARCHAR2(2400 CHAR) := 'ACT_GES_DIST_GESTORES'; -- Vble. auxiliar para almacenar el nombre de la tabla de ref.
    V_USU VARCHAR2(2400 CHAR) := 'REMVIP-12798';
    
BEGIN	
	
	DBMS_OUTPUT.PUT_LINE('[INICIO] ');
	
    -- LOOP para insertar o modificar los valores en ACT_GES_DIST_GESTORES -----------------------------------------------------------------
    DBMS_OUTPUT.PUT_LINE('[INFO]: INSERCION EN '||V_ESQUEMA||'.'||V_TEXT_TABLA);
      DBMS_OUTPUT.PUT_LINE('[INFO]: BORRAMOS GIAFORM PARA DIVARIAN PARA IGUALAR CON JAGUAR');

    V_MSQL := 'UPDATE '|| V_ESQUEMA ||'.'||V_TEXT_TABLA||' SET BORRADO = 1,
                 USUARIOBORRAR = '''||V_USU||''', FECHABORRAR = SYSDATE
                 WHERE COD_SUBCARTERA IN (''151'', ''152'') AND TIPO_GESTOR IN (''GIAADMT'', ''GIAFORM'', ''GTOPLUS'') AND BORRADO = 0';
    EXECUTE IMMEDIATE V_MSQL;
    DBMS_OUTPUT.PUT_LINE('[INFO]: '|| SQL%ROWCOUNT ||' REGISTROS BORRADOS CORRECTAMENTE PARA DIVARIAN');

    DBMS_OUTPUT.PUT_LINE('[INFO]: INSERTAMOS PARA SUBCARTERA DIVARIAN 151');
    V_MSQL := 'INSERT INTO '|| V_ESQUEMA ||'.'||V_TEXT_TABLA||' (ID,TIPO_GESTOR,COD_CARTERA,COD_PROVINCIA,USERNAME,
               NOMBRE_USUARIO,USUARIOCREAR,FECHACREAR,COD_SUBCARTERA)
               SELECT '|| V_ESQUEMA ||'.S_'||V_TEXT_TABLA||'.NEXTVAL,
               TIPO_GESTOR,COD_CARTERA,COD_PROVINCIA,USERNAME,NOMBRE_USUARIO , '''||V_USU||''', SYSDATE, ''151''
               FROM '|| V_ESQUEMA ||'.'||V_TEXT_TABLA||'
               WHERE COD_SUBCARTERA = ''70'' AND TIPO_GESTOR IN (''GIAADMT'', ''GIAFORM'', ''GTOPLUS'') AND BORRADO = 0';
    EXECUTE IMMEDIATE V_MSQL;
    DBMS_OUTPUT.PUT_LINE('[INFO]: '|| SQL%ROWCOUNT ||' REGISTROS INSERTADOS CORRECTAMENTE PARA DIVARIAN 151');

    DBMS_OUTPUT.PUT_LINE('[INFO]: INSERTAMOS PARA SUBCARTERA DIVARIAN 152');
    V_MSQL := 'INSERT INTO '|| V_ESQUEMA ||'.'||V_TEXT_TABLA||' (ID,TIPO_GESTOR,COD_CARTERA,COD_PROVINCIA,USERNAME,
               NOMBRE_USUARIO,USUARIOCREAR,FECHACREAR,COD_SUBCARTERA)
               SELECT '|| V_ESQUEMA ||'.S_'||V_TEXT_TABLA||'.NEXTVAL,
               TIPO_GESTOR,COD_CARTERA,COD_PROVINCIA,USERNAME,NOMBRE_USUARIO , '''||V_USU||''', SYSDATE, ''152''
               FROM '|| V_ESQUEMA ||'.'||V_TEXT_TABLA||'
               WHERE COD_SUBCARTERA = ''70'' AND TIPO_GESTOR IN (''GIAADMT'', ''GIAFORM'', ''GTOPLUS'') AND BORRADO = 0';
    EXECUTE IMMEDIATE V_MSQL;
    DBMS_OUTPUT.PUT_LINE('[INFO]: '|| SQL%ROWCOUNT ||' REGISTROS INSERTADOS CORRECTAMENTE PARA DIVARIAN 152');

    DBMS_OUTPUT.PUT_LINE('[INFO]: INSERTAMOS PARA SUBCARTERA APPLE 138');
    V_MSQL := 'INSERT INTO '|| V_ESQUEMA ||'.'||V_TEXT_TABLA||' (ID,TIPO_GESTOR,COD_CARTERA,COD_PROVINCIA,USERNAME,
               NOMBRE_USUARIO,USUARIOCREAR,FECHACREAR,COD_SUBCARTERA)
               SELECT '|| V_ESQUEMA ||'.S_'||V_TEXT_TABLA||'.NEXTVAL,
               TIPO_GESTOR,COD_CARTERA,COD_PROVINCIA,USERNAME,NOMBRE_USUARIO , '''||V_USU||''', SYSDATE, ''138''
               FROM '|| V_ESQUEMA ||'.'||V_TEXT_TABLA||'
               WHERE COD_SUBCARTERA = ''70'' AND TIPO_GESTOR IN (''GIAADMT'', ''GTOPLUS'') AND BORRADO = 0';
    EXECUTE IMMEDIATE V_MSQL;
    DBMS_OUTPUT.PUT_LINE('[INFO]: '|| SQL%ROWCOUNT ||' REGISTROS INSERTADOS CORRECTAMENTE PARA APPLE 138');


    COMMIT;
    DBMS_OUTPUT.PUT_LINE('[FIN]: TABLA '||V_TEXT_TABLA||' ACTUALIZADA CORRECTAMENTE ');


EXCEPTION
     WHEN OTHERS THEN
          err_num := SQLCODE;
          err_msg := SQLERRM;

          DBMS_OUTPUT.put_line('[ERROR] Se ha producido un error en la ejecución:'||TO_CHAR(err_num));
          DBMS_OUTPUT.put_line('-----------------------------------------------------------'); 
          DBMS_OUTPUT.put_line(err_msg);

          ROLLBACK;
          RAISE;          

END;
/

EXIT