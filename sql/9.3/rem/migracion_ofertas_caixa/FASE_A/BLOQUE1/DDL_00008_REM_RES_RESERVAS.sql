
--/*
--#########################################
--## AUTOR=PIER GOTTA
--## FECHA_CREACION=20210727
--## ARTEFACTO=batch
--## VERSION_ARTEFACTO=0.1
--## INCIDENCIA_LINK=HREOS-14680
--## PRODUCTO=NO
--##
--## Finalidad: Creación de tabla de migración
--##
--## INSTRUCCIONES:
--## VERSIONES:
--##        0.1 Versión inicial
--#########################################
--*/

--Para permitir la visualización de texto en un bloque PL/SQL utilizando DBMS_OUTPUT.PUT_LINE

WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON;
SET DEFINE OFF;


DECLARE

TABLE_COUNT NUMBER(1,0) := 0;
V_ESQUEMA_1 VARCHAR2(20 CHAR) := 'REM01';
V_ESQUEMA_2 VARCHAR2(20 CHAR) := 'REM01'; --SE CREA UNA SEGUNDA VARIABLE DE ESQUEMA POR SI EN ALGÚN MOMENTO QUEREMOS CREAR LA TABLA EN UN ESQUEMA DIFERENTE AL DEL USUARIO QUE LA ACCEDE O VICEVERSA
V_TABLA VARCHAR2(40 CHAR) := 'MIG2_RES_RESERVAS_CAIXA';

BEGIN

SELECT COUNT(1) INTO TABLE_COUNT FROM ALL_TABLES WHERE TABLE_NAME = ''||V_TABLA||'' AND OWNER= ''||V_ESQUEMA_1||'';

IF TABLE_COUNT > 0 THEN

    DBMS_OUTPUT.PUT_LINE('[INFO] TABLA '||V_ESQUEMA_1||'.'||V_TABLA||' YA EXISTENTE. SE PROCEDE A BORRAR Y CREAR DE NUEVO.');
    EXECUTE IMMEDIATE 'DROP TABLE '||V_ESQUEMA_1||'.'||V_TABLA||'';
    
END IF;

EXECUTE IMMEDIATE '
CREATE TABLE '||V_ESQUEMA_1||'.'||V_TABLA||'
(
	ECO_ID	NUMBER(16,0),
	RES_NUM_RESERVA	NUMBER(16,0),
	RES_FECHA_ENVIO	DATE,
	RES_FECHA_FIRMA	DATE,
	RES_FECHA_VENCIMIENTO	DATE,
	RES_FECHA_ANULACION	DATE,
	RES_MOTIVO_ANULACION	VARCHAR2(256 CHAR),
	DD_TAR_ID	NUMBER(16,0),
	DD_ERE_ID	NUMBER(16,0),
	RES_FECHA_SOLICITUD	DATE,
	RES_FECHA_RESOLUCION	DATE,
	RES_IND_IMP_ANULACION	NUMBER(1,0),
	RES_IMPORTE_DEVUELTO	NUMBER(16,2),
	DD_EDE_ID	NUMBER(16,0),
	DD_MAN_ID	NUMBER(16,0),
	DD_MAR_ID	NUMBER(16,0),
	DD_DER_ID	NUMBER(16,0),
	RES_FECHA_CONTABILIZACION	DATE,
	RES_FECHA_VIGENCIA_ARRAS	DATE,
	RES_FECHA_AMPLIACION_ARRAS	DATE,
	RES_SOLICITUD_AMPLIACION_ARRAS	VARCHAR2(300 CHAR),
	DD_MAA_ID	NUMBER(16,0),
	RES_FECHA_CONT_ARRAS	NUMBER(16,0)
)'
;

DBMS_OUTPUT.PUT_LINE('[INFO] '||V_ESQUEMA_1||'.'||V_TABLA||' CREADA');  

IF V_ESQUEMA_2 != V_ESQUEMA_1 THEN

	EXECUTE IMMEDIATE 'GRANT ALL ON "'||V_ESQUEMA_1||'"."'||V_TABLA||'" TO "'||V_ESQUEMA_2||'" WITH GRANT OPTION';
	DBMS_OUTPUT.PUT_LINE('[INFO] PERMISOS SOBRE LA TABLA '||V_ESQUEMA_1||'.'||V_TABLA||' OTORGADOS A '||V_ESQUEMA_2||''); 

END IF;


EXCEPTION

    WHEN OTHERS THEN
        DBMS_OUTPUT.put_line('[ERROR] Se ha producido un error en la ejecucion:'||TO_CHAR(SQLCODE));
        DBMS_OUTPUT.put_line('-----------------------------------------------------------');
        DBMS_OUTPUT.put_line(SQLERRM);
        ROLLBACK;
        RAISE;
END;
/

EXIT;
