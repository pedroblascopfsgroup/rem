OPTIONS (DIRECT=TRUE, BINDSIZE=512000, ROWS=10000, ERRORS=999999)
LOAD DATA
CHARACTERSET AL32UTF8
INTO TABLE REM01.AUX_REMVIP_12689
TRUNCATE
FIELDS TERMINATED BY '|'
TRAILING NULLCOLS
(
	   OFERTA			NULLIF(OFERTA=BLANKS)				"TRIM(:OFERTA)"
	  ,FECHA_CANCEL		NULLIF(FECHA_CANCEL=BLANKS)			"TRIM(:FECHA_CANCEL)"
)

