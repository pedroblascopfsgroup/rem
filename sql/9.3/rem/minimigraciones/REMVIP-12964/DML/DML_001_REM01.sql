--/*
--#########################################
--## AUTOR=IVAN REPISO
--## FECHA_CREACION=20220720
--## ARTEFACTO=online
--## VERSION_ARTEFACTO=9.3
--## INCIDENCIA_LINK=REMVIP-12964
--## PRODUCTO=NO
--## 
--## Finalidad: Script que MODIFICA en TAS_ID_EXTERNO.
--## INSTRUCCIONES:  
--## VERSIONES:
--##        0.1 Versión inicial
--#########################################
--*/

WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON;
SET DEFINE OFF;

DECLARE
	
	V_TABLA_TMP VARCHAR2(50 CHAR) := 'AUX_REMVIP_12964'; -- Variable para tabla 	
	V_ESQUEMA VARCHAR2(25 CHAR):= 'REM01';-- '#ESQUEMA#'; -- Configuracion Esquema
	V_ESQUEMA_M VARCHAR2(25 CHAR):= 'REMMASTER';-- '#ESQUEMA_MASTER#'; -- Configuracion Esquema Master
	ERR_NUM NUMBER;-- Numero de errores
	ERR_MSG VARCHAR2(2048);-- Mensaje de error
	V_MSQL VARCHAR2(4000 CHAR);--Variable para consultar la existencia de registros en la tabla
	V_NUM NUMBER(16); --Variable para comprobar si existen registros
	PL_OUTPUT VARCHAR2(32000 CHAR);
	V_USUARIO VARCHAR2(50 CHAR) := 'REMVIP-12964';
	V_NUM_ACTIVO VARCHAR2(32000 CHAR);
	

BEGIN

	DBMS_OUTPUT.PUT_LINE('[INICIO]'||CHR(10));		

	-- Comprobar que existen los activos
    V_MSQL := 'SELECT COUNT (1) FROM '||V_ESQUEMA||'.'||V_TABLA_TMP||' WHERE TAS_ID IN (SELECT TAS_ID FROM '||V_ESQUEMA||'.ACT_TAS_TASACION WHERE BORRADO = 0)';
	EXECUTE IMMEDIATE V_MSQL INTO V_NUM;
	
	IF V_NUM > 0 THEN
		DBMS_OUTPUT.PUT_LINE('[INFO] Se han encontrado '||V_NUM||' registros en la tabla de activos');	

		DBMS_OUTPUT.PUT_LINE('[INFO] INFORMAMOS TAS_ID_EXTERNO_OLD PARA ALMACENAR INFO ANTERIOR');
		
		V_MSQL := 'MERGE INTO '||V_ESQUEMA||'.'||V_TABLA_TMP||' T1 USING 
						(SELECT DISTINCT TAS.TAS_ID, TAS.TAS_ID_EXTERNO FROM '||V_ESQUEMA||'.ACT_TAS_TASACION TAS
						JOIN '||V_ESQUEMA||'.'||V_TABLA_TMP||' TMP ON TMP.TAS_ID = TAS.TAS_ID
						WHERE TAS.BORRADO = 0) T2
					ON (T1.TAS_ID = T2.TAS_ID)
					WHEN MATCHED THEN UPDATE SET
					T1.TAS_ID_EXTERNO_OLD = T2.TAS_ID_EXTERNO';
		EXECUTE IMMEDIATE V_MSQL;	

		DBMS_OUTPUT.PUT_LINE('[INFO] Se han ACTUALIZADO '||SQL%ROWCOUNT||' registros: '||V_TABLA_TMP||'');
	 
	COMMIT;

	DBMS_OUTPUT.PUT_LINE('[INFO] VOLCAMOS TAS_ID_EXTERNO DE AUXILIAR EN TABLA FINAL');
		
	V_MSQL := 'MERGE INTO '||V_ESQUEMA||'.ACT_TAS_TASACION T1 USING
					(SELECT DISTINCT TAS.TAS_ID, AUX.TAS_ID_EXTERNO FROM '||V_ESQUEMA||'.ACT_TAS_TASACION TAS
                        JOIN '||V_ESQUEMA||'.'||V_TABLA_TMP||' TMP ON TMP.TAS_ID = TAS.TAS_ID
                        WHERE TAS.BORRADO = 0) T2
				ON (T1.TAS_ID = T2.TAS_ID)
				WHEN MATCHED THEN UPDATE SET
				T1.TAS_ID_EXTERNO = T2.TAS_ID_EXTERNO,
				T1.USUARIOMODIFICAR = '''||V_USUARIO||''',
				T1.FECHAMODIFICAR = SYSDATE';
	EXECUTE IMMEDIATE V_MSQL;	

	DBMS_OUTPUT.PUT_LINE('[INFO] Se han ACTUALIZADO '||SQL%ROWCOUNT||' registros: ACT_TAS_TASACION');

	END IF;
	 
	COMMIT;	


	DBMS_OUTPUT.PUT_LINE('[FIN]');

EXCEPTION
    WHEN OTHERS THEN
      PL_OUTPUT := PL_OUTPUT ||'[ERROR] Se ha producido un error en la ejecución:'||TO_CHAR(SQLCODE)||CHR(10);
      PL_OUTPUT := PL_OUTPUT ||'-----------------------------------------------------------'||CHR(10);
      PL_OUTPUT := PL_OUTPUT ||SQLERRM||CHR(10);
      PL_OUTPUT := PL_OUTPUT ||V_MSQL||CHR(10);
      DBMS_OUTPUT.PUT_LINE(PL_OUTPUT);
      ROLLBACK;
      RAISE;
END;
/
EXIT;
