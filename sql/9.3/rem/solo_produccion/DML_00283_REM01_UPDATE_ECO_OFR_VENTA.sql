--/*
--######################################### 
--## AUTOR=Viorel Remus Ovidiu
--## FECHA_CREACION=20200502
--## ARTEFACTO=online
--## VERSION_ARTEFACTO=9.3
--## INCIDENCIA_LINK=REMVIP-7209
--## PRODUCTO=NO
--## 
--## Finalidad:
--##            
--## INSTRUCCIONES:  
--## VERSIONES:
--##        0.1 Versión inicial
--#########################################
--*/

--Para permitir la visualización de texto en un bloque PL/SQL utilizando DBMS_OUTPUT.PUT_LINE

WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON; 
SET DEFINE OFF;
alter session set NLS_NUMERIC_CHARACTERS = '.,';

DECLARE
	V_MSQL VARCHAR2(32000 CHAR); -- Sentencia a ejecutar    
	V_ESQUEMA VARCHAR2(25 CHAR):= '#ESQUEMA#'; -- #ESQUEMA# Configuracion Esquema
	V_ESQUEMA_M VARCHAR2(25 CHAR):= '#ESQUEMA_MASTER#'; -- #ESQUEMA_MASTER# Configuracion Esquema Master 
	ERR_NUM NUMBER(25);  -- Vble. auxiliar para registrar errores en el script.
	ERR_MSG VARCHAR2(1024 CHAR); -- Vble. auxiliar para registrar errores en el script.
	V_USUARIO VARCHAR2(50 CHAR) := 'REMVIP-7209';
	
BEGIN
    DBMS_OUTPUT.PUT_LINE('[INICIO]');
	
--quitar marca venta externa, quitar venta externa, revivir operacion, meterle fecha venta y poner expediente en vendido

--oferta 6002036


	DBMS_OUTPUT.PUT_LINE('[INICIO] QUITAR VENTA EXTERNA ACTIVO');

	V_MSQL := 'UPDATE '||V_ESQUEMA||'.ACT_ACTIVO
				SET ACT_VENTA_EXTERNA_FECHA = NULL
					,ACT_VENTA_EXTERNA_IMPORTE = NULL
					,USUARIOMODIFICAR = '''||V_USUARIO||'''
					,FECHAMODIFICAR = SYSDATE
				WHERE ACT_NUM_ACTIVO =  7288694 ';
	EXECUTE IMMEDIATE V_MSQL;

	DBMS_OUTPUT.PUT_LINE('[INICIO] PONER OFERTA EN TRAMITADA');

	V_MSQL := 'UPDATE REM01.OFR_OFERTAS SET DD_EOF_ID = 1,
		USUARIOMODIFICAR = '''||V_USUARIO||''',
		FECHAMODIFICAR = SYSDATE 
		WHERE OFR_NUM_OFERTA = 6002036';

	EXECUTE IMMEDIATE V_MSQL;

	DBMS_OUTPUT.PUT_LINE('[INFO] METER FECHA VENTA EXPEDIENTE Y PONERLO EN VENDIDO');

	V_MSQL := 'UPDATE REM01.ECO_EXPEDIENTE_COMERCIAL 
		SET ECO_FECHA_VENTA = TO_DATE (''15/05/2019'',''DD/MM/YYYY''), 
		ECO_FECHA_CONT_PROPIETARIO = TO_DATE (''15/05/2019'',''DD/MM/YYYY''), 
		DD_EEC_ID = 8,
		USUARIOMODIFICAR = '''||V_USUARIO||''',
		FECHAMODIFICAR = SYSDATE 
		WHERE ECO_NUM_EXPEDIENTE = 202405';

	EXECUTE IMMEDIATE V_MSQL;

	DBMS_OUTPUT.PUT_LINE('[INFO] Se han actualizado  '||SQL%ROWCOUNT||' registros .');


--oferta 6010353


	DBMS_OUTPUT.PUT_LINE('[INICIO] QUITAR VENTA EXTERNA ACTIVO');

	V_MSQL := 'UPDATE '||V_ESQUEMA||'.ACT_ACTIVO
				SET ACT_VENTA_EXTERNA_FECHA = NULL
					,ACT_VENTA_EXTERNA_IMPORTE = NULL
					,USUARIOMODIFICAR = '''||V_USUARIO||'''
					,FECHAMODIFICAR = SYSDATE
				WHERE ACT_NUM_ACTIVO =  7291053 ';
	EXECUTE IMMEDIATE V_MSQL;

	DBMS_OUTPUT.PUT_LINE('[INICIO] PONER OFERTA EN TRAMITADA');

	V_MSQL := 'UPDATE REM01.OFR_OFERTAS SET DD_EOF_ID = 1,
		USUARIOMODIFICAR = '''||V_USUARIO||''',
		FECHAMODIFICAR = SYSDATE 
		WHERE OFR_NUM_OFERTA = 6010353';

	EXECUTE IMMEDIATE V_MSQL;

	DBMS_OUTPUT.PUT_LINE('[INFO] METER FECHA VENTA EXPEDIENTE Y PONERLO EN VENDIDO');

	V_MSQL := 'UPDATE REM01.ECO_EXPEDIENTE_COMERCIAL SET ECO_FECHA_VENTA = TO_DATE (''12/02/2019'',''DD/MM/YYYY''), 
		ECO_FECHA_CONT_PROPIETARIO = TO_DATE (''12/05/2019'',''DD/MM/YYYY''), 
		DD_EEC_ID = 8,
		USUARIOMODIFICAR = '''||V_USUARIO||''',
		FECHAMODIFICAR = SYSDATE 
		WHERE ECO_NUM_EXPEDIENTE = 206514';

	EXECUTE IMMEDIATE V_MSQL;

	DBMS_OUTPUT.PUT_LINE('[INFO] Se han actualizado  '||SQL%ROWCOUNT||' registros .');

    COMMIT;

	DBMS_OUTPUT.PUT_LINE('[FIN]');
  
EXCEPTION
	WHEN OTHERS THEN 
	    DBMS_OUTPUT.PUT_LINE('KO!');
	    ERR_NUM := SQLCODE;
	    ERR_MSG := SQLERRM;
	    
	    DBMS_OUTPUT.PUT_LINE('[ERROR] Se ha producido un error en la ejecución:'||TO_CHAR(ERR_NUM));
	    DBMS_OUTPUT.PUT_LINE('-----------------------------------------------------------'); 
	    DBMS_OUTPUT.PUT_LINE(err_msg);
	    
	    ROLLBACK;
	    RAISE;          

END;
/
EXIT;
