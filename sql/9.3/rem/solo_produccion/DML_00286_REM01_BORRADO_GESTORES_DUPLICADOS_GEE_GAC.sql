--/*
--######################################### 
--## AUTOR=Viorel Remus Ovidiu
--## FECHA_CREACION=20200502
--## ARTEFACTO=online
--## VERSION_ARTEFACTO=9.3
--## INCIDENCIA_LINK=REMVIP-7211
--## PRODUCTO=NO
--## 
--## Finalidad:
--##            
--## INSTRUCCIONES:  
--## VERSIONES:
--##        0.1 Versión inicial
--#########################################
--*/

--Para permitir la visualización de texto en un bloque PL/SQL utilizando DBMS_OUTPUT.PUT_LINE

WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON; 
SET DEFINE OFF;

DECLARE
	V_MSQL VARCHAR2(32000 CHAR); -- Sentencia a ejecutar    
	V_ESQUEMA VARCHAR2(25 CHAR):= '#ESQUEMA#'; -- #ESQUEMA# Configuracion Esquema
	V_ESQUEMA_M VARCHAR2(25 CHAR):= '#ESQUEMA_MASTER#'; -- #ESQUEMA_MASTER# Configuracion Esquema Master 
	ERR_NUM NUMBER(25);  -- Vble. auxiliar para registrar errores en el script.
	ERR_MSG VARCHAR2(1024 CHAR); -- Vble. auxiliar para registrar errores en el script.
	V_USUARIO VARCHAR2(50 CHAR) := 'REMVIP-7211';
	
BEGIN
        DBMS_OUTPUT.PUT_LINE('[INICIO]');
	
	DBMS_OUTPUT.PUT_LINE('[INICIO] BORRADO LOGICO GESTORES HAYAGBOINM');

	V_MSQL := 'UPDATE REM01.GEE_GESTOR_ENTIDAD SET BORRADO = 1, 
		   USUARIOBORRAR = ''REMVIP-7211'',
		   FECHABORRAR = SYSDATE
		   WHERE GEE_ID IN 
		   (SELECT GEE_ID FROM (
		        SELECT GAC.GEE_ID,GAC.ACT_ID, ROW_NUMBER() OVER(PARTITION BY GAC.ACT_ID ORDER BY GAC.GEE_ID DESC) AS RN
		        FROM REM01.GAC_GESTOR_ADD_ACTIVO GAC
		        JOIN REM01.GEE_GESTOR_ENTIDAD GEE ON GEE.GEE_ID = GAC.GEE_ID
		        JOIN REMMASTER.DD_TGE_TIPO_GESTOR TGE ON (GEE.DD_TGE_ID = TGE.DD_TGE_ID AND DD_TGE_CODIGO = ''HAYAGBOINM'' AND  GEE.BORRADO = 0)
		        WHERE ACT_ID IN (SELECT ACT.ACT_ID
		                            FROM REM01.GAC_GESTOR_ADD_ACTIVO GAC 
		                            JOIN REM01.GEE_GESTOR_ENTIDAD GEE ON GEE.GEE_ID = GAC.GEE_ID
		                            JOIN REMMASTER.DD_TGE_TIPO_GESTOR TGE ON (GEE.DD_TGE_ID = TGE.DD_TGE_ID AND DD_TGE_CODIGO = ''HAYAGBOINM'' AND  GEE.BORRADO = 0)
		                            JOIN REMMASTER.USU_USUARIOS USU ON USU.USU_ID = GEE.USU_ID
		                            JOIN REM01.ACT_ACTIVO ACT ON ACT.ACT_ID = GAC.ACT_ID
		                            GROUP BY ACT.ACT_ID HAVING COUNT(*) > 1))
		WHERE RN = 2)';

	EXECUTE IMMEDIATE V_MSQL;

	DBMS_OUTPUT.PUT_LINE('[INFO] Se han actualizado  '||SQL%ROWCOUNT||' registros .');

	DBMS_OUTPUT.PUT_LINE('[INICIO] BORRADO LOGICO GESTORES GCOM');

	V_MSQL := 'UPDATE REM01.GEE_GESTOR_ENTIDAD SET BORRADO = 1, 
		   USUARIOBORRAR = ''REMVIP-7211'', 
		   FECHABORRAR = SYSDATE
		   WHERE GEE_ID IN 
		   (SELECT GEE_ID FROM (
				SELECT GAC.GEE_ID,GAC.ACT_ID, ROW_NUMBER() OVER(PARTITION BY GAC.ACT_ID ORDER BY GAC.GEE_ID DESC) AS RN
				FROM REM01.GAC_GESTOR_ADD_ACTIVO GAC
				JOIN REM01.GEE_GESTOR_ENTIDAD GEE ON GEE.GEE_ID = GAC.GEE_ID
				JOIN REMMASTER.DD_TGE_TIPO_GESTOR TGE ON (GEE.DD_TGE_ID = TGE.DD_TGE_ID AND DD_TGE_CODIGO = ''GCOM'' AND  GEE.BORRADO = 0)
				WHERE ACT_ID IN (SELECT ACT.ACT_ID
				                    FROM REM01.GAC_GESTOR_ADD_ACTIVO GAC 
				                    JOIN REM01.GEE_GESTOR_ENTIDAD GEE ON GEE.GEE_ID = GAC.GEE_ID
				                    JOIN REMMASTER.DD_TGE_TIPO_GESTOR TGE ON (GEE.DD_TGE_ID = TGE.DD_TGE_ID AND DD_TGE_CODIGO = ''GCOM'' AND  GEE.BORRADO = 0)
				                    JOIN REMMASTER.USU_USUARIOS USU ON USU.USU_ID = GEE.USU_ID
				                    JOIN REM01.ACT_ACTIVO ACT ON ACT.ACT_ID = GAC.ACT_ID
				                    GROUP BY ACT.ACT_ID HAVING COUNT(*) > 1))
       		 WHERE RN = 2)';

	EXECUTE IMMEDIATE V_MSQL;

	DBMS_OUTPUT.PUT_LINE('[INFO] Se han actualizado  '||SQL%ROWCOUNT||' registros .');

	V_MSQL := 'DELETE FROM REM01.GAC_GESTOR_ADD_ACTIVO WHERE GEE_ID IN (SELECT GEE_ID FROM  REM01.GEE_GESTOR_ENTIDAD WHERE BORRADO = 1 AND USUARIOBORRAR = ''REMVIP-7211'')';

	EXECUTE IMMEDIATE V_MSQL;

	DBMS_OUTPUT.PUT_LINE('[INFO] Se han actualizado  '||SQL%ROWCOUNT||' registros .');

    	COMMIT;

	DBMS_OUTPUT.PUT_LINE('[FIN]');
  
EXCEPTION
	WHEN OTHERS THEN 
	    DBMS_OUTPUT.PUT_LINE('KO!');
	    ERR_NUM := SQLCODE;
	    ERR_MSG := SQLERRM;
	    
	    DBMS_OUTPUT.PUT_LINE('[ERROR] Se ha producido un error en la ejecución:'||TO_CHAR(ERR_NUM));
	    DBMS_OUTPUT.PUT_LINE('-----------------------------------------------------------'); 
	    DBMS_OUTPUT.PUT_LINE(err_msg);
	    
	    ROLLBACK;
	    RAISE;          

END;
/
EXIT;
