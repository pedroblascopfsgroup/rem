--/*
--###########################################
--## AUTOR=Viorel Remus Ovidiu
--## FECHA_CREACION=20200616
--## ARTEFACTO=online
--## VERSION_ARTEFACTO=9.3
--## INCIDENCIA_LINK=REMVIP-7558
--## PRODUCTO=NO
--## 
--## Finalidad: reenviar cam,bios detector webcom
--##			
--## INSTRUCCIONES:  
--## VERSIONES:
--##        0.1 Versión inicial
--###########################################
----*/


WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON; 
SET DEFINE OFF;


DECLARE
  V_MSQL VARCHAR2(32000 CHAR); -- Sentencia a ejecutar     
  V_ESQUEMA VARCHAR2(25 CHAR):= '#ESQUEMA#'; -- Configuracion Esquema
  V_ESQUEMA_M VARCHAR2(25 CHAR):= '#ESQUEMA_MASTER#'; -- Configuracion Esquema Master
  V_SQL VARCHAR2(4000 CHAR); -- Vble. para consulta que valida la existencia de una tabla.
  V_NUM_ACT NUMBER(16); -- Vble. para validar la existencia de una tabla.   
  V_COUNT1 NUMBER(16);
  V_COUNT2 NUMBER(16);
  ERR_NUM NUMBER(25);  -- Vble. auxiliar para registrar errores en el script.
  ERR_MSG VARCHAR2(1024 CHAR); -- Vble. auxiliar para registrar errores en el script.
  V_USUARIOMODIFICAR VARCHAR2(100 CHAR) := 'REMVIP-7558';
  
    
BEGIN	
	
    DBMS_OUTPUT.PUT_LINE('[INICIO] ');
	 
    DBMS_OUTPUT.PUT_LINE('[INFO]: ELIMINAR REGISTROS DE HISTORICO WEBCOM PARA REENVIAR CAMBIOS ' );


	V_MSQL := 'DELETE FROM '||V_ESQUEMA||'.SWH_STOCK_ACT_WEBCOM_HIST ACTHIST WHERE ID_ACTIVO_HAYA IN (
			SELECT ID_ACTIVO_HAYA FROM '||V_ESQUEMA||'.SWH_STOCK_ACT_WEBCOM_HIST ACTHIST
			INNER JOIN '||V_ESQUEMA||'.ACT_ACTIVO ACT ON ACTHIST.ID_ACTIVO_HAYA = ACT.ACT_NUM_ACTIVO
			INNER JOIN '||V_ESQUEMA||'.ACT_AGA_AGRUPACION_ACTIVO AGA ON AGA.ACT_ID = ACT.ACT_ID
			INNER JOIN '||V_ESQUEMA||'.ACT_AGR_AGRUPACION AGRUP ON AGRUP.AGR_ID = AGA.AGR_ID 
			INNER JOIN '||V_ESQUEMA||'.AWH_AGRUP_ONV_WEBCOM_HIST AGRWEB ON AGRWEB.ID_AGRUPACION_REM = AGRUP.AGR_NUM_AGRUP_REM
			WHERE TO_DATE(AGRWEB.FECHA_ACCION, ''YYYY-MM-DD"T"HH24:MI:SS'') > TO_DATE(''2020-06-05T07:00:00'', ''YYYY-MM-DD"T"HH24:MI:SS''))';

        EXECUTE IMMEDIATE V_MSQL;

	DBMS_OUTPUT.PUT_LINE('[INFO] Eliminados '||SQL%ROWCOUNT||' registros en SWH_STOCK_ACT_WEBCOM_HIST');  

	V_MSQL := 'DELETE FROM '||V_ESQUEMA||'.SWH_STOCK_ACT_WEBCOM_HIST ACTHIST WHERE ID_ACTIVO_HAYA IN (
			SELECT ID_ACTIVO_HAYA FROM '||V_ESQUEMA||'.SWH_STOCK_ACT_WEBCOM_HIST ACTHIST
			INNER JOIN '||V_ESQUEMA||'.ACT_ACTIVO ACT ON ACTHIST.ID_ACTIVO_HAYA = ACT.ACT_NUM_ACTIVO
			INNER JOIN '||V_ESQUEMA||'.ACT_AGA_AGRUPACION_ACTIVO AGA ON AGA.ACT_ID = ACT.ACT_ID
			INNER JOIN '||V_ESQUEMA||'.ACT_AGR_AGRUPACION AGRUP ON AGRUP.AGR_ID = AGA.AGR_ID 
			INNER JOIN '||V_ESQUEMA||'.cwh_cabec_onv_webcom_hist AGRWEB ON AGRWEB.ID_AGRUPACION_REM = AGRUP.AGR_NUM_AGRUP_REM
			WHERE TO_DATE(AGRWEB.FECHA_ACCION, ''YYYY-MM-DD"T"HH24:MI:SS'') > TO_DATE(''2020-06-05T07:00:00'', ''YYYY-MM-DD"T"HH24:MI:SS''))';

        EXECUTE IMMEDIATE V_MSQL;

	DBMS_OUTPUT.PUT_LINE('[INFO] Eliminados '||SQL%ROWCOUNT||' registros en SWH_STOCK_ACT_WEBCOM_HIST');  

	V_MSQL := 'DELETE FROM '||V_ESQUEMA||'.AWH_AGRUP_ONV_WEBCOM_HIST';

	EXECUTE IMMEDIATE V_MSQL;

	V_MSQL := 'DELETE FROM '||V_ESQUEMA||'.CWH_CABEC_ONV_WEBCOM_HIST';

	EXECUTE IMMEDIATE V_MSQL;

        COMMIT;
        DBMS_OUTPUT.PUT_LINE('[FIN] ');

EXCEPTION

   WHEN OTHERS THEN
        err_num := SQLCODE;
        err_msg := SQLERRM;

        DBMS_OUTPUT.put_line('[ERROR] Se ha producido un error en la ejecución:'||TO_CHAR(err_num));
        DBMS_OUTPUT.put_line('-----------------------------------------------------------'); 
        DBMS_OUTPUT.put_line(err_msg);

        ROLLBACK;
        RAISE;          

END;

/

EXIT
