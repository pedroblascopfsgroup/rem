--/*
--##########################################
--## AUTOR=Juan Beltran
--## FECHA_CREACION=20200402
--## ARTEFACTO=online
--## VERSION_ARTEFACTO=9.3
--## INCIDENCIA_LINK=REMVIP-6835
--## PRODUCTO=NO
--## 
--## Finalidad: Actualización tabla 
--##			
--## INSTRUCCIONES:  
--## VERSIONES:
--##        0.1 Versión inicial
--#########################################
--*/

--Para permitir la visualización de texto en un bloque PL/SQL utilizando DBMS_OUTPUT.PUT_LINE

WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON; 
SET DEFINE OFF;


DECLARE
    V_MSQL VARCHAR2(32000 CHAR); -- Sentencia a ejecutar     
    V_ESQUEMA VARCHAR2(25 CHAR):= '#ESQUEMA#'; -- Configuracion Esquema
    V_ESQUEMA_M VARCHAR2(25 CHAR):= '#ESQUEMA_MASTER#'; -- Configuracion Esquema Master
    V_SQL VARCHAR2(4000 CHAR); -- Vble. para consulta que valida la existencia de una tabla.
    V_NUM_TABLAS NUMBER(16); -- Vble. para validar la existencia de una tabla.   
    ERR_NUM NUMBER(25);  -- Vble. auxiliar para registrar errores en el script.
    ERR_MSG VARCHAR2(1024 CHAR); -- Vble. auxiliar para registrar errores en el script.
    V_TEXT_TABLA VARCHAR2(2400 CHAR) := 'ACT_GES_DIST_GESTORES'; -- Vble. auxiliar para almacenar el nombre de la tabla de ref.
    V_ID NUMBER(16);

    
BEGIN		
	DBMS_OUTPUT.PUT_LINE('[INICIO] ');
	 	
     DBMS_OUTPUT.PUT_LINE('[INFO]: ACTUALIZAR GARSA ');

       	  V_MSQL := 'INSERT INTO '|| V_ESQUEMA ||'.AUX_REMVIP_8946_GEE(GEE_ID, USUARIO)
			SELECT DISTINCT GEE.GEE_ID, ''garsa03_bbva''
			FROM '|| V_ESQUEMA ||'.ACT_ACTIVO ACT
			JOIN '|| V_ESQUEMA ||'.ACT_OFR ACO ON ACO.ACT_ID = ACT.ACT_ID
			JOIN '|| V_ESQUEMA ||'.OFR_OFERTAS OFR ON OFR.OFR_ID = ACO.OFR_ID
			JOIN '|| V_ESQUEMA ||'.ECO_EXPEDIENTE_COMERCIAL ECO ON ECO.OFR_ID = OFR.OFR_ID
			JOIN '|| V_ESQUEMA ||'.GCO_GESTOR_ADD_ECO GAC ON GAC.ECO_ID = ECO.ECO_ID
			JOIN '|| V_ESQUEMA ||'.GEE_GESTOR_ENTIDAD GEE ON GEE.GEE_ID = GAC.GEE_ID
			JOIN '|| V_ESQUEMA_M ||'.USU_USUARIOS USU ON USU.USU_ID = GEE.USU_ID
			JOIN '|| V_ESQUEMA ||'.DD_CRA_CARTERA CRA ON CRA.DD_CRA_ID = ACT.DD_CRA_ID
			WHERE GEE.DD_TGE_ID = (SELECT DD_TGE_ID FROM '|| V_ESQUEMA_M ||'.DD_TGE_TIPO_GESTOR WHERE DD_TGE_CODIGO = ''GIAFORM'') 
			AND USU.USU_USERNAME = ''garsa03'' AND GEE.BORRADO = 0 AND CRA.DD_CRA_CODIGO = ''16''';
					
          EXECUTE IMMEDIATE V_MSQL;           	
          DBMS_OUTPUT.PUT_LINE('[INFO] ACTUALIZADOS  '|| SQL%ROWCOUNT ||' REGISTROS ');
          
     DBMS_OUTPUT.PUT_LINE('[INFO]: ACTUALIZAR GARSA ');

       	  V_MSQL := 'INSERT INTO '|| V_ESQUEMA ||'.AUX_REMVIP_8946_GEH(GEE_ID, USUARIO)
			SELECT DISTINCT GEE.GEH_ID, ''garsa03_bbva''
			FROM '|| V_ESQUEMA ||'.ACT_ACTIVO ACT 
			JOIN '|| V_ESQUEMA ||'.ACT_OFR ACO ON ACO.ACT_ID = ACT.ACT_ID
			JOIN '|| V_ESQUEMA ||'.OFR_OFERTAS OFR ON OFR.OFR_ID = ACO.OFR_ID
			JOIN '|| V_ESQUEMA ||'.ECO_EXPEDIENTE_COMERCIAL ECO ON ECO.OFR_ID = OFR.OFR_ID
			JOIN '|| V_ESQUEMA ||'.GCH_GESTOR_ECO_HISTORICO GAC ON GAC.ECO_ID = ECO.ECO_ID
			JOIN '|| V_ESQUEMA ||'.GEH_GESTOR_ENTIDAD_HIST GEE ON GEE.GEH_ID = GAC.GEH_ID
			JOIN '|| V_ESQUEMA_M ||'.USU_USUARIOS USU ON USU.USU_ID = GEE.USU_ID
			JOIN '|| V_ESQUEMA ||'.DD_CRA_CARTERA CRA ON CRA.DD_CRA_ID = ACT.DD_CRA_ID
			WHERE GEE.DD_TGE_ID = (SELECT DD_TGE_ID FROM '|| V_ESQUEMA_M ||'.DD_TGE_TIPO_GESTOR WHERE DD_TGE_CODIGO = ''GIAFORM'')
			AND USU.USU_USERNAME = ''garsa03'' AND GEE.BORRADO = 0 AND CRA.DD_CRA_CODIGO = ''16''';
					
          EXECUTE IMMEDIATE V_MSQL;           	
          DBMS_OUTPUT.PUT_LINE('[INFO] ACTUALIZADOS  '|| SQL%ROWCOUNT ||' REGISTROS ');
          
     DBMS_OUTPUT.PUT_LINE('[INFO]: ACTUALIZAR PINOS ');

       	  V_MSQL := 'INSERT INTO '|| V_ESQUEMA ||'.AUX_REMVIP_8946_GEE(GEE_ID, USUARIO)
			SELECT DISTINCT GEE.GEE_ID, ''pinos03_bbva''
			FROM '|| V_ESQUEMA ||'.ACT_ACTIVO ACT
			JOIN '|| V_ESQUEMA ||'.ACT_OFR ACO ON ACO.ACT_ID = ACT.ACT_ID
			JOIN '|| V_ESQUEMA ||'.OFR_OFERTAS OFR ON OFR.OFR_ID = ACO.OFR_ID
			JOIN '|| V_ESQUEMA ||'.ECO_EXPEDIENTE_COMERCIAL ECO ON ECO.OFR_ID = OFR.OFR_ID
			JOIN '|| V_ESQUEMA ||'.GCO_GESTOR_ADD_ECO GAC ON GAC.ECO_ID = ECO.ECO_ID
			JOIN '|| V_ESQUEMA ||'.GEE_GESTOR_ENTIDAD GEE ON GEE.GEE_ID = GAC.GEE_ID
			JOIN '|| V_ESQUEMA_M ||'.USU_USUARIOS USU ON USU.USU_ID = GEE.USU_ID
			JOIN '|| V_ESQUEMA ||'.DD_CRA_CARTERA CRA ON CRA.DD_CRA_ID = ACT.DD_CRA_ID
			WHERE GEE.DD_TGE_ID = (SELECT DD_TGE_ID FROM '|| V_ESQUEMA_M ||'.DD_TGE_TIPO_GESTOR WHERE DD_TGE_CODIGO = ''GIAFORM'') 
			AND USU.USU_USERNAME = ''pinos03'' AND GEE.BORRADO = 0 AND CRA.DD_CRA_CODIGO = ''16''';
					
          EXECUTE IMMEDIATE V_MSQL;           	
          DBMS_OUTPUT.PUT_LINE('[INFO] ACTUALIZADOS  '|| SQL%ROWCOUNT ||' REGISTROS ');
          

     DBMS_OUTPUT.PUT_LINE('[INFO]: ACTUALIZAR PINOS ');

       	  V_MSQL := 'INSERT INTO '|| V_ESQUEMA ||'.AUX_REMVIP_8946_GEH(GEE_ID, USUARIO)
			SELECT DISTINCT GEE.GEH_ID, ''pinos03_bbva''
			FROM '|| V_ESQUEMA ||'.ACT_ACTIVO ACT 
			JOIN '|| V_ESQUEMA ||'.ACT_OFR ACO ON ACO.ACT_ID = ACT.ACT_ID
			JOIN '|| V_ESQUEMA ||'.OFR_OFERTAS OFR ON OFR.OFR_ID = ACO.OFR_ID
			JOIN '|| V_ESQUEMA ||'.ECO_EXPEDIENTE_COMERCIAL ECO ON ECO.OFR_ID = OFR.OFR_ID
			JOIN '|| V_ESQUEMA ||'.GCH_GESTOR_ECO_HISTORICO GAC ON GAC.ECO_ID = ECO.ECO_ID
			JOIN '|| V_ESQUEMA ||'.GEH_GESTOR_ENTIDAD_HIST GEE ON GEE.GEH_ID = GAC.GEH_ID
			JOIN '|| V_ESQUEMA_M ||'.USU_USUARIOS USU ON USU.USU_ID = GEE.USU_ID
			JOIN '|| V_ESQUEMA ||'.DD_CRA_CARTERA CRA ON CRA.DD_CRA_ID = ACT.DD_CRA_ID
			WHERE GEE.DD_TGE_ID = (SELECT DD_TGE_ID FROM '|| V_ESQUEMA_M ||'.DD_TGE_TIPO_GESTOR WHERE DD_TGE_CODIGO = ''GIAFORM'')
			AND USU.USU_USERNAME = ''pinos03'' AND GEE.BORRADO = 0 AND CRA.DD_CRA_CODIGO = ''16''';
					
          EXECUTE IMMEDIATE V_MSQL;           	
          DBMS_OUTPUT.PUT_LINE('[INFO] ACTUALIZADOS  '|| SQL%ROWCOUNT ||' REGISTROS ');
          
     DBMS_OUTPUT.PUT_LINE('[INFO]: ACTUALIZAR KBN ');

       	  V_MSQL := 'INSERT INTO '|| V_ESQUEMA ||'.AUX_REMVIP_8946_GEE(GEE_ID, USUARIO)
			SELECT DISTINCT GEE.GEE_ID, ''knb03_bbva''
			FROM '|| V_ESQUEMA ||'.ACT_ACTIVO ACT
			JOIN '|| V_ESQUEMA ||'.ACT_OFR ACO ON ACO.ACT_ID = ACT.ACT_ID
			JOIN '|| V_ESQUEMA ||'.OFR_OFERTAS OFR ON OFR.OFR_ID = ACO.OFR_ID
			JOIN '|| V_ESQUEMA ||'.ECO_EXPEDIENTE_COMERCIAL ECO ON ECO.OFR_ID = OFR.OFR_ID
			JOIN '|| V_ESQUEMA ||'.GCO_GESTOR_ADD_ECO GAC ON GAC.ECO_ID = ECO.ECO_ID
			JOIN '|| V_ESQUEMA ||'.GEE_GESTOR_ENTIDAD GEE ON GEE.GEE_ID = GAC.GEE_ID
			JOIN '|| V_ESQUEMA_M ||'.USU_USUARIOS USU ON USU.USU_ID = GEE.USU_ID
			JOIN '|| V_ESQUEMA ||'.DD_CRA_CARTERA CRA ON CRA.DD_CRA_ID = ACT.DD_CRA_ID
			WHERE GEE.DD_TGE_ID = (SELECT DD_TGE_ID FROM '|| V_ESQUEMA_M ||'.DD_TGE_TIPO_GESTOR WHERE DD_TGE_CODIGO = ''GIAFORM'') 
			AND USU.USU_USERNAME = ''ogf03'' AND GEE.BORRADO = 0 AND CRA.DD_CRA_CODIGO = ''16''';
					
          EXECUTE IMMEDIATE V_MSQL;           	
          DBMS_OUTPUT.PUT_LINE('[INFO] ACTUALIZADOS  '|| SQL%ROWCOUNT ||' REGISTROS ');
          
     DBMS_OUTPUT.PUT_LINE('[INFO]: ACTUALIZAR KBN ');

       	  V_MSQL := 'INSERT INTO '|| V_ESQUEMA ||'.AUX_REMVIP_8946_GEH(GEE_ID, USUARIO)
			SELECT DISTINCT GEE.GEH_ID, ''knb03_bbva''
			FROM '|| V_ESQUEMA ||'.ACT_ACTIVO ACT 
			JOIN '|| V_ESQUEMA ||'.ACT_OFR ACO ON ACO.ACT_ID = ACT.ACT_ID
			JOIN '|| V_ESQUEMA ||'.OFR_OFERTAS OFR ON OFR.OFR_ID = ACO.OFR_ID
			JOIN '|| V_ESQUEMA ||'.ECO_EXPEDIENTE_COMERCIAL ECO ON ECO.OFR_ID = OFR.OFR_ID
			JOIN '|| V_ESQUEMA ||'.GCH_GESTOR_ECO_HISTORICO GAC ON GAC.ECO_ID = ECO.ECO_ID
			JOIN '|| V_ESQUEMA ||'.GEH_GESTOR_ENTIDAD_HIST GEE ON GEE.GEH_ID = GAC.GEH_ID
			JOIN '|| V_ESQUEMA_M ||'.USU_USUARIOS USU ON USU.USU_ID = GEE.USU_ID
			JOIN '|| V_ESQUEMA ||'.DD_CRA_CARTERA CRA ON CRA.DD_CRA_ID = ACT.DD_CRA_ID
			WHERE GEE.DD_TGE_ID = (SELECT DD_TGE_ID FROM '|| V_ESQUEMA_M ||'.DD_TGE_TIPO_GESTOR WHERE DD_TGE_CODIGO = ''GIAFORM'')
			AND USU.USU_USERNAME = ''ogf03'' AND GEE.BORRADO = 0 AND CRA.DD_CRA_CODIGO = ''16''';
					
          EXECUTE IMMEDIATE V_MSQL;           	
          DBMS_OUTPUT.PUT_LINE('[INFO] ACTUALIZADOS  '|| SQL%ROWCOUNT ||' REGISTROS ');
          
    
    COMMIT;
    DBMS_OUTPUT.PUT_LINE('[FIN] ');

EXCEPTION
     WHEN OTHERS THEN
          ERR_MSG := SQLCODE;
          ERR_MSG := SQLERRM;

          DBMS_OUTPUT.PUT_LINE('[ERROR] Se ha producido un error en la ejecución:'||TO_CHAR(ERR_NUM));
          DBMS_OUTPUT.PUT_LINE('-----------------------------------------------------------'); 
          DBMS_OUTPUT.PUT_LINE(ERR_MSG);

          ROLLBACK;
          RAISE;          

END;
/

EXIT
