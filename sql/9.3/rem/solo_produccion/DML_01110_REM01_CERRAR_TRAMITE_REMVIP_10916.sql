--/*
--#########################################
--## AUTOR=PIER GOTTA
--## FECHA_CREACION=20211224
--## ARTEFACTO=batch
--## VERSION_ARTEFACTO=0.11
--## INCIDENCIA_LINK=REMVIP-10916
--## PRODUCTO=NO
--## 
--## Finalidad:
--## 
--## INSTRUCCIONES:
--## VERSIONES:
--## 		0.1 Versión inicial
--#########################################
--*/

--Para permitir la visualización de texto en un bloque PL/SQL utilizando DBMS_OUTPUT.PUT_LINE

WHENEVER SQLERROR EXIT SQL.SQLCODE;
SET SERVEROUTPUT ON;
SET DEFINE OFF;

DECLARE
	TABLE_COUNT NUMBER(10,0) := 0;
	TABLE_COUNT_2 NUMBER(10,0) := 0;
	MAX_NUM_OFR NUMBER(10,0) := 0;
	V_NUM_TABLAS NUMBER(10,0) := 0;
	V_ESQUEMA VARCHAR2(10 CHAR) := 'REM01'; --REM01
	V_ESQUEMA_M VARCHAR2(15 CHAR) := 'REMMASTER'; --REMMASTER
	V_USUARIO VARCHAR2(50 CHAR) := 'MIG_CAIXA';
	V_TABLA VARCHAR2(40 CHAR) := 'AUX_REMVIP_10916';
	V_SENTENCIA VARCHAR2(32000 CHAR);
	V_NUM_TABLAS_2 NUMBER(16);
	V_MSQL VARCHAR2(32000 CHAR); -- Vble. para consulta que valida la existencia de una tabla.

BEGIN

	--Inicio del proceso de volcado sobre OFR_OFERTAS
        	
        V_SENTENCIA := 'MERGE INTO '||V_ESQUEMA||'.ACT_TRA_TRAMITE TRA USING(
		SELECT DISTINCT TRA.TRA_ID FROM '||V_ESQUEMA||'.ECO_EXPEDIENTE_COMERCIAL ECO 
		JOIN '||V_ESQUEMA||'.AUX_REMVIP_10916 AUX ON ECO.TBJ_ID = AUX.TBJ_ID
       	JOIN '||V_ESQUEMA||'.ACT_TRA_TRAMITE TRA ON TRA.TBJ_ID = ECO.TBJ_ID
		)
		AUX ON (AUX.TRA_ID = TRA.TRA_ID)
		WHEN MATCHED THEN UPDATE SET
		TRA.TBJ_ID = NULL,
		TRA.USUARIOMODIFICAR = ''REMVIP-10916'',
		TRA.FECHAMODIFICAR = SYSDATE';
                   
        	EXECUTE IMMEDIATE V_SENTENCIA;
        	
       /* V_SENTENCIA := 'MERGE INTO '||V_ESQUEMA||'.ECO_EXPEDIENTE_COMERCIAL ECO USING(
		SELECT DISTINCT ECO.ECO_ID FROM '||V_ESQUEMA||'.ECO_EXPEDIENTE_COMERCIAL ECO 
		JOIN '||V_ESQUEMA||'.AUX_REMVIP_10916 AUX ON ECO.TBJ_ID = AUX.TBJ_ID
		)
		AUX ON (AUX.ECO_ID = ECO.ECO_ID)
		WHEN MATCHED THEN UPDATE SET
		ECO.TBJ_ID = NULL,
		ECO.USUARIOMODIFICAR = ''REMVIP-10916'',
		ECO.FECHAMODIFICAR = SYSDATE';
                   
        	EXECUTE IMMEDIATE V_SENTENCIA;*/

        V_SENTENCIA := 'MERGE INTO '||V_ESQUEMA||'.TAR_TAREAS_NOTIFICACIONES TAR USING(
		SELECT DISTINCT TAR.TAR_ID FROM '||V_ESQUEMA||'.ECO_EXPEDIENTE_COMERCIAL ECO
		INNER JOIN '||V_ESQUEMA||'.OFR_OFERTAS OFR ON ECO.OFR_ID = OFR.OFR_ID
		INNER JOIN '||V_ESQUEMA||'.ACT_OFR ACO ON ACO.OFR_ID = OFR.OFR_ID
		INNER JOIN '||V_ESQUEMA||'.ACT_ACTIVO ACT ON ACT.ACT_ID = ACO.ACT_ID
		INNER JOIN '||V_ESQUEMA||'.ACT_TRA_TRAMITE ATR ON /*ATR.ACT_ID = ACT.ACT_ID AND */ATR.TBJ_ID=ECO.TBJ_ID
		INNER JOIN '||V_ESQUEMA||'.TAC_TAREAS_ACTIVOS TAC ON ATR.TRA_ID = TAC.TRA_ID
		INNER JOIN '||V_ESQUEMA||'.TAR_TAREAS_NOTIFICACIONES TAR ON TAR.TAR_ID = TAC.TAR_ID
		INNER JOIN '||V_ESQUEMA||'.TEX_TAREA_EXTERNA TXT ON TXT.TAR_ID = TAR.TAR_ID
		INNER JOIN '||V_ESQUEMA||'.TAP_TAREA_PROCEDIMIENTO TAP ON TXT.TAP_ID = TAP.TAP_ID
		WHERE OFR.OFR_NUM_OFERTA = 90345209
		) AUX ON (AUX.TAR_ID = TAR.TAR_ID)
		WHEN MATCHED THEN UPDATE SET
		TAR.TAR_FECHA_FIN = SYSDATE,
		TAR.TAR_TAREA_FINALIZADA = 1,
		TAR.USUARIOMODIFICAR = ''REMVIP-10916'',
		TAR.FECHAMODIFICAR = SYSDATE';
                   
        	EXECUTE IMMEDIATE V_SENTENCIA;

        V_SENTENCIA := 'MERGE INTO '||V_ESQUEMA||'.ACT_TRA_TRAMITE TRA USING(
		SELECT DISTINCT ATR.TRA_ID FROM '||V_ESQUEMA||'.ECO_EXPEDIENTE_COMERCIAL ECO
		INNER JOIN '||V_ESQUEMA||'.OFR_OFERTAS OFR ON ECO.OFR_ID = OFR.OFR_ID
		INNER JOIN '||V_ESQUEMA||'.ACT_OFR ACO ON ACO.OFR_ID = OFR.OFR_ID
		INNER JOIN '||V_ESQUEMA||'.ACT_ACTIVO ACT ON ACT.ACT_ID = ACO.ACT_ID
		INNER JOIN '||V_ESQUEMA||'.ACT_TRA_TRAMITE ATR ON /*ATR.ACT_ID = ACT.ACT_ID AND */ATR.TBJ_ID=ECO.TBJ_ID
		INNER JOIN '||V_ESQUEMA||'.TAC_TAREAS_ACTIVOS TAC ON ATR.TRA_ID = TAC.TRA_ID
		INNER JOIN '||V_ESQUEMA||'.TAR_TAREAS_NOTIFICACIONES TAR ON TAR.TAR_ID = TAC.TAR_ID
		INNER JOIN '||V_ESQUEMA||'.TEX_TAREA_EXTERNA TXT ON TXT.TAR_ID = TAR.TAR_ID
		INNER JOIN '||V_ESQUEMA||'.TAP_TAREA_PROCEDIMIENTO TAP ON TXT.TAP_ID = TAP.TAP_ID
		WHERE OFR.OFR_NUM_OFERTA = 90345209
		) AUX ON (AUX.TRA_ID = TRA.TRA_ID)
		WHEN MATCHED THEN UPDATE SET
		TRA.DD_EPR_ID = (SELECT DD_EPR_ID FROM '||V_ESQUEMA_M||'.DD_EPR_ESTADO_PROCEDIMIENTO WHERE DD_EPR_CODIGO = ''05''),
		TRA.USUARIOMODIFICAR = ''REMVIP-10916'',
		TRA.FECHAMODIFICAR = SYSDATE';
                   
        	EXECUTE IMMEDIATE V_SENTENCIA;


        
        COMMIT;

	DBMS_OUTPUT.PUT_LINE('[INFO] - '||to_char(sysdate,'HH24:MI:SS')||'  '||V_ESQUEMA||'.MIG2_OFR_MAPEO_NUM_OFERTAS cargada. '||SQL%ROWCOUNT||' Filas.');

EXCEPTION
	WHEN OTHERS THEN
		DBMS_OUTPUT.put_line('[ERROR] Se ha producido un error en la ejecucion:'||TO_CHAR(SQLCODE));
		DBMS_OUTPUT.put_line('-----------------------------------------------------------');
		DBMS_OUTPUT.put_line(V_SENTENCIA);
		ROLLBACK;
		RAISE;
END;
/
EXIT;
