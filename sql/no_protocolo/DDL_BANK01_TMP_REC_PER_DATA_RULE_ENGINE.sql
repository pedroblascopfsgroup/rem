CREATE OR REPLACE FORCE VIEW "BANK01"."TMP_REC_PER_DATA_RULE_ENGINE"  AS 
  SELECT *
	    FROM BANK01.DATA_RULE_ENGINE
	   WHERE per_id IN (SELECT DISTINCT (per_id) FROM BANK01.tmp_rec_exp_desnormalizado);

GRANT ALL PRIVILEGES ON "BANK01"."TMP_REC_PER_DATA_RULE_ENGINE" TO PFSRECOVERY;
