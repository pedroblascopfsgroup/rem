CREATE TABLE MSV_TMP_FILES
(
	MSV_TMP_ID       	NUMBER                      NOT NULL,
	MSV_TMP_FICHERO  	BLOB                        NOT NULL,
	MSV_TMP_NOMBRE		VARCHAR2(200 BYTE),
	MSV_TMP_CONTENT		VARCHAR2(100 BYTE),
  	VERSION            	INTEGER                    DEFAULT 0                     NOT NULL,
  	USUARIOCREAR       	VARCHAR2(10 BYTE)          	NOT NULL,
  	FECHACREAR         	TIMESTAMP(6)               NOT NULL,
  	USUARIOMODIFICAR   	VARCHAR2(10 BYTE),
  	FECHAMODIFICAR     	TIMESTAMP(6),
  	USUARIOBORRAR      	VARCHAR2(10 BYTE),
  	FECHABORRAR        	TIMESTAMP(6),
  	BORRADO            	NUMBER(1)                  	DEFAULT 0                     NOT NULL	
);

CREATE UNIQUE INDEX PK_MSV_TMP_FILES ON MSV_TMP_FILES (MSV_TMP_ID);

ALTER TABLE MSV_TMP_FILES ADD (
  CONSTRAINT PK_MSV_TMP_FILES PRIMARY KEY (MSV_TMP_ID)
);

CREATE SEQUENCE S_MSV_TMP_FILES;