CREATE TABLE RAS_RANKING_SUBCARTERA (    
    RAS_ID                  NUMBER(16)          NOT NULL,
    RCF_SCA_ID              NUMBER(16)          NOT NULL,
    RCF_AGE_ID              NUMBER(16)          NOT NULL,
	RAS_POSICION	      	INTEGER,    
	RAS_PORCENTAJE          NUMBER(16),
    VERSION                 INTEGER    DEFAULT 0 NOT NULL,
    USUARIOCREAR            VARCHAR2(10 CHAR)    NOT NULL,
    FECHACREAR              TIMESTAMP(6)         NOT NULL,
    USUARIOMODIFICAR        VARCHAR2(10 CHAR),
    FECHAMODIFICAR          TIMESTAMP(6),
    USUARIOBORRAR           VARCHAR2(10 CHAR),
    FECHABORRAR             TIMESTAMP(6),
    BORRADO                 NUMBER(1)  DEFAULT 0  NOT NULL
);
ALTER TABLE RAS_RANKING_SUBCARTERA ADD CONSTRAINT PK_RAS_RANKING_SUBCARTERA PRIMARY KEY (RAS_ID);

CREATE TABLE H_RAS_RANKING_SUBCARTERA (    
	FECHA_HIST				TIMESTAMP(6) 	    NOT NULL,
	H_RAS_ID                NUMBER(16)          NOT NULL,
    RAS_ID                  NUMBER(16)          NOT NULL,
    RCF_SCA_ID              NUMBER(16)          NOT NULL,
    RCF_AGE_ID              NUMBER(16)          NOT NULL,
	RAS_POSICION	      	INTEGER,    
	RAS_PORCENTAJE          NUMBER(16),
    VERSION                 INTEGER    DEFAULT 0 NOT NULL,
    USUARIOCREAR            VARCHAR2(10 CHAR)    NOT NULL,
    FECHACREAR              TIMESTAMP(6)         NOT NULL,
    USUARIOMODIFICAR        VARCHAR2(10 CHAR),
    FECHAMODIFICAR          TIMESTAMP(6),
    USUARIOBORRAR           VARCHAR2(10 CHAR),
    FECHABORRAR             TIMESTAMP(6),
    BORRADO                 NUMBER(1)  DEFAULT 0  NOT NULL
);
ALTER TABLE H_RAS_RANKING_SUBCARTERA ADD CONSTRAINT PK_H_RAS_RANKING_SUBCARTERA PRIMARY KEY (RAS_ID);

CREATE TABLE RSD_RANKING_SUBCAR_DETALLE (   
    RSD_ID                  NUMBER(16)              NOT NULL,
    RCF_SCA_ID              NUMBER(16)              NOT NULL,
    RCF_AGE_ID              NUMBER(16)              NOT NULL,
    RCF_DD_VAR_ID           NUMBER(16)              NOT NULL,
    RSD_PESO                NUMBER (14,2)           NOT NULL,
    RSD_RESULTADO           NUMBER (14,2)           NOT NULL,
    RSD_POSICION            INTEGER                 NOT NULL,
    VERSION                 INTEGER    DEFAULT 0    NOT NULL,
    USUARIOCREAR            VARCHAR2(10 CHAR)       NOT NULL,
    FECHACREAR              TIMESTAMP(6)            NOT NULL,
    USUARIOMODIFICAR        VARCHAR2(10 CHAR),
    FECHAMODIFICAR          TIMESTAMP(6),
    USUARIOBORRAR           VARCHAR2(10 CHAR),
    FECHABORRAR             TIMESTAMP(6),
    BORRADO                 NUMBER(1)  DEFAULT 0    NOT NULL
);
ALTER TABLE RSD_RANKING_SUBCAR_DETALLE ADD CONSTRAINT PK_RSD_RANKING_SUBCAR_DETALLE PRIMARY KEY (RSD_ID);

CREATE TABLE H_RSD_RANKING_SUBCAR_DETALLE 
(
  FECHA_HIST 				TIMESTAMP(6) NOT NULL,
  H_RSD_ID 					NUMBER(16, 0) NOT NULL, 
  RSD_ID 					NUMBER(16, 0) NOT NULL, 
  RCF_SCA_ID 				NUMBER(16, 0) NOT NULL, 
  RCF_AGE_ID 				NUMBER(16, 0) NOT NULL, 
  RCF_DD_VAR_ID 			NUMBER(16, 0) NOT NULL, 
  RSD_PESO 					NUMBER(14, 2) NOT NULL, 
  RSD_RESULTADO 			NUMBER(14, 2) NOT NULL, 
  RSD_POSICION 				NUMBER(*, 0) NOT NULL, 
  VERSION 					NUMBER(*, 0) NOT NULL, 
  USUARIOCREAR 				VARCHAR2(10 CHAR) NOT NULL, 
  FECHACREAR 				TIMESTAMP(6) NOT NULL, 
  USUARIOMODIFICAR 			VARCHAR2(10 CHAR), 
  FECHAMODIFICAR			TIMESTAMP(6), 
  USUARIOBORRAR 			VARCHAR2(10 CHAR), 
  FECHABORRAR 				TIMESTAMP(6), 
  BORRADO 					NUMBER(1, 0) NOT NULL 
);
ALTER TABLE H_RSD_RANKING_SUBCAR_DETALLE ADD CONSTRAINT PK_H_RSD_RANKING_SUBCAR_DET PRIMARY KEY (RSD_ID);
