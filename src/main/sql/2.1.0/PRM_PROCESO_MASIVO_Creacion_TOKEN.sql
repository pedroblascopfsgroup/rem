--Añade el campo token a la tabla.
ALTER TABLE PRM_PROCESO_MASIVO
 ADD PRM_PROCESO_TOKEN NUMBER(16);

--Como ya tiene valores se le dan por defecto unos tokens invalidos.
UPDATE PRM_PROCESO_MASIVO SET PRM_PROCESO_TOKEN = S_BPM_PROCESO_TOKEN.NEXTVAL;
COMMIT;

--Se modifica a Not Null y se crea la constraint unique.
ALTER TABLE PRM_PROCESO_MASIVO
    MODIFY (PRM_PROCESO_TOKEN NUMBER(16) NOT NULL);
    
ALTER TABLE PRM_PROCESO_MASIVO ADD 
CONSTRAINT PRM_PROCESO_MASIVO_TOKEN
 UNIQUE (PRM_PROCESO_TOKEN)

 ENABLE
 VALIDATE;