-- Insertar datos de fecha de creacion
UPDATE CLI_CLIENTES
SET CLI_FECHA_CREACION = FECHACREAR;

-- Calcular estado del cliente
-- Cliente Activo
UPDATE CLI_CLIENTES
SET DD_ECL_ID = (SELECT DD_ECL_ID FROM PFSMASTER.DD_ECL_ESTADO_CLIENTE WHERE DD_ECL_CODIGO = '1')
WHERE BORRADO = 0;
-- Cliente cancelado
UPDATE CLI_CLIENTES
SET DD_ECL_ID = (SELECT DD_ECL_ID FROM PFSMASTER.DD_ECL_ESTADO_CLIENTE WHERE DD_ECL_CODIGO = '2')
WHERE BORRADO = 1;

-- Restricciones de NO NULO
ALTER TABLE CLI_CLIENTES
MODIFY(CLI_FECHA_CREACION  NOT NULL);

ALTER TABLE CLI_CLIENTES
 MODIFY (CLI_TELECOBRO NUMBER(1) DEFAULT 0 NOT NULL);

commit;