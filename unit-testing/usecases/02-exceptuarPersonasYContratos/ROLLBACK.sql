SELECT * FROM BATCH_DATOS_EXCEPTUADOS;  

SELECT * FROM BATCH_DATOS_EXP;

SELECT * FROM BANKMASTER.DD_MOB_MOTIVOS_BAJA;

SELECT * FROM CRC_CICLO_RECOBRO_CNT WHERE CRC_FECHA_BAJA IS NOT NULL;

SELECT * FROM CRP_CICLO_RECOBRO_PER WHERE CRE_ID IN (SELECT CRE_ID FROM CRC_CICLO_RECOBRO_CNT WHERE CRC_FECHA_BAJA IS NOT NULL);

SELECT * FROM CPE_CONTRATOS_PERSONAS WHERE PER_ID = 1110062;

SELECT * FROM CRC_CICLO_RECOBRO_CNT WHERE CRE_ID = 122;

SELECT * FROM CRP_CICLO_RECOBRO_PER WHERE CRE_ID = 122;



UPDATE CRC_CICLO_RECOBRO_CNT 
  SET CRC_FECHA_BAJA = NULL
    , EXC_ID = NULL
    , DD_MOB_ID = NULL
WHERE CRC_FECHA_BAJA IS NOT NULL;

UPDATE CRP_CICLO_RECOBRO_PER 
  SET CRP_FECHA_BAJA = NULL
    , EXC_ID = NULL
    , DD_MOB_ID = NULL
WHERE CRP_FECHA_BAJA IS NOT NULL;

COMMIT;